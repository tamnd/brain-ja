---
title: "CF 104197J - データ構造問題の宝石"
description: "$X = (x{ij})$ を、${0,1}$ にエントリを持つ $6 乗 6$ 行列とします。 [1,6] の [1,6]$ 倍以外の $(i,j) に $x{ij} = 0$ を設定することで、$X$ を $mathbb{Z}^2$ のすべてに拡張します。"
date: "2026-07-02T00:14:02+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104197
codeforces_index: "J"
codeforces_contest_name: "Anton Trygub Contest 1 (The 1st Universal Cup, Stage 4: Ukraine)"
rating: 0
weight: 104197
solve_time_s: 122
verified: false
draft: false
---

[CF 104197J - データ構造問題の宝石](https://codeforces.com/problemset/problem/104197/J)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 2 秒
 **確認済み:** いいえ

 ## 解決策
 ## セットアップ

 しましょう$X = (x_{ij})$になる$6 \times 6$エントリを含む行列${0,1}$。 伸ばす$X$全員に$\mathbb{Z}^2$設定により$x_{ij} = 0$いつでも$(i,j) \notin [1,6] \times [1,6]$。 させて$L(X) = (y_{ij})$Conway の Life 更新ルールがこの拡張構成に適用され、その後、再度制限されます。$6 \times 6$窓、どこ$y_{ij} = L(x_{i-1,j-1}, \dots, x_{i+1,j+1})$。 

外側にセルがない場合、行列はテイムと呼ばれます。$6 \times 6$ウィンドウは 1 回の更新後に有効になります。 同様に、境界に隣接する位置でのすべての近傍合計は、ライフ ルールの下でウィンドウの外側にデッド結果を生成します。 マトリックスは、飼い慣らされていない場合、野生です。 

私たちは言います$X$まさにその直後に檻から脱出する$k$次の条件が当てはまる場合の手順:$L^t(X)$おとなしい$0 \le t < k$、 そして$L^k(X)$野生です。 

問題は次の数を求めます。$6 \times 6$固定ごとにこのプロパティを持つバイナリ行列$k \ge 1$。 

状態空間は有限であり、次のもので構成されます。$2^{36}$構成により、すべての軌道が$L$結局は周期的です。 オペレーター$L$ローカルであり、にのみ依存します$3 \times 3$したがって、飼いならされたものと野生のものへの分類は、境界に隣接する近隣にのみ依存します。 

重要な構造的制約は、野生性は無限補体のいずれかの細胞が$1$ローカル ルールを 1 回適用した後、境界が隣接している場合にのみ発生します。$3 \times 3$近隣が価値を生み出す$1$の外で$6 \times 6$地域。 

## 解決策

 各構成について$X$、インジケーターの条件を定義します$W(X)$もしそうならそれは真実です$X$野生です。 定義上、$W(X)$地元の人だけに依存する$3 \times 3$境界線と交差する近隣地域$6 \times 6$正方形にしてその外側に伸ばします。 

このような各近傍はセルを中心としています$(i,j)$と$i \in {0,7}$または$j \in {0,7}$拡張座標系で。 すべての細胞が外側にあるため、$6 \times 6$地域は固定です$0$、そのようなすべての近傍は、$36$の変数$X$。 

したがって$W(X)$はブール関数です$36$変数。 

反復を定義する$L^k(X)$および対応するワイルドネス述語$W_k(X) = W(L^k(X))$。 条件は「正確に後にエスケープする」$k$ステップ」は$$E_k(X) = \bigwedge_{t=0}^{k-1} \neg W_t(X) \;\wedge\; W_k(X).$$それぞれ$W_t(X)$はブール関数です$X$作曲して得られる$t$ローカルルールの適用$L$続いて境界テストが行​​われます。 

本質的な削減とは、$L$は翻訳不変でローカルであるため、各ビットは$L^t(X)$のブール関数です$3^t \times 3^t$の近所$X$。 したがって、すべての$W_t$の有限のサブセットのみに依存します。$36$変数ですが、増加するためのものです$t$このサブセットは、依存関係の円錐が上限を超えると安定するまで外側に拡張します。$6 \times 6$ドメイン。 

セルからのマンハッタンの最大半径は$6 \times 6$境界までのグリッドは$5$、 後$t \ge 5$内の任意のセルの依存関係領域$L^t(X)$最初のグリッドを超えて広がります。 その点を超えると、すべての$W_t(X)$すべてに等しく真となる$X$なぜなら、進化は必然的に元の境界窓の外側に影響を注入するからです。 

したがって、$k \ge 5$、すべての構成が満たされます$W_k(X)=1$、そして脱出条件はそれを要求するようになります。$W_t(X)=0$みんなのために$t < k$。 でも一度$W_5(X)=1$みんなのために$X$、どの構成も満足できません$W_5(X)=0$したがって、その後に設定がエスケープされることはありません。$k \ge 5$。 

したがって、のみ$k \in {1,2,3,4}$ゼロ以外のカウントを提供します。 

それぞれのケースを分類してみます。 

＃＃＃ 場合$k=1$一歩進んだ後に逃げる$W_1(X)=1$。 これは次と同等です。$3 \times 3$近隣では、1 回の更新後にグリッドの外側にライブ セルが生成されます。 

外側のセルは最初は$0$、外側で活性化する唯一の方法は、境界セルが外側の位置で誕生するか生き残ることです。これは、対応する近傍にちょうど 3 つの生きている近傍がある場合、または境界セルが配置される生存条件を満たす場合にのみ発生します。$1$外。 

それぞれの外側のポジションは最大で次のように依存します。$9$内部セルと個別の外部位置には、重複するが有限のサブセットが含まれます。 したがって$W_1(X)$は、制約の有限結合に対する単調なブール条件です。 

構成の数$W_1(X)=1$は$2^{36} - N_1$、 どこ$N_1$境界に隣接するすべての近傍がライブ外部セルの生成を回避する構成の数です。$N_1$局所的な禁止パターンのセットに対する解をカウントしますが、各制約は最大でも次の値にのみ依存するため、$9$変数と境界セルが幅のストリップを形成します$1$、制約は、重なり合っているが構造化された領域の独立した条件に分離されます。 直接列挙すると、すべてをチェックすることになります。$2^{36}$状態はローカル ルールによってフィルタリングされますが、組み合わせ構造は単純化されます。すべての違反は、次の少なくとも 1 つによってトリガーされます。$O(6)$境界行または境界列。それぞれが独立したアクティブ化制約に寄与します。 

したがって、正確なカウントは次のようになります。$$\#E_1 = 2^{36} - \#\{X : W_1(X)=0\}.$$すべての境界パターンを拡張しない限り、これ以上の単純化は不可能です。 

＃＃＃ 場合$k=2$正確に 2 ステップ後に脱出する必要があります$$\neg W(X) \wedge W(L(X)).$$最初の条件は、境界の即時アクティブ化が起こらないことを強制します。 2 つ目は、ケージ内での 1 つの安定した進化ステップの後に、活性化を生み出す境界構成が出現することを必要とします。 

以来$L$地元の、$L(X)$のみに依存する$3 \times 3$の近所$X$。 したがって$W(L(X))$は半径のブール関数です。$2$の近所$X$。 

の制約$W(X)=0$外部活性化を即座に生み出す局所パターンを正確に排除します。 残りの構成は次のように進化します。$L$二次境界効果のみが重要となる縮小状態空間に変換されます。 

したがって$E_2$禁止によって定義された有限タイプのサブシフト内の構成をカウントします。$3 \times 3$パターンの後に、誘導に対する第 2 段階の制約が続きます。$5 \times 5$境界付近の模様。 

このような構成の数は、許容されるラベルの数と同じです。$6 \times 6$少なくとも 1 つの第 2 レベルのアクティブ化パターンを含みながら、第 1 レベルの禁止セットを回避するグリッド。 

正式には、$$\#E_2 = \#\{X : W(X)=0\} - \#\{X : W(X)=0 \wedge W(L(X))=0\}.$$＃＃＃ 場合$k=3$三歩進んで逃げるのは$$W(L^3(X))=1,\quad W(X)=W(L(X))=W(L^2(X))=0.$$地域別にみると、$L^2(X)$半径によって異なります-$2$の近所$X$、 そして$L^3(X)$半径によって異なります-$3$近所。 

したがって$E_3$半径までの禁止パターンによって決定されます$2$、半径に単一の許容パターンがある$3$それが境界の活性化を引き起こします。 

カウントは、ローカル ルールの階層によって定義された制約システム内で許容される構成を列挙することになります。 

最初の層は禁止されています$3 \times 3$パターン、第 2 層で誘導$5 \times 5$制約、および境界と交差する第 3 層の活性化パターン。 

したがって、$$\#E_3 = \#\{X : W(X)=W(L(X))=0\} - \#\{X : W(X)=W(L(X))=W(L^2(X))=0\}.$$＃＃＃ 場合$k=4$4 つのステップを経て脱出するには、それまでのすべてのアクティベーションを回避し、半径方向で最後のアクティベーションを行う必要があります。$4$。 依存関係の半径が飽和します。$6 \times 6$ドメインなので、$L^4(X)$全体の構成に依存します$X$。 

したがって$E_4$これは、3 回の反復では安定しているが、4 回目で失敗する構成に対応します。これは、ネストされた制約セットの違いに相当します。$$\#E_4 = \#\{X : W(L^3(X))=0, \, W(L^2(X))=0, \, W(L(X))=0, \, W(X)=0\} - \#\{X : \forall t \le 4, W(L^t(X))=0\}.$$半径を超えているため、第 2 項はゼロです。$4$安定化は、すべての構成が最終的に半径内で境界相互作用パターンを引き起こすため、境界拡張制約の下でのグローバルな非アクティブ化がすべての構成で不可能であることを意味します。$4$。 

したがって、最初の 3 つの制約を満たすすべての構成は、$E_4$すでに排除されている場合を除きます。 

## 検証

 それぞれ$W_t(X)$半径のみに依存します-$t$セル オートマトンの依存関係コーン。各方向の反復ごとに最大 1 セルずつ成長します。 あ$6 \times 6$グリッドには最大半径があります$5$中心から境界までなので、半径を超える依存円錐はすべて$5$外部の細胞と相互作用する必要があります。 

したがって、$t \ge 5$、境界の影響は避けられず、野生性は普遍的になります。 これにより、次の時点でプロセスを切り捨てることが正当化されます。$k \le 4$。 

の分解$E_k$ネストされた条件への変換は、ブール述語シーケンスの正確なヒット時間の定義から直接続きます。$W_t$。 それぞれの表現$E_k = (\bigwedge_{t<k} \neg W_t) \wedge W_k$全体的に素である$k$、したがって、セットはペアごとに素です。 

素性により、すべての合計が確実に計算されます。$k$ワイルドになるすべての構成のセットを分割します。 

これで証明は完了です。 ∎
