---
title: "CF 102277F - さまざまな方法で丸める"
description: "正の整数 N が与えられます。これは、未知の丸め操作の後に表示される値です。 丸め単位として使用できるすべての正の整数 X を決定する必要があります。 丸め単位 X には 2 つの制限があります。"
date: "2026-08-16T19:35:30+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102277
codeforces_index: "F"
codeforces_contest_name: "UCF Locals 2018"
rating: 0
weight: 102277
solve_time_s: 72
verified: true
draft: false
---

[CF 102277F - さまざまな方法での丸め](https://codeforces.com/problemset/problem/102277/F)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 12 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 正の整数が与えられます`N`これは、未知の丸め操作の後に表示された値です。 考えられるすべての正の整数を決定する必要があります`X`丸め単位として使用できた可能性があります。 

丸め単位`X`には 2 つの制限があります。 初め、`X`は 10 の累乗を除算する必要があるため、可能な素因数は 2 と 5 だけです。`X`形式がなければなりません`2^a * 5^b`いくつかの非負の整数の場合`a`そして`b`。 2 番目に四捨五入された値`N`それ自体が次の倍数でなければなりません`X`。 元の統計は単純に次のようになった可能性があります`N`, したがって、この 2 つの条件も十分です。 公式声明では、すべてを見つけるというタスクを明示的に削減しています。`X`これら 2 つの割り切れる条件を満たしていること。 

入力には 1 つの整数が含まれています`N`、 と`1 <= N <= 10^18`。 出力では、最初に次の有効な値の数が示されます。`X`、その後、それらの値を昇順に 1 行に 1 つずつ出力します。 公開されているサンプルは、`N = 30`、`N = 120`、 そして`N = 8`。 

の上限`10^18`以下の値まですべての可能な値をスキャンするものを除外します。`N`。 たとえ`O(sqrt(N))`除数の検索には約次の値が必要になる場合があります`10^9`これは 1 秒の制限をはるかに超えています。 便利な構造は、次の 2 と 5 の指数のみを考慮することです。`N`、それらの指数はそれぞれ最大 59 と 25 です。 

より機械的な除数ベースのソリューションを騙す可能性のあるエッジケースがいくつかあります。 のために`N = 1`、正しい出力は次のとおりです。`1`に続く`1`。 2 で検索を開始するソリューションは、たとえ`X = 1`は常に有効です。 

のために`N = 8`、正しい出力は次のとおりです。`4`、 に続く`1`、`2`、`4`、 そして`8`。 10 の累乗のみを考慮したソリューションでは失敗します。`2`、`4`、 そして`8`有効な単位は、それ自体が 10 の累乗である数値だけでなく、素因数が 2 と 5 に制限されているすべての数値であるためです。 

のために`N = 30`、正しい出力は次のとおりです。`4`、 に続く`1`、`2`、`5`、 そして`10`。 次のような値`3`、`6`、 そして`15`は 30 の約数ですが、素因数 3 が含まれているため、10 のべき乗を割り算できません。任意の約数を列挙する解`N`それらをすべて受け入れると、不正確な答えが生成されます。 

のために`N = 120`、正しい値は次のとおりです。`1, 2, 4, 5, 8, 10, 20, 40`。 ここで 2 の指数`N`は 3 で、5 の指数は 1 なので、すべての組み合わせは`2^a * 5^b`と`0 <= a <= 3`そして`0 <= b <= 1`有効です。 これにより、`(3 + 1)(1 + 1) = 8`と答える。 

## アプローチ

 直接的な総当たりアプローチでは、すべての整数が試行されます。`X`1から`N`、どうかを確認してください`X`分割する`N`、そして、次のことを確認します。`X`には、2 と 5 以外の素因数はありません。2 つのチェックは正確に 2 つの数学的要件であるため、これは正しいです。 問題は範囲です。 のために`N = 10^18`、このようなループは実行します`10^18`繰り返しますが、これは完全に不可能です。 

もう少し優れたブルートフォース除数アプローチでは、次の約数を列挙します。`N`までのすべての整数をチェックすることで、`sqrt(N)`。 これにより、最悪のケースはおおよそ次のようになります。`10^9`反復のとき`N`近いです`10^18`。 これでも、1 秒のコンテストの問題としては遅すぎます。 

重要な観察は、次の形式を持つ場合にのみ、数値が 10 のべき乗を割ることです。`X = 2^a * 5^b`。 

の素因数分解を仮定します。`N`含まれています`N = 2^A * 5^B * R`、

どこ`R`は 2 または 5 で割り切れません。`X`分けなければなりません`N`、その指数は次の指数を超えることはできません。`N`。 したがって、すべての有効な答えは次のいずれかになります。`2^a * 5^b`、 どこ`0 <= a <= A`そして`0 <= b <= B`。 

残りを因数分解する必要はありません`N`全然。 分けていくだけを繰り返す`N`2までに見つける`A`を繰り返し、5 で割って求めます。`B`。 

ブルート フォースは、可能なすべてのユニットを明示的にテストするため機能しますが、数値範囲が膨大であるため失敗します。 有効な単位には素数 2 と 5 のみが含まれるという観察により、最大 2 までの検索を置き換えることができます。`10^18`最大約 60 の可能な 2 の累乗と 26 の可能な 5 の累乗を検索する整数。 

すべてを生成したら、`2^a * 5^b`、結果の値を並べ替えて出力します。 生成される候補の数は多くてもごくわずかです`(59 + 1)(25 + 1) = 1560`。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォースオーバー`1..N`|`O(N log N)`簡単な実装では |`O(1)`出力を除く | 遅すぎる |
 | 除数検索まで`sqrt(N)`|`O(sqrt(N))`|`O(1)`出力を除く | 遅すぎる |
 | 最適な指数の列挙 |`O(log N + K log K)`|`O(K)`| 承認済み |

 ここ`K`は有効な値の数です`X`、 そして`K <= 1560`指定された境界に対して。 

## アルゴリズムのチュートリアル

 1.読む`N`そしてそれを 2 で何倍にするかを決定します。 から始める`a = 0`そして分割を繰り返す`N`偶数の間は2ずつ増加します`a`毎回。 

最後に、`a`は、元の関数を素因数分解したときの 2 の指数です。`N`。 
2. 5 についても同じことを行います。可能な限り、残りの数を繰り返し 5 で割って増加させます。`b`。 

必要なのはこれら 2 つの指数だけです。`X`2 と 5 以外の素因数を含めることはできません。 
3. すべての指数のペアを生成する`i`そして`j`満足のいく`0 <= i <= a`そして`0 <= j <= b`。 

各ペアについて、次のように構築します。`2^i * 5^j`そしてそれを保管します。 そのようなすべての値が分割されます`N`、どちらの指数も の対応する指数を超えないため、`N`。 
4. 生成された値を並べ替えます。 

入れ子になったループでは、当然、数値の大きさではなく指数によって値が列挙されるため、その生成順序は必ずしも増加するわけではありません。 ソートにより、問題に必要な出力順序が正確に得られます。 
5. 生成された値の数を出力し、各値をそれぞれの行に出力します。 

### なぜ効果があるのか

 不変条件は、生成されるすべての値が次の形式を持つことです。`2^i * 5^j`の指数以下の指数を持つ`N`。 したがって、生成されるすべての値は両方を除算します。`N`と十分に大きい 10 の累乗なので、両方の必要な条件を満たします。 

逆に、有効な任意の値を取得します。`X`。 以来`X`10 の累乗を除算しますが、その素因数分解には 2 と 5 のみを含めることができます。`X = 2^i * 5^j`。 以来`X`も分割します`N`、その指数は満たす`i <= a`そして`j <= b`。 アルゴリズムはそのペアを正確に列挙するため、有効なすべてのペアが列挙されます。`X`が生成されます。 2 つの方向を合わせると、生成されたセットがまさに必要な応答セットであることが証明されます。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve():
    n = int(input())

    x = n
    a = 0
    while x % 2 == 0:
        x //= 2
        a += 1

    b = 0
    while x % 5 == 0:
        x //= 5
        b += 1

    powers2 = [1] * (a + 1)
    for i in range(1, a + 1):
        powers2[i] = powers2[i - 1] * 2

    powers5 = [1] * (b + 1)
    for j in range(1, b + 1):
        powers5[j] = powers5[j - 1] * 5

    ans = []
    for p2 in powers2:
        for p5 in powers5:
            ans.append(p2 * p5)

    ans.sort()

    out = [str(len(ans))]
    out.extend(map(str, ans))
    sys.stdout.write("\n".join(out))

if __name__ == "__main__":
    solve()
```最初のループは 2 の指数を抽出します。`N`。 2 番目のループでは、残りの値から 5 の指数を抽出します。 2 と 5 は異なる素数であるため、5 の因数を数える前に 2 の因数を削除しても、5 の指数には影響しません。 

2 つのべき乗配列により、入れ子になったループ内で累乗を繰り返し計算することが回避されます。 彼らは小さいので、`N <= 10^18`。 実際には、`2^60 > 10^18`そして`5^26 > 10^18`したがって、答えに関連する 2 のべき乗は最大 60 個、5 のべき乗は 26 個あります。 

Python の整数はオーバーフローしませんが、生成された積も次の制限を受けます。`N`、すべてが生成されるため、`2^i * 5^j`分割する`N`。 指数は包含境界を使用します。これは次の理由から必要です。`X`それ自体に、存在する 2 または 5 のすべての累乗が含まれる場合があります。`N`。 

指数の順序は数値の順序と等しくないため、ソート手順が必要です。 たとえば、`N = 120`、ペア`(3, 0)`8を生成しますが、`(0, 1)`5を生成します。 

## 実用的な例

 ### サンプル 1

 のために`N = 30`, 2 と 5 に関してのみ数値を因数分解します。 

|`a`|`b`|`2^a`|`5^b`| 生成される値 |
 | --- | --- | --- | --- | --- |
 | 1 | 1 | 1、2 | 1、5 | 1、5、2、10 |

 並べ替え後の値は次のようになります。`1, 2, 5, 10`。 

2 の指数は 1、5 の指数は 1 となり、次のようになります。`(1 + 1)(1 + 1) = 4`候補者たち。 すべては 30 を除算し、2 と 5 以外に素因数はありません。したがって、出力は次のようになります。```
4
1
2
5
10
```この例は、任意の約数では十分ではない理由を示しています。 約数 3 は 10 のべき乗を割り切れないため無効です。 

### サンプル 2

 のために`N = 120`、関連する因数分解は次のとおりです。`120 = 2^3 * 5^1 * 3`。 

因数 3 は、`X`。 

|`i`|`2^i`|`j`|`5^j`|`X`|
 | --- | --- | --- | --- | --- |
 | 0 | 1 | 0 | 1 | 1 |
 | 0 | 1 | 1 | 5 | 5 |
 | 1 | 2 | 0 | 1 | 2 |
 | 1 | 2 | 1 | 5 | 10 |
 | 2 | 4 | 0 | 1 | 4 |
 | 2 | 4 | 1 | 5 | 20 |
 | 3 | 8 | 0 | 1 | 8 |
 | 3 | 8 | 1 | 5 | 40 |

 これらの値を並べ替えると、`1, 2, 4, 5, 8, 10, 20, 40`。 

2 つの指数範囲では、2 の累乗については 4 つの選択肢があり、5 の累乗については 2 つの選択肢があるため、答えは 8 つあります。 これにより、指数範囲の積が不変であることが確認されます。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 |`O(log N + K log K)`| 2テイクと5テイクによる因数分解`O(log N)`、答えを生成するには時間がかかります`O(K)`、ソートには時間がかかります`O(K log K)`。 |
 | スペース |`O(K)`| 回答リストと 2 つの小さな累乗配列は、有効な値の数に比例したスペースを使用します。 |

 のために`N <= 10^18`、候補は最大 1560 個あるため、並べ替えコストは無視できます。 このアルゴリズムは、数十回の除算を実行し、その後に非常に小規模な並べ替えを実行するだけで、この問題で規定されている 1 秒の時間制限と 256 MB のメモリ制限に快適に適合します。 

## テストケース```python
# helper: run solution on input string, return output string
import sys
import io

def solve():
    input = sys.stdin.readline
    n = int(input())

    x = n
    a = 0
    while x % 2 == 0:
        x //= 2
        a += 1

    b = 0
    while x % 5 == 0:
        x //= 5
        b += 1

    powers2 = [1] * (a + 1)
    for i in range(1, a + 1):
        powers2[i] = powers2[i - 1] * 2

    powers5 = [1] * (b + 1)
    for j in range(1, b + 1):
        powers5[j] = powers5[j - 1] * 5

    ans = []
    for p2 in powers2:
        for p5 in powers5:
            ans.append(p2 * p5)

    ans.sort()

    out = [str(len(ans))]
    out.extend(map(str, ans))
    return "\n".join(out)

def run(inp: str) -> str:
    old_stdin = sys.stdin
    sys.stdin = io.StringIO(inp)
    try:
        return solve()
    finally:
        sys.stdin = old_stdin

# Provided samples
assert run("30\n") == "4\n1\n2\n5\n10", "sample 1"
assert run("120\n") == "8\n1\n2\n4\n5\n8\n10\n20\n40", "sample 2"
assert run("8\n") == "4\n1\n2\n4\n8", "sample 3"

# Minimum-size input
assert run("1\n") == "1\n1", "minimum N"

# All relevant factors are powers of 2
assert run("64\n") == "7\n1\n2\n4\n8\n16\n32\n64", "power of 2"

# Only one factor of 2 and many factors of 5
assert run("1250\n") == "12\n1\n2\n5\n10\n25\n50\n125\n250\n625\n1250", "mixed exponents"

# Maximum-size input
assert run("1000000000000000000\n") == (
    "361\n" +
    "\n".join(
        str((2 ** i) * (5 ** j))
        for value in sorted(
            (2 ** i) * (5 ** j)
            for i in range(19)
            for j in range(19)
        )
    )
), "maximum N"
```上記の最大サイズのアサーションは、ハードコーディングされているのではなく、意図的に生成されています。 以来`10^18 = 2^18 * 5^18`、まさにあります`19 * 19 = 361`有効な値が含まれており、式は期待されるセットを独立して構築します。 

| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`1`|`1, 1`| 最小限の入力と包含`X = 1`|
 |`64`|`1, 2, 4, 8, 16, 32, 64`| 2 のべき乗の包含的な指数境界 |
 |`1250`|`1, 2, 5, 10, 25, 50, 125, 250, 625, 1250`| 2 と 5 の混合累乗 |
 |`10^18`| 361 の値 | 最大入力と最大指数の範囲 |

 ## 特殊なケース

 のために`N = 1`、アルゴリズムはどちらの因子計数ループにも入りません。`a = b = 0`。 ネストされた列挙にはペアが 1 つだけあります。`(0, 0)`、生産`2^0 * 5^0 = 1`。 出力は`1`に続く`1`最も近い 1 の倍数に四捨五入することは常に有効な方法であるため、これは正しいです。 

のために`N = 8`、問題に関連する因数分解は次のとおりです。`2^3`。 アルゴリズムが取得するのは、`a = 3`そして`b = 0`、次に生成します`1, 2, 4, 8`。 出力は`4`その後にこれら 4 つの値が続きます。 これは、「10 の累乗を割る」を「10 の累乗である」と解釈するというよくある間違いを捉えています。 

のために`N = 30`、関連する指数は次のとおりです。`a = 1`そして`b = 1`。 生成されたセットは、`{1, 2, 5, 10}`。 30 には他にも 3、6、15 などの約数がありますが、それぞれに 3 の因数が含まれているため、10 の累乗を割り切れるものはありません。アルゴリズムがこれらの約数を生成することはありません。 

のために`N = 120`、指数は`a = 3`そして`b = 1`。 4 つの可能な 2 のべき乗が 2 つの可能な 5 のべき乗と組み合わされて、正確に 8 つの値が生成されます。 最大の候補は、`2^3 * 5 = 40`2 と 5 のみを含む次に大きい数 80 は、2 の因数が 4 つ必要であるため、120 を除算しません。これは、両方の指数の範囲が包含的である必要がありますが、超えてはいけない理由を示しています。 

のために`N = 10^18`、因数分解は`2^18 * 5^18`。 すべてのペア`(i, j)`と`0 <= i, j <= 18`は有効で、正確に 361 個の回答が得られます。 これは、入力範囲内で関連する最大の指数の組み合わせであり、次の場合でも指数ペアの列挙が小さいままである理由を示しています。`N`それ自体が巨大です。 

本質的な削減は、元の丸めのストーリーでは、未知の元の統計を再構築する必要がないことです。 一度`X`分割する`N`、元の統計を選択できます。`N`それ自体なので、問題全体は次の約数を見つけることになります。`N`その素因数は 2 と 5 に制限されています。これにより、潜在的に巨大な約数の問題が 2 つの素指数に関する小さな列挙に変わります。
