---
title: "CF 104842G - ゲーム・ウィズ・ストーンズ"
description: "$n$ 個の箱の列と、それらに分散された $m$ 個の同一の石の初期構成がいくつか与えられました。 各構成は単純に $n$ の非負整数の配列であり、その合計は $m$ に固定されます。"
date: "2026-06-28T11:32:26+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104842
codeforces_index: "G"
codeforces_contest_name: "2020-2021 ICPC, Moscow Subregional"
rating: 0
weight: 104842
solve_time_s: 26
verified: false
draft: false
---

[CF 104842G - ストーンズとのゲーム](https://codeforces.com/problemset/problem/104842/G)

 **評価:** -
 **タグ:** -
 **解決時間:** 26 秒
 **確認済み:** いいえ

 ## 解決策
 ## 問題の理解

 私たちに与えられた一行は、$n$ボックスといくつかの初期構成$m$同じ石がそれらの全体に分布しています。 各構成は単に次の配列です。$n$合計が固定されている非負の整数$m$。 

1 回の移動で、ボックスから石を 1 つ取り出して、隣接するボックスに移すことができます。 2 つの構成間のコストは、あるディストリビューションを別のディストリビューションに変換するために必要なユニット移動の最小回数です。 これは、エッジを横切って 1 つのユニットを移動するのに 1 コストかかる、パス グラフに沿って質量を移動するのとまったく同じです。 

私たちは与えられています$k$このような構成。 タスクは新しい構成を選択することです$b$(有効な配布物でもあります)$m$石）により、あらゆる特定の構成から$b$。 

したがって、線上の Earth Mover Distance の下の「中央分布」を検索します。 

制約が意味するのは、$n, k \le 1000$そして$m \le 10^9$。 各配列の合計は次のようになりますが、$m$、$m$個々の石をシミュレートするには大きすぎます。 配布をユニットトークンに拡張する解決策は直ちに不可能です。 プレフィックスフロー表現または累積数量を操作する必要があります。$O(nk)$またはそれ以上です。 

重要な構造上のエッジケースは、分布が高度に集中している場合に現れます。 たとえば、あるディストリビューションではすべての石をボックス 1 に配置し、別のディストリビューションではボックスにすべての石を配置することができます。$n$。 座標ごとの中央値を取得しようとする単純な平均化アプローチは、コストが座標ごとに分離できないため失敗します。 移動質量は位置を越えて相互作用します。 

もう 1 つの微妙なケースは、複数の最適な答えが存在する場合です。 この問題では任意のものが許可されるため、一意性に依存することは避けなければなりません。 

## アプローチ

 強引な解釈では、考えられるすべての分布を列挙することになります。$b$。 弱い組成の数$m$の中へ$n$部品は$\binom{m+n-1}{n-1}$、これは天文学的に大きい$m = 10^9$。 候補者を立てることすら不可能なので、構造改革が必要だ。 

重要な観察点は、回線のコストが$W(a, b)$は、プレフィックス合計の絶対差のエッジにわたる合計として解釈できます。 定義すると$$A_i(x) = \sum_{t \le x} a_i[t],
\quad
B(x) = \sum_{t \le x} b[t],$$その場合、交通費は$$W(a_i, b) = \sum_{x=1}^{n-1} |A_i(x) - B(x)|.$$これにより、問題は機能の選択に変換されます$B(x)$制約付き$B(x)$減少しない、$B(n) = m$、各位置での絶対偏差の合計を最小化します。 

現在、問題はポジションを超えて切り離されています$x$の単調性制約を除きます。$B(x)$。 ボックス間の各カットで、効果的に値を選択しています。$B(x)$までの距離の合計を最小化するもの$A_1(x), \dots, A_k(x)$。 固定の場合$x$これは古典的な事実です。絶対偏差の合計の最小値はマルチセットの中央値です。 

したがって、局所的には、各位置のプレフィックス合計の中央値が最適な選択となります。 残りの問題は、ポジション間の一貫性を確保することです。$B(x)$減少していなければなりません。 

これにより、単調性に違反する可能性のある一連の中央値が得られます。 修正は、このシーケンスを非減少シーケンスの空間に投影することです。これは、次の場合とまったく同じ等張回帰です。$L_1$損失。 この設定では、位置を左から右に処理し、中央値が順序に違反するセグメントをマージする構造を維持することによって、最適なソリューションを取得できます。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ディストリビューションのブルートフォース列挙 | 指数関数的に$m$| 大 | 遅すぎる |
 | 中央値プレフィックス + アイソトニック マージ |$O(kn)$|$O(kn)$| 承認済み |

 ## アルゴリズムのチュートリアル

 各分布をそのプレフィックス合計配列に変換します。 

1. プレフィックスの合計を計算する$A_i(x)$すべての入力分布に対して。 これにより、各構成が各位置までの累積質量に変換されます。 
2. ポジションごとに$x$、値を収集します$A_1(x), A_2(x), \dots, A_k(x)$。 それらを並べ替えるか、中央値を抽出できる構造を維持してください。 中央値は局所的に最適な値を示します。
