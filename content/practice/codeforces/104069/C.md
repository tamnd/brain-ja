---
title: "CF 104069C - CCM"
description: "$M2(x1,x2,x3,x4)$ が 4 ウェイ マルチプレクサを表すものとします。 データ変数は $x3x4$ 、選択変数は $x1x2$ です。 {0,1,2,3}$ の $j について、$j$ をバイナリで $x1x2 として {00,01,10,11}$ に書き込み、対応するデータ ビットを $x{2+j}$ とします。"
date: "2026-07-02T03:00:08+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104069
codeforces_index: "C"
codeforces_contest_name: "VII MaratonUSP Freshman Contest"
rating: 0
weight: 104069
solve_time_s: 131
verified: false
draft: false
---

[CF 104069C - CCM](https://codeforces.com/problemset/problem/104069/C)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 11 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 しましょう$M_2(x_1,x_2,x_3,x_4)$は 4 ウェイ マルチプレクサを示します。 データ変数は次のとおりです。$x_3x_4$、選択変数は次のとおりです。$x_1x_2$。 のために$j \in {0,1,2,3}$、 書く$j$バイナリとして$x_1x_2 \in {00,01,10,11}$対応するデータビットを次のようにします$x_{2+j}$。 したがって$$M_2 = \bar{x}_1\bar{x}_2x_3 \;\vee\; \bar{x}_1x_2x_4 \;\vee\; x_1\bar{x}_2x_5 \;\vee\; x_1x_2x_6,$$入力が順序付けされるセクション 7.1.4 の標準真理値表規則に合わせてインデックスを再作成した後$(x_1,x_2,x_3,x_4)$出力は辞書順に並べられます。$(x_1,x_2,x_3,x_4)$。 

Z 変換は、サイズのブロックへの再帰的分解によって真理値表上で定義されます。$2^{n-1}$サブテーブルの等価性または正方構造に従って置き換えます。 マルチプレクサの場合、真理値表には最初の変数を固定するという特別な特性があります。$x_1$関数をより小さいアリティの 2 つの独立したマルチプレクサーに分割します。 確かに、もし$x_1=0$、機能は 2 方向マルチプレクサに縮小されます。$(x_2,x_3,x_4)$から選択する$x_3$そして$x_4$。 もし$x_1=1$、再び 2 方向マルチプレクサに縮小されます。$(x_2,x_3,x_4)$から選択する$x_5$そして$x_6$、ただし、4 変数の場合、これは同じ構造パターンと同型です。 

真理値表レベルでは、定義的な特性は、表の各半分が ($x_1=0$または$x_1=1$) 自体は双方向マルチプレクサの真理値表であり、それぞれの半分内で次のように分割されます。$x_2$一定の長さのサブテーブルを生成します$2^{2}$単一の変数によって決定されます。 したがって、次の中に現れるすべての重要なサブテーブルは、$M_2$定数ブロックまたは射影と等しいブロックのいずれかです$x_i$。 

Z 変換は連結を置き換えます$\alpha\beta$どちらかの重複により$\alpha^Z\alpha^Z$、への吸収$0^n$、または等しい長さのコンポーネントの再帰的なペアリング。 この場合、次のサブテーブルはありません。$M_2$という形です$\alpha\alpha$ただし、定数ブロックの最上位レベルは除きます。これは、各選択分岐が素の変数依存関係を生成するためです。 したがって、すべての非定数サブテーブルは定義の 3 番目の句によって処理され、分解構造を保持しながら各変数ブロックをシングルトン依存関係の Z イメージとして再解釈します。 

これは、BDD 解釈のすべてのノードが$M_2$LO および HI サクセサが、互いに素な変数サブセットに対する同一の構造決定をエンコードする Z ノードに対応します。 の効果$Z$したがって、多重化階層を維持しながら、各決定ノードを ZDD スタイルの分解に変換します。ZDD スタイルの分解では、選択パスが値のバイナリ分岐ではなく、変数の包含または除外に対応します。 

以来$M_2$高さの完全な決定木にグループ化された 4 つの変数に対する単調選択関数です$2$、その Z 変換は、到達可能な決定パターンのセットを変更しません。 BDD 内のすべてのパス$M_2$これは 4 つのデータ変数のうちの 1 つを正確に選択することに対応し、Z 変換はこの選択構造を保持しながら、2 つの多重化レベル間の対称性から生じる同一の部分構造を折りたたみます。 

したがって$Z(M_2)$は、ZDD セマンティクスで解釈されるのと同じ多重化関数を表し、2 段階セレクターの固有の対称性を超えてさらなる縮小は発生しません。 したがって$Z(M_2)$Z は、元の 4 ウェイ マルチプレクサ構造と同等です。 

最小化のために、$Z_{\min}(M_2)$は、ZDD 表現内のすべての同型部分構造を識別することによって取得されます。 第 2 段階の選択に対応する 2 つの中間レベルの決定ノードは同一です。これは、どちらも同じ構造規則に基づいて終端変数間の 2 方向の選択をエンコードしているためです。 これらを折りたたむと、単一の代表的な下位決定が得られます。 同様に、終端定数サブツリーは 1 つに減ります。$\bot$そしてシングル$\top$、ZDD リダクションは同一のリーフをマージするためです。 

残りの各決定ノードには選択階層内で個別の変数インデックスと個別のサポートがあるため、これ以上の削減は不可能です。 それらをマージすると、ZDD で必要な順序付けされた構造に違反します。 したがって、$Z_{\min}(M_2)$は、2 つの対称的な中間ノードを識別し、ターミナル シンクを共有することによって取得される正規 ZDD です。 

最大化のために、$Z_{\max}(M_2)$は、構文的には異なるが意味的には同一の部分構造をすべて保持し、縮小する前に表現を拡張することによって取得されます。 最初のレベルの分割により、2 方向セレクターの 2 つのコピーが生成されます。 これらのそれぞれは、第 2 レベルの選択に対応するさらに 2 つのコピーに展開されます。 同一のサブ構造の識別は実行されないため、ブール関数と等しい場合でも、サブ関数の各出現は個別に表されます。 これにより、深さの完全なバイナリ拡張ツリーが生成されます。$2$すべてのノードに重複した部分構造があります。 

したがって$Z_{\max}(M_2)$は、マルチプレクサの完全な決定木から取得された非縮小 ZDD です。ここで、4 つの選択パスはそれぞれ独立して表され、各リーフは共有シンクではなく別個の終端ノードのオカレンスとして表示されます。 

したがって、3 つのケースはそれぞれ、Z での構造の保存、ZDD 削減と一致する最大の共有、および削減前の最大の重複に対応します。 これで証明は完了です。 ∎
