---
title: "CF 102267L - ABC"
description: "アルファベット a、b、c にわたる可変文字列があります。 a は ab に、b は bc に、c は ba に成長できます。 実際に文字を削除する唯一の操作は、abc の出現を削除することです。 この仕事は建設的なものです。"
date: "2026-08-17T19:40:23+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102267
codeforces_index: "L"
codeforces_contest_name: "The 2019 University of Jordan Collegiate Programming Contest"
rating: 0
weight: 102267
solve_time_s: 514
verified: false
draft: false
---

[CF 102267L - ABC](https://codeforces.com/problemset/problem/102267/L)

 **評価:** -
 **タグ:** -
 **解決時間:** 8 分 34 秒
 **確認済み:** いいえ

 ## 解決策
 ## 問題の理解

 アルファベットの上に可変文字列があります`a`、`b`、`c`。 アン`a`に成長することができます`ab`、`b`に成長することができます`bc`、そして`c`に成長することができます`ba`。 実際に文字を削除する唯一の操作は、出現した文字を削除することです。`abc`。 

この仕事は建設的なものです。 最大でも次の値を使用して、入力文字列を空の文字列に変換する有効な操作の完全なシーケンスを生成する必要があります。`3n`操作を実行するか、印刷によってそのようなシーケンスが存在しないことを証明します。`-1`。 出力内のインデックスは、その瞬間に存在する文字列を参照するため、実装では、以前の操作によって長さと位置がどのように変更されたかを追跡する必要があります。 

と`n`まで`2 * 10^5`1 秒という制限があるため、二次関数はすでに危険であり、演算シーケンスを探索することは完全に不可能です。 出力自体には最大で次のものを含めることができます。`600000`オペレーションなので、`O(n)`を使用した建設`O(n)`出力バッファが自然なターゲットです。 

なぜ盲目的に検索するのかを明らかにするいくつかのエッジケースがあります。`abc`十分ではありません。 入力`bac`不可能です。 その最初の文字は`b`そして、どの操作も文字列の最初の文字を次のように変換することはできません。`a`: 操作 2 は最初の値を保持します`b`最初として`b`、操作 3 が最初のターンになります`c`の中へ`b`、操作 1 は、`a`それはすでに存在します。 最初の文字は最終的には`abc`削除、で始まる文字列`b`または`c`消えることはありません。 

入力`abb`で始まっても無理です`a`。 最初`b`イニシャルと一緒に削除できます`a`、2番目を残して`b`最初のキャラクターとして。 それ`b`には決してなれない`a`, そのため、プロセスが停止します。 最初に利用可能なものを貪欲に削除する構造`abc`で始まるすべての文字列を想定するのではなく、この状況を検出する必要があります。`a`解決可能です。 

もう一方の極端では、`a`すぐに解決できます: に展開します`ab`を展開し、`b`に`bc`、削除します`abc`。 3 つの操作は正確に許容範囲内にあります。 シングル`abc`は 1 回の操作で削除できるため、さらに簡単です。 

## アプローチ

 直接的な総当たりアプローチでは、すべての操作が選択肢として扱われ、可能な長さまでのすべての操作シーケンスが検索されます。`3n`。 どの状態でも、拡張できる多くの文字が存在する可能性があります。`abc`削除する部分文字列。 文字列を操作するコストを無視しても、深さは`3n`一定の分岐係数を約 4 に設定した検索では、`O(4^(3n))`枝。 すべての正当なシーケンスが表現されるため、これは正しいですが、非常に小さな文字列の場合でも役に立ちません。 

有益な観察は、どちらを選択するかをグローバルに決定する必要はないということです。`abc`作成します。 文字列を左から右に処理して、すべてを削除できます。`c`すぐに。 前に考えられる 3 つのローカル エンディング`c`非常に単純な動作をします。 

現在のサフィックスが`ac`を変更して、`c`の中へ`ba`与える`aba`。 の`c`文字列の前半部分は変更されずに消えます。 

現在のサフィックスが`abc`、すぐに削除できます。 

現在のサフィックスが`bbc`、少し分かりにくいですが、次のように変更する 3 つの操作による変換があります。`bb`:`bbc -> bcbc -> bbabc -> bb`。 

最初の操作により最初の操作が変更されます`b`の中へ`bc`。 2 番目は新しく挿入されたものを変更します`c`の中へ`ba`。 結果として得られる`abc`その後削除されます。 周囲のプレフィックスは変更されません。 

これは、元の文字列をスキャンしているときに、プレフィックスを処理した後も、すべて処理された文字列を除いて、正確な現在の文字列を維持できることを意味します。`c`キャラクターはすでに削除されています。 結果として得られる補助文字列には、`a`そして`b`。 

一度すべて`c`文字が消えて、残っているすべての文字が消えます`b`以前のものとペアリングできます`a`。 現在残っているプレフィックスが次のように表されると仮定します。`g`、次の文字は`b`。 もし`g`空ではないので、これを展開します`b`に`bc`。 最後`a`の`g`今は形になっています`abc`これを使用すると、トリプルを削除できます。 これで1つ消費される`a`そして1つ`b`。 

もし`b`中に遭遇します`g`空です、それは`b`は実際の文字列の最初の文字になります。 上で説明したように、最初の`b`決して最初にはなれない`a`、したがって、答えは本当に不可能です。 

結局`b`文字だけが削除されました。`a`文字は残ります。 固定された 3 つの操作シーケンスにより、それぞれを独立して処理できます。`a -> ab -> abc -> empty`。 

コンテストの公式チュートリアルでは、これと同じ左から右の構造が使用されており、最初に省略されています。`c`文字、その後のマッチング`b`前に付いている文字`a`文字を作成し、最後に残りの文字を削除します`a`文字。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース | 最悪の場合は O(4^(3n)) | 指数 | 遅すぎる |
 | 最適 | O(n) プラス出力サイズ | O(n) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. 文字列を読み取り、リストを維持します。`v`すでに処理されたプレフィックスの後の現在の文字列を表します。 構築が完了した後にインデックスを出力できるように、すべての操作を配列に格納します。 
2. 入力を左から右に処理します。 のために`a`または`b`に追加します`v`。 これらの文字は後で処理できるため、まだ何もする必要はありません。 
3. 次に入力文字が入力されたとき`c`、まず次のことを確認してください。`v`は空です。 そうである場合、元の文字列は次で始まります。`c`, したがって、答えは不可能です。 最初の文字は決してなることができません`a`。 
4. 最後の文字が`v`は`a`、現在のサフィックスは`ac`。 操作 3 を`c`、インデックスを使用して`len(v) + 1`。 接尾辞は次のようになります`aba`ので追加します`b`そして`a`に`v`。 表現された現在の文字列が再び正しくなりました。 
5. 最後の文字が`v`は`b`そしてその前のキャラクター`a`、現在のサフィックスは`abc`。 操作 4 でこのサフィックスを削除します。最後の 2 文字を削除します。`v`、前回の`a`はすでに削除によって消費されています`b`そして現在`c`。 
6. 最後の 2 文字が`v`は`bb`、現在のサフィックスは`bbc`。 固定変換を適用する`bbc -> bcbc -> bbabc -> bb`。 3 つの運用指標は次のとおりです。`len(v) - 1`、`len(v)`、 そして`len(v) + 1`。 これらの操作後の文字列は次とまったく同じになります。`v`、したがって、変更はありません`v`必要です。 
7. この最初のパスの後、`v`のみが含まれます`a`そして`b`。 維持したまま再度スキャンしてください`g`、からなる部分`a`まだ消費されていない文字。 いつでも`a`と表示されるので追加します`g`。 
8. いつでも`b`と表示されるかどうかを確認してください。`g`は空です。 空いていればこれ`b`は現在の文字列の最初の文字であり、答えは不可能です。 それ以外の場合は、`b`操作 2 を使用します。結果は`c`最後の直後に座ります`a`で`g`ので、それを削除します`abc`操作4で最後のものを取り外します`a`から`g`。 
9. 毎回の後に`b`加工されていて、`g`一致しないものだけが含まれています`a`文字。 そういったものごとに`a`、位置 1 で操作 1、位置 2 で操作 2、位置 1 で操作 4 を実行します。各トリプルはそれを独立して回転させます。`a`空の文字列に入力します。 
10. 収集した操作を印刷します。 すべての元のキャラクターに関連付けられる操作は最大 3 つであるため、合計がこの値を超えることはありません。`3n`。 

なぜそれが機能するのか。 最初のパスでは、`v`すべての処理された入力文字が処理された後の現在の文字列の正確な表現です。 毎`c`は 3 つのローカル ケースの 1 つを使用して削除または変換され、各ケースでは未処理のサフィックスがそのまま残ります。 最初のパスの後は、いいえ`c`残っています。 2 回目のパスでは、`b`前の 1 つと一緒に削除されます`a`、 そして`g`前述のものを正確に表します`a`今も存在するキャラクターたち。 もし`g`のときは空です`b`表示されます、それ`b`は最初の文字であり、決してなることができません`a`、したがって、不可能であると宣言するのは正しいです。 最後に、残ったすべての`a`には、直接的な 3 つの操作の消去シーケンスがあります。 したがって、生成されたすべての操作は有効であり、アルゴリズムが報告するたびに`-1`、現在の最初の文字は、継続が不可能であることを証明しています。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve_string(s):
    operations = []
    v = []

    def add(tp, idx):
        operations.append((tp, idx))

    def impossible():
        return None

    # First pass: eliminate all c's.
    for ch in s:
        if ch != 'c':
            v.append(ch)
            continue

        if not v:
            return impossible()

        if v[-1] == 'a':
            # ...ac -> ...aba
            add(3, len(v) + 1)
            v.append('b')
            v.append('a')
        else:
            # v ends in b
            if len(v) == 1:
                # The current string starts with bc.
                return impossible()

            if v[-2] == 'a':
                # ...abc -> ...
                add(4, len(v) - 1)
                v.pop()
                v.pop()
            else:
                # ...bbc -> ...bb
                #
                # bbc -> bcbc -> bbabc -> bb
                add(2, len(v) - 1)
                add(3, len(v))
                add(4, len(v) + 1)

    # Second pass: remove every b using a preceding a.
    g = []

    for ch in v:
        if ch == 'a':
            g.append('a')
        else:
            if not g:
                return impossible()

            # ...a b -> ...abc -> ...
            add(2, len(g) + 1)
            add(4, len(g))
            g.pop()

    # Every remaining a can be removed independently:
    # a -> ab -> abc -> empty
    for _ in g:
        add(1, 1)
        add(2, 2)
        add(4, 1)

    return operations

def main():
    s = input().strip()
    operations = solve_string(s)

    if operations is None:
        print(-1)
        return

    out = [str(len(operations))]
    out.extend(f"{tp} {idx}" for tp, idx in operations)
    sys.stdout.write("\n".join(out))

if __name__ == "__main__":
    main()
```の`operations`リストには、操作タイプと 1 から始まるインデックスのペアが含まれます。 Python 文字列を変更する代わりに操作を維持すると、構築にはすでに処理されたプレフィックスのコンパクトな表現のみが必要であり、実際の出力インデックスは現在の長さから計算されるため便利です。 

最初のパスでは、`v`Python 文字列ではなくリストとして。 末尾への追加と末尾からの削除は定数時間の操作であり、繰り返し行われる文字列の再構築による偶発的な二次動作を回避します。 

の`ac`ケースは特に単純です。 処理する前に、`c`、`v`で終わる接頭辞が含まれています`a`、それで、`c`位置にあります`len(v) + 1`。 に変更した後、`ba`、表される接尾辞は次のとおりです。`aba`、まさにこれが 2 つの文字が追加される理由です。`v`。 

の`abc`case は、現在の文字列の最後の 3 文字を削除します。 以来`v`現在のものは含まれていません`c`、最後の 2 つの要素は`a`そして`b`そのトリプルの。 削除は次から始まります`len(v) - 1`、1 ベースのインデックスを使用します。 

の`bbc`大文字と小文字の区別は、インデックス付けの間違いを犯しやすい場所です。 加工前`c`、`v`で終わる`bb`したがって、現在のローカル文字列は次のようになります。`bbc`。 操作 2 は、これら 2 つのうちの最初の操作に適用されます`b`文字、で`len(v) - 1`。 を挿入します`c`、そして新しく挿入されたもの`c`位置にあります`len(v)`したがって、操作 3 ではまさにそのインデックスが使用されます。 その後、ローカル文字列は次のようになります。`bbabc`、そして`abc`から始まります`len(v) + 1`。 

2回目のパスでは、`g`生き残ったものだけが含まれています`a`文字。 もし`len(g) = k`、次`b`位置にあります`k + 1`。 操作 2 の後の結果は、`c`最後に続きます`a`、それで、`abc`位置から始まります`k`。 これで 2 つの指標が説明されます`len(g) + 1`そして`len(g)`。 

最後のループでは常にインデックスが使用されます`1`、`2`、 そして`1`。 最初の 2 つの操作の後、現在の文字列全体は次で始まります。`abc`を削除すると、選択したものが削除されます。`a`。 同じ 3 つのインデックスは、次に残るインデックスでも再度有効になります。`a`。 

Python の整数はオーバーフローせず、保存される操作の最大数は`3n`、せいぜい`600000`, これはメモリ制限内に余裕で収まります。 

## 実用的な例

 サンプル 1 の場合、`acab`、問題は有効なシーケンスを受け入れるため、構築ではサンプル出力を正確に再現する必要はありません。 私たちの施工ではまず、`c`、その後削除します`b`文字を削除し、最後に残りの文字を削除します。`a`文字。 

| フェーズ | 入力文字 |`v`|`g`| 追加された操作 |
 | --- | --- | --- | --- | --- |
 | 開始 | | 空 | 空 | |
 | 最初のパス |`a`|`a`| | |
 | 最初のパス |`c`|`aba`| |`3 2`|
 | 最初のパス |`a`|`abaa`| | |
 | 最初のパス |`b`|`abaab`| | |
 | 2 番目のパス |`a`|`abaab`|`a`| |
 | 2 番目のパス |`b`|`abaab`| 空 |`2 2`、`4 1`|
 | 2 番目のパス |`a`|`abaab`|`a`| |
 | 2 番目のパス |`a`|`abaab`|`aa`| |
 | 最終パス | 初め`a`| | |`1 1`、`2 2`、`4 1`|
 | 最終パス | 2番`a`| | |`1 1`、`2 2`、`4 1`|

 結果のシーケンスには 9 つの演算が含まれます。`3n = 12`。 最初のパスの不変条件は、`c`: 元の接頭辞`ac`実際になりました`aba`、 それで`v`単に元の文字を保存するのではなく、実際の文字列を記述し続けます。 

サンプル 2 の場合、`bac`、アルゴリズムは最初の文字が次であることをすぐに検出します。`b`。 

| フェーズ | 入力文字 |`v`|`g`| 結果 |
 | --- | --- | --- | --- | --- |
 | 開始 | | 空 | 空 | |
 | 最初のパス |`b`|`b`| | |
 | 最初のパス |`a`|`ba`| | |
 | 最初のパス |`c`|`baba`| |`3 3`|
 | 2 番目のパス | 初め`b`|`baba`| 空 | 不可能 |

 最初`b`2 番目のパスで見つかった文字は、実際に残っている文字列の最初の文字になります。 どの操作も最初の文字を次の文字に変えることはできません`a`したがって、正しい出力は次のようになります。`-1`。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | O(n + m) | すべての入力文字は一定の回数処理され、`m <= 3n`操作が生成されます。 |
 | スペース | O(n + m) | 補助文字列と格納された操作リストは両方とも入力サイズにおいて線形です。 |

 以来`n <= 2 * 10^5`、アルゴリズムはキャラクターごとに一定量の作業のみを実行し、最大でも出力します。`600000`操作。 線形構造は 1 秒の制限時間内に快適に収まりますが、保存される出力および補助配列は 256 MB をはるかに下回ります。 

## テストケース

 この問題では多くの異なる有効な出力が許可されるため、以下のチェッカーは建設的な答えを 1 つの正確なシーケンスと比較しません。 代わりに、出力されるすべての操作をシミュレートし、各インデックスが有効であること、すべての操作が適用可能であること、最後の文字列が空であること、および操作カウントが最大で次であることを検証します。`3n`。```python
# helper: run solution on input string, return output string
import io
import sys

def solve_string(s):
    operations = []
    v = []

    def add(tp, idx):
        operations.append((tp, idx))

    for ch in s:
        if ch != 'c':
            v.append(ch)
            continue

        if not v:
            return None

        if v[-1] == 'a':
            add(3, len(v) + 1)
            v.append('b')
            v.append('a')
        else:
            if len(v) == 1:
                return None

            if v[-2] == 'a':
                add(4, len(v) - 1)
                v.pop()
                v.pop()
            else:
                add(2, len(v) - 1)
                add(3, len(v))
                add(4, len(v) + 1)

    g = []

    for ch in v:
        if ch == 'a':
            g.append('a')
        else:
            if not g:
                return None
            add(2, len(g) + 1)
            add(4, len(g))
            g.pop()

    for _ in g:
        add(1, 1)
        add(2, 2)
        add(4, 1)

    return operations

def run(inp: str) -> str:
    s = inp.strip()
    operations = solve_string(s)

    if operations is None:
        return "-1\n"

    out = [str(len(operations))]
    out.extend(f"{tp} {idx}" for tp, idx in operations)
    return "\n".join(out) + "\n"

def validate(inp: str, output: str) -> bool:
    s = inp.strip()
    tokens = output.split()

    if not tokens:
        return False

    if tokens[0] == "-1":
        return len(tokens) == 1

    m = int(tokens[0])
    if m < 1 or m > 3 * len(s):
        return False

    if len(tokens) != 1 + 2 * m:
        return False

    cur = list(s)
    p = 1

    for _ in range(m):
        tp = int(tokens[p])
        idx = int(tokens[p + 1])
        p += 2

        if tp == 1:
            if not (1 <= idx <= len(cur)) or cur[idx - 1] != 'a':
                return False
            cur[idx - 1:idx - 1] = ['b']

        elif tp == 2:
            if not (1 <= idx <= len(cur)) or cur[idx - 1] != 'b':
                return False
            cur[idx - 1:idx] = ['b', 'c']

        elif tp == 3:
            if not (1 <= idx <= len(cur)) or cur[idx - 1] != 'c':
                return False
            cur[idx - 1:idx] = ['b', 'a']

        elif tp == 4:
            if not (1 <= idx <= len(cur) - 2):
                return False
            if cur[idx - 1:idx + 2] != ['a', 'b', 'c']:
                return False
            del cur[idx - 1:idx + 2]

        else:
            return False

    return not cur

# Provided samples
out = run("acab")
assert validate("acab", out), "sample 1"

out = run("bac")
assert out.strip() == "-1", "sample 2"

# Minimum-size solvable input
out = run("a")
assert validate("a", out), "single a"

# Minimum-size impossible inputs
assert run("b").strip() == "-1", "single b"
assert run("c").strip() == "-1", "single c"

# All-equal impossible input
assert run("bbb").strip() == "-1", "all b"

# All-equal impossible input with c
assert run("ccc").strip() == "-1", "all c"

# Boundary case involving c
out = run("ac")
assert validate("ac", out), "ac"

# Case where there are too many b characters
assert run("abb").strip() == "-1", "abb"

# Maximum-size solvable input
s = "a" * 200000
out = run(s)
assert validate(s, out), "maximum-size input"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`a`| 3 つの操作の有効なシーケンス | 最小の解決可能な入力と最終`a -> ab -> abc`建設 |
 |`b`|`-1`| 初めて`b`決して削除できません |
 |`c`|`-1`| 初めて`c`最初になれない`a`|
 |`bbb`|`-1`| 利用可能な文字列がありません`a`サポートは拒否されます |
 |`ccc`|`-1`| 繰り返し`c`キャラクターは最初の人を救出することはできません`c`|
 |`ac`| 有効なシーケンス | の`ac`に`aba`ファーストパス変換 |
 |`abb`|`-1`| あ`b`| は、以前に一致した後の最初の文字になる可能性があります。 
|`a`200000 回繰り返した | ちょうど 600000 回の操作を含む有効なシーケンス | 最大入力サイズと`3n`操作範囲 |

 ## 特殊なケース

 のために`bac`、最初の文字は`b`。 ファーストパスストア`b`、 それから`a`。 とき`c`処理されると、`ac`接尾辞は次のように変換されます`aba`、与える`baba`。 2 回目のパスでは、最初の文字はすでに`b`, したがって、先行するものはありません`a`で`g`。 アルゴリズムは出力します`-1`、最初の`b`決して最初にはなれない`a`。 

のために`c`、最初のパスは空から始まります。`v`。 アルゴリズムはすぐに戻ります`-1`。 これは単に構造上の制限ではありません。 オペレーション 3 の変更`c`の中へ`ba`したがって、最初の文字を拡張しても、それを次の文字に変えることはできません。`a`。 最初の文字を含む最終的な削除では、その文字が`a`。 

のために`abb`、最初のパスは次のように終了します。`v = abb`、ないからです。`c`文字。 2 番目のパスは最初のパスを消費します`b`前のものと一緒に`a`、2番目を残して`b`最初のキャラクターとして。 その瞬間`g`は空であるため、アルゴリズムは戻ります`-1`。 どの操作も最初にそれを変えることができないため、この状況は避けられません`b`の中へ`a`。 

のために`ac`、最初のパスではローカル パターンが表示されます。`ac`。 位置 2 の操作 3 が変更されます`ac`の中へ`aba`、 それで`v`になる`aba`。 2番目のパスは中央に一致します`b`前のものと`a`、操作 2 に続いて操作 4 を使用し、1 つを残します。`a`。 決勝戦`a`3回の操作で除去できます。 構築で使用されるすべてのインデックスは現在の文字列を参照するため、このケースでは最初のフェーズと 2 番目のフェーズの間の境界も行使されます。 

のために`abc`、`c`見えます`v = ab`そして`v[-2] = a`したがって、アルゴリズムは位置 1 で操作 4 を直接実行します。文字列全体が 1 回の操作で消えます。 これは、削除操作自体を拡張せずに使用できる最小のケースです。 

最大入力の場合、`200000`のコピー`a`、最初の 2 つのパスでは、すべての文字が残ります。`g`。 それぞれ`a`次に、正確に 3 つの操作を使用して独立して削除されます。 構造は正確に生成します`600000 = 3n`これは、実装が最悪の場合でも最大出力サイズを尊重していることを示しています。
