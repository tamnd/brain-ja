---
title: "CF 103389H - 4G\u7f51\u7edc"
description: "$alpha = a1 a2 dots an$ を ${1,dots,n}$ の順列とします。 $pi$ を逆順列とすると、$pi(ai)=i$ となります。 セクション 7.2.1.2 の逆変換テーブルは、 $cj = {, i : pi(i) pi(j), i < j }, qquad 1 le j le n,$ によって定義されているため、 $0 le cj < j$ となります。"
date: "2026-07-03T12:15:03+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103389
codeforces_index: "H"
codeforces_contest_name: "2021\u5e74\u4e2d\u56fd\u5927\u5b66\u751f\u7a0b\u5e8f\u8bbe\u8ba1\u7ade\u8d5b\u5973\u751f\u4e13\u573a"
rating: 0
weight: 103389
solve_time_s: 151
verified: false
draft: false
---

[CF 103389H - 4G\u7f51\u7edc](https://codeforces.com/problemset/problem/103389/H)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 31 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 しましょう$\alpha = a_1 a_2 \dots a_n$の順列である${1,\dots,n}$。 させて$\pi$は逆順列を示しますので、$\pi(a_i)=i$。 セクション 7.2.1.2 の反転テーブルは次のように定義されます。$c_j = \#\{\, i : \pi(i) > \pi(j),\ i < j \}, \qquad 1 \le j \le n,$それで$0 \le c_j < j$。 ランクは、階乗体系におけるこのテーブルの混合基数の値として定義されます。 

定義する$r(\alpha) = \sum_{j=1}^n c_j (j-1)!.$バウンド$c_j < j$暗示する$0 \le r(\alpha) < n!$これは数字を使った標準的な階乗表現であるため、$c_n,c_{n-1},\dots,c_1$。 

計算するには$r(\alpha)$線形時間では、すべてのペアを繰り返しスキャンせずに逆変換テーブルを取得する必要があります。 配列を維持する$\pi[1..n]$そのような$\pi[x]$値の位置です$x$。 これは 1 つのパスで構築されます。$\pi[a_i] \leftarrow i \quad (1 \le i \le n).$価値観$c_j$セットから決定されます${1,\dots,j-1}$およびそれらに対する相対的な位置$\pi(j)$。 これらのカウントを効率的にサポートするには、すでに処理された値のサブセットごとに、特定のプレフィックス内に位置がある要素の数を格納する位置に関するデータ構造を維持します。 バイナリインデックス付きツリーを使用$1..n$、各挿入およびプレフィックス クエリは、単一のツリー トラバーサルに比例した時間で実行されます。 

値の処理$j=1,2,\dots,n$昇順で、ポジションを含む構造を維持します。$\pi(1),\dots,\pi(j-1)$。 それから$c_j = (j-1) - \#\{ i<j : \pi(i) \le \pi(j)\}.$2 番目の項は、インデックスのプレフィックス合計クエリとして取得されます。$\pi(j)$、それでそれぞれ$c_j$構造に対する 1 回の更新と 1 回のクエリの後に計算されます。 各操作は単一のルートからリーフへのパスを通過するため、全体の作業量は$j$セクション 7.2.1.2 で使用されるワード RAM の意味では線形です。 

したがって$r(\alpha)$累積して計算されます$c_j (j-1)!$同じスキャン中に、$k=r(\alpha)$で$O(n)$ステップ。 

逆マッピングの場合、次のようにします。$k$与えられる$0 \le k < n!$。 書く$k$階乗表現で$k = \sum_{j=1}^n c_j (j-1)!,$除算によって順番に得られる数字:$c_j = k \bmod j,\qquad k \leftarrow \left\lfloor k/j \right\rfloor,\qquad n \ge j \ge 1.$再構築するには$\alpha$、セットを維持する$S$最初に含まれている${1,\dots,n}$。 のために$j=n,n-1,\dots,1$を選択し、$(c_j+1)$-st の最小要素$S$そしてそれを次のように追加します$a_{n-j+1}$から削除します。$S$。 この選択は、上で使用したのと同じ位置構造によって実装され、操作ごとに 1 回の走査での削除と順序の統計をサポートします。 各ステップでは、要素を 1 つだけ削除します。$S$したがって、更新の合計数は次のようになります。$n$、各選択は同じ単位コストの仮定の下で一定の償却時間で実行されます。 

この構築により、反転テーブルが正確に次のような順列が生成されます。$(c_1,\dots,c_n)$、したがって階乗展開は以下に等しい$k$、したがって、結果の順列は次のようになります。$r^{-1}(k)$。 

どちらのマッピングでも、インデックスに対する 1 回のスキャンと、インデックスごとに 1 回の更新および 1 回のクエリが使用され、セクション 7.2.1.2 のモデルの意味で線形時間が得られます。 

これで証明は完了です。 ∎
