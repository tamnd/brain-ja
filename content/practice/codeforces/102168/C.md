---
title: "CF 102168C - \u0421\u043a\u043e\u0431\u043e\u0447\u043a\u0438"
description: "偶数の長さのかっこの文字列があります。 開き括弧と閉じ括弧の数は同じであるため、スワップによって位置を変更しても、どちらのタイプの合計数も変わることはありません。 1回の操作で2つの異なるポジションを選択し、キャラクターを交換します。"
date: "2026-08-19T15:12:24+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102168
codeforces_index: "C"
codeforces_contest_name: "\u041b\u0438\u0447\u043d\u044b\u0439 \u0447\u0435\u043c\u043f\u0438\u043e\u043d\u0430\u0442 \u0421\u0430\u043c\u0430\u0440\u0441\u043a\u043e\u0433\u043e \u0443\u043d\u0438\u0432\u0435\u0440\u0441\u0438\u0442\u0435\u0442\u0430 \u0441\u0440\u0435\u0434\u0438 \u043d\u043e\u0432\u0438\u0447\u043a\u043e\u0432 2018-2019"
rating: 0
weight: 102168
solve_time_s: 232
verified: true
draft: false
---

[CF 102168C - \u0421\u043a\u043e\u0431\u043e\u0447\u043a\u0438](https://codeforces.com/problemset/problem/102168/C)

 **評価:** -
 **タグ:** -
 **解決時間:** 3 分 52 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 偶数の長さのかっこの文字列があります。 開き括弧と閉じ括弧の数は同じであるため、スワップによって位置を変更しても、どちらのタイプの合計数も変わることはありません。 1回の操作で2つの異なるポジションを選択し、キャラクターを交換します。 タスクは、文字列を通常の括弧シーケンスにするために必要なこのような交換の最小数を見つけることです。 

すべてのプレフィックスに少なくとも同じ数の括弧が含まれている場合、括弧のシーケンスは規則的になります。`(`として`)`、シーケンス全体には両方の同じ数が含まれます。 2 番目の条件は入力によってすでに保証されているため、唯一間違っている可能性があるのは、一部のプレフィックスに閉じ括弧が多すぎることです。 

長さは (10^6) に達することがあります。 二次アルゴリズムは、最悪の場合でもすでに約 (10^{12}) 個の基本演算を実行しますが、これは 2 秒の制限がサポートできる範囲をはるかに超えています。 線形または線形に近い解決策が必要ですが、入力が十分に大きいため、長いサフィックスを繰り返しスキャンするだけでも危険です。 

便利なエッジケースは次のとおりです。`)(`。 合計残高はゼロですが、最初のプレフィックスには残高 (-1) があるため、答えは次のようになります。`1`。 最終残高のみをチェックするソリューションでは、誤ってゼロが返されます。 

もう一つのエッジケースは、`))((`。 最小プレフィックスバランスは (-2) ですが、必要なスワップは 1 回のみです。最初のスワップを交換するだけです。`)`3番目の文字を使用すると、`()()`。 赤字の単位を個別にカウントするソリューションでは、誤って 2 が返される可能性があります。 

丸める方向も重要です。 のために`)))(((`、最小プレフィックスバランスは (-3) ですが、答えは次のようになります。`2`、 ない`1`。 1 回のスワップでプレフィックス残高が正確に 2 だけ変更されるため、不足が 3 の場合は 2 回のスワップが必要になります。 

提供されたサンプルは、`))((`、その答えは`1`、 そして`(())`、その答えは`0`。 

## アプローチ

 直接貪欲な実装では、左から右にスキャンし、現在のプレフィックスが無効になるたびに、未使用のプレフィックスを右に検索します。`(`そしてそれを違反者と交換します`)`。 負のプレフィックスは後でどこかから左括弧を受け取る必要があり、そのような交換により、影響を受けるすべてのプレフィックスの残高が 2 ずつ増加するため、この考えは正しいです。 

問題はその未来の探求だ`(`。 次のような文字列で`))))...((((...`、各補正に対して直線距離の検索が可能です。 (n) 文字の場合、(\Theta(n^2)) 文字の検査が必要になる場合があります。 (n=10^6) の場合、これは (10^{12}) 回の演算に相当し、非常に遅すぎます。 

高価な部分を取り除く観察は、将来どの開き括弧が交換されるかを実際に知る必要がないということです。 不正なプレフィックスがどの程度ゼロ以下であるかを知る必要があるだけです。 代表する`(`(+1) と`)`(-1)として。 一部のプレフィックスにバランス (-d) があるとします。 交換する`)`このプレフィックスから`(`プレフィックスの後は、プレフィックスのバランスがちょうど (2) だけ増加します。 したがって、1 回のスワップで 2 単位の赤字を修復できます。 

(m) を最小プレフィックスバランスとします。 (m \ge 0) の場合、シーケンスはすでに規則的であり、スワップは必要ありません。 (m<0) の場合、少なくとも (\lceil -m/2\rceil) 個のスワップが必要です。 スワップの回数は常に同じで十分です。実行残高がマイナスになるたびに、概念的にはその閉じ括弧を後の開始括弧に交換できます。 修正により影響を受ける残高が 2 増加し、合計残高がゼロになるため、適切な左括弧が必ず後で存在します。 

同じ推論を実装するさらに簡単な方法があります。 スキャン中は、現在の残高と表示された最小残高を維持します。 結局のところ、答えは、

 \frac{-m+1}{2}
 ]

 マイナス（m）の場合。 同様に、残高がマイナスになる回数を数えて、すぐに (-1) から (1) にリセットできます。 最小残高の式は証明が明確になるため、より明確になります。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース | (O(n^2)) 検索と交換を繰り返す | (O(n)) | 遅すぎる |
 | 最適 | (O(n)) | (O(1)) 入力文字列に加えて | 承認済み |

 ## アルゴリズムのチュートリアル

 1. 括弧文字列全体を読み取り、初期化します。`balance = 0`そして`minimum = 0`。 変数`balance`現在のプレフィックスの開き括弧と閉じ括弧の数の差を表します。 
2. 文字列を左から右にスキャンします。 追加`1`現在のキャラクターが`(`そして引きます`1`そうなったとき`)`。 
3. 各文字を処理した後、更新します`minimum`これまでに到達した最小残高で。 負の値は、対応する接頭辞に左括弧よりも多くの閉じ括弧が含まれていることを意味します。 
4. スキャン後、次の場合は`minimum`非負、出力`0`。 すべてのプレフィックスにはすでに負でない残高があり、合計残高はゼロであるため、シーケンスは規則的です。 
5. もし`minimum`が負の場合、出力`(-minimum + 1) // 2`。 スワップごとに不良プレフィックスの残高が最大 2 増加する可能性がありますが、貪欲な修復ではまさにその改善を達成できるため、これがスワップの最小数になります。 

### なぜ効果があるのか

 残高がグローバル最小値 (m<0) であるプレフィックスを考えます。 すべての有効なシーケンスは、このプレフィックスを (m) から少なくとも 0 まで上げる必要があります。 スワップで 1 つ移動できます`(`プレフィックスの後からその中に移動しながら`)`アウトし、プレフィックスのバランスを正確に (2) だけ変更します。 したがって、少なくとも (\lceil -m/2\rceil) 個のスワップが必要になります。 

それだけの交換数でも十分です。 文字列全体に同じ数の開き括弧と閉じ括弧があるため、プレフィックスが負の値になる場合は常に、後で開き括弧が必要になります。 そんな未来を入れ替えて`(`犯罪者と一緒に`)`2 つのポジション間のすべてのバランスを 2 上げます。 この操作を繰り返すと欠損が解消され、必要な修正回数はちょうど (\lceil -m/2\rceil) になります。 アルゴリズムはその下限を正確に計算するため、その答えは最適になります。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve():
    s = input().strip()

    balance = 0
    minimum = 0

    for ch in s:
        if ch == '(':
            balance += 1
        else:
            balance -= 1

        if balance < minimum:
            minimum = balance

    print((-minimum + 1) // 2)

if __name__ == "__main__":
    solve()
```テスト ケースが 1 つだけ存在するため、入力は 1 つの文字列として読み取られます。 文字列には最大 (10^6) 文字を含めることができます。`sys.stdin.readline`不要な入力オーバーヘッドを回避します。 

スキャンでは、1 つの整数のみを使用してプレフィックスのバランスが維持されます。 証明により、スワップで使用される正確な位置は操作の数に影響せず、最悪のプレフィックス不足のみが問題となることが示されているため、文字列を変更する必要はありません。`minimum`空のプレフィックスのバランスはゼロであるため、大きな正の値ではなくゼロから始まります。 これにより、次のようなすでに有効な文字列も作成されます。`(())`そして`()()`当然、答えはゼロになります。 

表現`(-minimum + 1) // 2`正の値に対して整数上限除算を実行します。 たとえば、1 の不足があると、`1`、2 の不足があると、`1`、3 の不足があると、`2`。 Python の整数はオーバーフローしないため、長さ (10^6) であっても特別な処理は必要ありません。 

最終残高自体を確認する必要はありません。 このステートメントは、開き括弧と閉じ括弧の数が等しいことを保証するため、文字列全体が処理された後の残高は必然的にゼロになります。 

## 実用的な例

 ### サンプル 1:`))((`シーケンスは 2 つの閉じ括弧で始まるため、最初のプレフィックスは無効になります。 最小残高は (-2) で、1 回のスワップで赤字全体を修復できることを意味します。 

| ポジション | キャラクター | バランス | 最小 |
 | --- | --- | --- | --- |
 | 0 |`)`| -1 | -1 |
 | 1 |`)`| -2 | -2 |
 | 2 |`(`| -1 | -2 |
 | 3 |`(`| 0 | -2 |

 答えは

 [
 \frac{-(-2)+1}{2} = \frac{3}{2} = 1
 ]

 整数の除算を使用します。 考えられる交換の 1 つは位置 0 と 2 の間で、次のようになります。`()()`。 

最終残高だけでは不十分な理由がトレースからわかります。 最終的な値はゼロですが、プレフィックスの最小残高は負であるため、元のシーケンスは規則的ではありません。 

### サンプル 2:`(())`すべてのプレフィックスには負でない残高があるため、最小値はゼロです。 

| ポジション | キャラクター | バランス | 最小 |
 | --- | --- | --- | --- |
 | 0 |`(`| 1 | 0 |
 | 1 |`(`| 2 | 0 |
 | 2 |`)`| 1 | 0 |
 | 3 |`)`| 0 | 0 |

 答えは`0`。 シーケンスは必要なプレフィックス条件をすでに満たしており、合計残高はゼロです。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | (O(n)) | すべての文字は 1 回だけ処理されます。 |
 | スペース | (O(1)) 補助 | のみ`balance`そして`minimum`文字列を読み取った後も維持されます。 |

 (n\le 10^6) の場合、線形スキャンは約 100 万回の反復のみを実行します。 これは、意図した 2 秒制限の範囲内に十分収まりますが、二次アプローチでは、およそ (10^{12}) 個の演算が必要になる可能性があります。 

## テストケース```python
import sys
import io

def solve_string(s: str) -> str:
    balance = 0
    minimum = 0

    for ch in s:
        if ch == '(':
            balance += 1
        else:
            balance -= 1

        minimum = min(minimum, balance)

    return str((-minimum + 1) // 2)

def run(inp: str) -> str:
    return solve_string(inp.strip()) + "\n"

# Provided samples
assert run("))((") == "1\n", "sample 1"
assert run("(())") == "0\n", "sample 2"

# Minimum-size valid input
assert run("()") == "0\n", "minimum size"

# A single unit of negative prefix balance
assert run(")(") == "1\n", "one swap needed"

# Odd-sized deficit, catches incorrect floor division
assert run(")))(((") == "2\n", "ceil division is required"

# Maximum-size input, already regular
assert run("()" * 500000) == "0\n", "maximum size"

# All possible symbols cannot be equal under the input guarantee,
# so the closest meaningful all-equal-style stress case is a
# repeated identical valid block.
assert run("()" * 10) == "0\n", "repeated blocks"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`()`|`0`| 最小の空ではない入力とすでに有効なシーケンス |
 |`)(`|`1`| 合計残高がゼロであるにもかかわらず、負のプレフィックス |
 |`)))(((`|`2`| 赤字が奇数の場合の正しい天井分割 |
 |`()`500000 回繰り返した |`0`| 最大入力サイズと線形時間動作 |
 |`()`10回繰り返した |`0`| 繰り返されるバランスの取れたブロックと安定したプレフィックス追跡 |

 入力制限により、リテラルのすべてが等しい文字列は不可能になります。`(`そして`)`等しくなければなりません。 のみを含む文字列`(`またはのみ`)`保証に違反する可能性があるため、テスト スイートでは代わりに、最大の意味のある繰り返し文字パターンのケースを使用します。 

## 特殊なケース

 のために`)(`、残高は (-1) と (0) です。 最小値は (-1) であるため、式は次のようになります。`(-(-1) + 1) // 2 = 1`。 2 つの位置を交換すると、`()`。 最終残高のみのソリューションでは、無効な最初のプレフィックスが欠落します。 

のために`))((`、残高は (-1,-2,-1,0) であるため、最小値は (-2) になります。 式は次のようになります`1`。 重要な点は、1 回のスワップでプレフィックス残高が 2 変化するため、2 の不足に対して 2 回の個別の修正は必要ないということです。 

のために`)))(((`、残高は (-1、-2、-3、-2、-1、0) です。 最小値は (-3) です。`(-(-3) + 1) // 2 = 2`。 これにより、よくある使用上の間違いがわかります`-minimum // 2`これは切り捨てられ、誤って返されます。`1`。 

のために`()`500,000 回繰り返すと、バランスは次のようになります。`1`そして`0`、 それで`minimum`100 万文字の入力全体を通じてゼロのままです。 アルゴリズムは文字ごとに 1 つの一定量の作業を実行し、結果を返します。`0`。 これにより、スワップを必要とせずに入力の上限が適用されます。 

のために`(())`、残高は次のように進みます。`1,2,1,0`、決してマイナスになることはありません。 その結果、アルゴリズムはスワップを見つけようとせずにゼロを返します。これは、すでに有効なシーケンスが特別な場合の操作なしで処理されることを確認します。
