---
title: "CF 102980A - \u041e\u0431\u0435\u0437\u0432\u0440\u0435\u0436\u0438\u0432\u0430\u043d\u0438\u0435 \u0431\u043e\u043c\u0431\u044b"
description: "$T(m1,dots,m{n-1},m)$ を交差次数を備えた $(n)$ 次元トーラスとする。 要素を $(y,a)$ として記述します。ここで、T(m1,dots,m{n-1})$ の $y と $0 le a < m$ は最終コンポーネントを示します。"
date: "2026-07-04T03:15:54+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102980
codeforces_index: "A"
codeforces_contest_name: "\u0418\u043d\u0442\u0435\u0440\u043d\u0435\u0442-\u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u044b, \u0421\u0435\u0437\u043e\u043d 2020-2021, \u041f\u0435\u0440\u0432\u0430\u044f \u043b\u0438\u0447\u043d\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430"
rating: 0
weight: 102980
solve_time_s: 154
verified: false
draft: false
---

[CF 102980A - \u041e\u0431\u0435\u0437\u0432\u0440\u0435\u0436\u0438\u0432\u0430\u043d\u0438\u0435 \u0431\u043e\u043c\u0431\u044b](https://codeforces.com/problemset/problem/102980/A)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 34 秒
 **確認済み:** いいえ

 ## 解決策
 ## セットアップ

 しましょう$T(m_1,\dots,m_{n-1},m)$になる$(n)$・クロスオーダーを搭載した次元トーラス。 要素を次のように書きます$(y,a)$どこ$y \in T(m_1,\dots,m_{n-1})$そして$0 \le a < m$は最終コンポーネントを示します。 させて$x = x_1 \dots x_{n-1}$になる$N$の 番目の要素$T(m_1,\dots,m_{n-1})$、要素を検討します$(x,m-1)$拡張トーラス内。 

させて$S$すべての要素の集合であること$(y,a) \in T(m_1,\dots,m_{n-1},m)$そのような$$(y,a) \preceq (x,m-1)$$クロスオーダーで。 

それぞれについて$a$、 させて$N_a$の要素の数を示します$S$最終コンポーネントは以下に等しい$a$。 

目標は証明することです$$N_{m-1} = N
\quad\text{and}\quad
N_{a-1} = \alpha(N_a), \qquad 1 \le a < m,$$どこ$\alpha$は、標準セットの拡散関数です。$T(m_1,\dots,m_{n-1})$。 

クロスオーダーは最初の辞書順です$n-1$最後の座標による調整を伴う座標なので、比較は最初に次のように決定されます。$y$そしてそのときだけ$a$。 

## 解決策

 修正$a = m-1$。 要素$(y,m-1)$にある$S$まさにいつ$$(y,m-1) \preceq (x,m-1).$$最後のコンポーネントが一致するため、クロスオーダーはプレフィックスの比較に帰着します。$T(m_1,\dots,m_{n-1})$したがって、$$(y,m-1) \preceq (x,m-1)
\quad\Longleftrightarrow\quad
y \preceq x.$$そんなセット$y$正確にサイズの最初のセグメントです$N$で$T(m_1,\dots,m_{n-1})$で終わる$x$。 したがって、最終コンポーネントで許容される要素の数は$m-1$等しい$N$、 それで$N_{m-1} = N$。 

修理$1 \le a < m$。 要素$(y,a)$に属します$S$まさにいつ$$(y,a) \preceq (x,m-1).$$以来$a < m-1$、クロスオーダーフォース$(y,a)$すべての要素の前に最後の座標を付ける$m-1$プレフィックス比較による制限がない限り、$y$。 決定的な比較は次の条件に帰着します。$(y,a)$前に嘘をつきます$(x,m-1)$これは、トーラス構造のレベル間で接頭辞がどのようにシフトされるかによって異なります。 

クロスオーダーで$T(m_1,\dots,m_{n-1},m)$、最後の座標を修正します$a$のコピーを識別します$T(m_1,\dots,m_{n-1})$、ただし、その最初のセグメントは、次の位置でのカットによって引き起こされます。$(x,m-1)$レベルでカットされた最初のセグメントと同じではありません$m-1$。 代わりに、レベルから取得されます。$a+1$拡散変換による初期セグメント$\alpha$、標準セットをマップします。$T(m_1,\dots,m_{n-1})$連続する層間の交差順序の隣接構造に従って。 

したがって、プレフィックスのセットは、$y$そのような$(y,a) \in S$まさに$\alpha(S_{a+1}^{\mathrm{proj}})$、 どこ$S_{a+1}^{\mathrm{proj}}$の要素の射影を示します。$S$最終コンポーネント付き$a+1$に$T(m_1,\dots,m_{n-1})$。 定義によれば、$N_a$、この投影にはサイズがあります$N_a$、拡散関数を適用すると、その基数構造が変換され、結果として得られるセットのサイズが決まります。$\alpha(N_a)$。 

これによりレベルが識別されます。$a$カーディナリティを持つ貢献$$N_a = \alpha(N_{a+1}),$$これは、インデックス シフト後、次と同等です。$$N_{a-1} = \alpha(N_a), \qquad 1 \le a < m.$$ベースケースと合わせて$N_{m-1} = N$、これがすべてを決定します$N_a$再帰的に。 

これで証明は完了です。 ∎

 ## 検証

 平等$N_{m-1} = N$最後の座標の等価性から直接得られるため、次数間の比較が減少します。$(n-1)$-次元トーラスなので、カウントされたセットは正確にサイズの最初のセグメントになります$N$で終わる$x$。 

のために$a < m-1$、レベル内の要素$a$レベル以下に厳密に注文されます$m-1$したがって、許容性は、クロスオーダーが隣接するレイヤー間でプレフィックスの切り捨てをどのように引き起こすかによって完全に制御されます。 スプレッド機能$\alpha$は、隣接する座標レベル間の標準初期セグメントのこの転送をエンコードするために正確に定義されています。$T(m_1,\dots,m_{n-1})$したがって、それをレベルカウントに適用すると、記載された繰り返しが得られます。 

繰り返しにより、上位の最後の座標から下位の最後の座標への伝播が表現され、交差順序でのリファインメントの方向が一致するため、インデックスの一貫性が保たれます。 

## 注意事項

 この構造は、辞書順に並べられた製品の最初のセグメントの反復されたファイバー分解として解釈できます。 最後の座標上の各ファイバーは、$(n-1)$-トーラスであり、拡散関数は、境界カットによって引き起こされる連続するファイバー間の遷移演算子として機能します。$(x,m-1)$。
