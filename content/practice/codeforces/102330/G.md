---
title: "CF 102330G - \u0421\u0430\u0448\u0430 \u0438 \u0441\u0442\u0430\u0436\u0438\u0440\u043e\u0432\u043a\u0438"
description: "一連の 10 進数として書かれたタスク番号が与えられます。 分割では桁間のいくつかのカット位置が選択されるため、結果として得られるすべての部分が個別のタスク番号として解釈されます。"
date: "2026-08-13T04:06:28+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102330
codeforces_index: "G"
codeforces_contest_name: "\u0421\u0438\u0440\u0438\u0443\u0441.2019.\u041d\u043e\u044f\u0431\u0440\u044c.\u041e\u0447\u043d\u044b\u0439 \u043e\u0442\u0431\u043e\u0440"
rating: 0
weight: 102330
solve_time_s: 80
verified: true
draft: false
---

[CF 102330G - \u0421\u0430\u0448\u0430 \u0438 \u0441\u0442\u0430\u0436\u0438\u0440\u043e\u0432\u043a\u0438](https://codeforces.com/problemset/problem/102330/G)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 20 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 一連の 10 進数として書かれたタスク番号が与えられます。 分割では桁間のいくつかのカット位置が選択されるため、結果として得られるすべての部分が個別のタスク番号として解釈されます。 有効なピースは 2 つの条件を満たさなければなりません。その 10 進表現は最大でも`k`数字であり、その数値は素数です。 先頭のゼロは禁止されているため、次のような部分`03`数値が 3 であっても無効です。 

目標は、結果として得られるすべてのピースが有効になるように、カットを配置する可能なすべての方法を数えることです。 一部の部分の数値が同じであっても、カット位置が異なると、異なる分解が生成されます。 答えはモジュロで出力されます`10^9 + 7`。 

入力境界`|n| <= 10^6`桁分割の問題としては異常に制限的です。 絶対値の 10 進数は最大 7 桁ですが、`k <= 6`。 つまり、可能なカット位置は最大 6 つあり、したがって最大でも 6 つしかありません。`2^6 = 64`完全なパーティション。 ブルート フォース ソリューションは、すでにこれらの制約に対して十分に小さいものになっています。 それでも、同じ繰り返しを動的プログラムとして記述することができ、同じプレフィックス状態の再計算を回避し、桁長が増加した場合にはるかに適切にスケーリングできます。 

絶対値は、数字列を作成するときに使用されます。 マイナス記号は数字ではなく、10 進数のタスク番号に含めることはできないため、分割に意味のある表現は次の数字のシーケンスです。`|n|`。 

不注意な実装が失敗する可能性がある小さなケースがいくつかあります。 入力用`2`と`k = 1`、答えは`1`タスク番号全体が素数 2 であるためです。素数テストを 2 から開始し、誤って除数を必要とする実装では、このケースが拒否される可能性があります。 

入力用`11`と`k = 1`、答えは`0`。 それぞれの1桁のピースは、`1`、これは素数ではありません。 ゼロを除くすべての数字を素数の可能性があるものとして扱うソリューションは、誤って返します。`1`。 

入力用`103`と`k = 2`、答えは`0`。 可能な 2 桁のプレフィックスは次のとおりです。`10`、残りの桁は`3`;`10`プライムではありません。 代替案`1|03`禁止されているので、`03`先頭にゼロが付きます。 すべての部分文字列をチェックする前に整数に直接変換するソリューションは、この無効な表現を黙って受け入れます。 

入力用`23`と`k = 2`、答えは`2`。 両方`23`それ自体と`2|3`有効です。 正確な長さの部分のみを考慮するソリューション`k`、または短い最終部分が許可されていることを忘れている場合は、これらの分解のいずれかを見逃します。 

## アプローチ

 直接的なアプローチは、考えられるカットのセットをすべて列挙することです。 と`L`数字はあります`L - 1`すべてのギャップにはカットが含まれる場合と含まれない場合があり、`2^(L-1)`パーティション。 パーティションごとに、生産された各ピースをチェックし、より長いピースを拒否します。`k`、先行ゼロを拒否し、残りの値の素数性をテストします。 これは正しいです。考えられるすべての分解がギャップの 1 つのサブセットに正確に対応するため、列挙によって分解が欠落したり重複したりすることはありません。 

実際の制約の下では`L <= 7`、この総当たり力は決して遅くなりすぎることはありません。 完全なパーティションは最大 64 個あり、各パーティションには最大 7 個のピース​​があるため、最大 448 個のピース​​ チェックがあります。 チェックされるすべての数値は最大 6 桁であるため、試算の除算に必要な除数テストは 1 個あたり 1000 未満です。 その結果、最悪の場合の上限は約 448,000 個の基本除数チェックとなり、これは簡単に管理できます。 したがって、この問題に対してはブルートフォース手法がすでに受け入れられています。 

より再利用可能なアプローチは、分解が左から右に構築できることを観察することです。 最初のものを仮定します`i`数字はすでに有効な素数部分に分割されています。 次にどの有効な部分が来るかを決めるだけで済みます。 多くてもあります`k`次のピースの可能な長さを確認して、その位置から移行できるようにします。`i`ポジションへ`i + 1`を通して`i + k`。 これにより、一次元の動的プログラムが得られます。 

させて`dp[i]`最初の有効な分解の数になります。`i`数字。 最初は`dp[0] = 1`、空のプレフィックスを表します。 到達可能なあらゆる位置に対して`i`、次の 1、2、までを取り上げます。`k`数字。 結果の部分文字列に先頭のゼロがなく、素数を表す場合は、`dp[i]`する方法`dp[j]`、 どこ`j`はその部分文字列の直後の位置です。 

ブルートフォース手法は、カット位置の数が少ないため機能しますが、同じ有効なプレフィックスを繰り返し探索します。 すべての分解が桁位置間の有効な遷移のシーケンスであるという観察により、繰り返されるプレフィックスをすべて 1 つの DP 状態にマージできます。 

ここでは素数性テストに大きなふるいは必要ありません。 多くてもあります`7 * 6 = 42`DP によってさまざまな部分文字列候補が検討され、すべての候補には最大 6 桁の数字が含まれます。 平方根までの除算の試行は十分すぎるほど高速です。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース |`O(2^L * L * sqrt(10^k))`|`O(L)`| 受け入れられました`L <= 7`|
 | 最適 |`O(L * k * sqrt(10^k))`|`O(L)`| 承認済み |

 ここ`L`の桁数です`|n|`。 DP は、実装がまだ非常に小さいにもかかわらず、入力長が増加してもその構造が有用なままであるため、好ましいです。 

## アルゴリズムのチュートリアル

 1.読む`n`そして`k`、変換します`abs(n)`文字列に`s`。 分割は数値演算に従ってではなく、10 進数の間で実行されるため、文字列表現が必要です。 
2. 正の整数の素数性テストを定義します。 2 より小さい値は拒否されます。 少なくとも 2 の値については、その平方根までの整数で割り切れるかどうかをテストします。 除数が見つからない場合、値は素数になります。 
3. 配列を作成する`dp`長さの`len(s) + 1`そしてセット`dp[0] = 1`。 州`dp[i]`最初のことを意味します`i`数字はすでに完全に有効な部分に分割できます`dp[i]`さまざまな方法で。 
4. 位置を左から右に処理します。 もし`dp[i]`がゼロの場合、この位置に到達する有効な分解は存在しないため、そこから拡張するものは何もありません。 
5. 位置から`i`1 から 1 までのすべての長さを試してください。`k`、文字列の最後で止まります。 これは、最大長制限によって許可されるすべての可能な次のピースをカバーします。 
6. 候補者の最初の桁が次の場合は、直ちに候補者を拒否します。`0`。 このような部分文字列には先頭にゼロがあり、有効なタスク番号を表すことができません。 
7. 候補部分文字列を整数に変換し、それが素数であるかどうかをテストします。 プライムの場合は追加します`dp[i]`候補の末尾に対応する状態に遷移します。 この遷移は、プレフィックスのすべての有効な分解がこの特定の素数部分によって拡張できることを意味します。 
8. すべてのポジションが処理された後、戻ります。`dp[len(s)]`。 この状態では、すべての桁を消費する分解が正確にカウントされるため、未完成の接頭辞は答えに含まれません。 

### なぜ効果があるのか

 不変条件は、処理後の位置です。`i`、`dp[i]`最初の有効な分解の数が正確に含まれます。`i`数字。 このような分解にはそれぞれ、固有の最終ピースが含まれます。 最後の部分を削除すると、以前の位置の有効な分解が残ります。`j`、削除された部分は正確に、からの遷移によって考慮される部分文字列の 1 つです。`j`に`i`。 逆に、アルゴリズムによって受け入れられるすべての遷移は、有効なプライム部分をすでに有効な分解に追加します。 したがって、カウントされたすべての分解は有効であり、すべての有効な分解は 1 回だけカウントされます。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

MOD = 1_000_000_007

def is_prime(x):
    if x < 2:
        return False
    if x == 2:
        return True
    if x % 2 == 0:
        return False

    d = 3
    while d * d <= x:
        if x % d == 0:
            return False
        d += 2
    return True

def solve():
    n = int(input())
    k = int(input())

    s = str(abs(n))
    m = len(s)

    dp = [0] * (m + 1)
    dp[0] = 1

    for i in range(m):
        if dp[i] == 0:
            continue

        value = 0

        for length in range(1, k + 1):
            j = i + length
            if j > m:
                break

            if length > 1 and s[i] == '0':
                break

            value = value * 10 + (ord(s[j - 1]) - ord('0'))

            if is_prime(value):
                dp[j] = (dp[j] + dp[i]) % MOD

    print(dp[m])

if __name__ == "__main__":
    solve()
```の`is_prime`関数ハンドル`0`そして`1`可算性をテストする前に処理します`2`偶数のショートカットが最小の素数を拒否しないように、別々に行います。 その後は、奇数の約数のみを考慮する必要があります。 

DP が使用するのは、`dp[0] = 1`空のプレフィックスの基本ケースとして。 この値がないと、有効な最初のピースには、その寄与を受け取るための状態が存在しません。 

候補値は、部分文字列を繰り返し変換するのではなく、段階的に構築されます。 たとえば、ポジションからの延長中`i`、値`3`、`37`、 そして`377`が次々と得られます。 これにより、遷移ループが簡素化され、不必要な文字列のスライスが回避されます。 

先行ゼロのチェックは、複数桁の候補を受け入れる前に意図的に実行されます。 一桁`0`も自然に拒否されます`is_prime`、 それで`0`別途特殊なケースを用意する必要はありません。 もし`s[i] == '0'`、それより長い部分文字列には先頭にゼロが残るため、そこから始まる部分文字列は有効ではなくなります。 そのため、ループはすぐに停止できます。 

最も長い候補は最大 6 桁なので、`value`せいぜい`999999`。 ここでは、Python の整数にはオーバーフローの懸念はなく、この問題の実際の分解数が最大 64 であっても、すべての DP 遷移のモジュロ演算は必要な出力規則に従います。 

## 実用的な例

 提供されたサンプルの入力は次のとおりです。`37735`と`k = 2`。 DP は、素数に従って、すべての位置で 1 つまたは 2 つの数字を選択できます。 

| 位置`i`|`dp[i]`| 候補者 | プライム？ | 更新された状態 |
 | --- | --- | --- | --- | --- |
 | 0 | 1 |`3`| はい |`dp[1] += 1`|
 | 0 | 1 |`37`| はい |`dp[2] += 1`|
 | 1 | 1 |`7`| はい |`dp[2] += 1`|
 | 1 | 1 |`77`| いいえ | なし |
 | 2 | 2 |`7`| はい |`dp[3] += 2`|
 | 2 | 2 |`73`| はい |`dp[4] += 2`|
 | 3 | 2 |`3`| はい |`dp[4] += 2`|
 | 3 | 2 |`35`| いいえ | なし |
 | 4 | 4 |`5`| はい |`dp[5] += 4`|

 最終的な値は`dp[5] = 4`。 これら 4 つのパスは以下に対応します。`3|7|7|3|5`、`37|7|3|5`、`37|73|5`、 そして`3|7|73|5`。 トレースは、複数のウェイが同じ位置に到達できる理由と、それらのウェイを置き換えるのではなく蓄積する必要がある理由を示します。 

2 番目の例として、次のことを考えてみましょう。`23`と`k = 2`。 

| 位置`i`|`dp[i]`| 候補者 | プライム？ | 更新された状態 |
 | --- | --- | --- | --- | --- |
 | 0 | 1 |`2`| はい |`dp[1] = 1`|
 | 0 | 1 |`23`| はい |`dp[2] = 1`|
 | 1 | 1 |`3`| はい |`dp[2] = 2`|

 答えは、`2`を表す`2|3`そして`23`。 この例は、トランジションでは 1 から 2 までのすべての長さを考慮する必要があることを示しています。`k`、残りのサフィックス全体が 1 つの素数部分を形成する場合を含みます。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 |`O(L * k * sqrt(10^k))`| せいぜい`k`候補作品はそれぞれからテストされます`L`位置、素数性テストには平方根時間がかかります。 
| スペース |`O(L)`| DP 配列には、各桁位置に 1 つの状態が含まれます。 

ここ`L <= 7`そして`k <= 6`。 緩い境界でさえ、素数性テストは 42 個しか提供されず、各テストには 6 桁の数値に対して最大約 500 個の奇数約数が含まれます。 このアルゴリズムは、1 秒および 256 MB の制限内に問題なく収まります。 

## テストケース```python
import sys
import io

MOD = 1_000_000_007

def is_prime(x):
    if x < 2:
        return False
    if x == 2:
        return True
    if x % 2 == 0:
        return False

    d = 3
    while d * d <= x:
        if x % d == 0:
            return False
        d += 2
    return True

def solution(inp):
    data = inp.split()
    n = int(data[0])
    k = int(data[1])

    s = str(abs(n))
    m = len(s)

    dp = [0] * (m + 1)
    dp[0] = 1

    for i in range(m):
        if dp[i] == 0:
            continue

        value = 0

        for length in range(1, k + 1):
            j = i + length
            if j > m:
                break

            if length > 1 and s[i] == '0':
                break

            value = value * 10 + int(s[j - 1])

            if is_prime(value):
                dp[j] = (dp[j] + dp[i]) % MOD

    return str(dp[m])

def run(inp: str) -> str:
    return solution(inp).strip()

# Provided sample
assert run("37735\n2\n") == "4", "provided sample"

# Minimum-size prime task number
assert run("2\n1\n") == "1", "single-digit prime"

# All equal digits, every digit is prime but every two-digit block is composite
assert run("777777\n2\n") == "1", "all-equal values"

# Both the whole number and the one-digit split are valid
assert run("23\n2\n") == "2", "boundary on k"

# Leading zero must not be accepted as a multi-digit prime
assert run("103\n2\n") == "0", "leading zero"

# Maximum absolute value allowed by the statement
assert run("1000000\n6\n") == "0", "maximum-size input"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`2 / 1`|`1`| 可能な最小の素数と`2`素数境界 |
 |`777777 / 2`|`1`| 繰り返される数字とその区別`7`と複合`77`|
 |`23 / 2`|`2`| 1 桁の分割と最大許容ピース長の両方 |
 |`103 / 2`|`0`| 先行ゼロの拒否と複合複数桁部分 |
 |`1000000 / 6`|`0`| 許容される最大絶対入力と多数のゼロを含む候補 |

 ## 特殊なケース

 のために`2`と`k = 1`、DPはで始まります`dp[0] = 1`。 それが候補となる`2`、 そして`is_prime(2)`true を返すので、`dp[1]`は 1 になります。最終的な答えは次のとおりです。`1`。 素数テストにおける 2 の特別な処理により、偶数として誤って拒否されることが防止されます。 

のために`11`と`k = 1`、唯一の候補は 2 つの個別の数字です`1`そして`1`。 どちらも失敗します`x < 2`状態`is_prime`したがって、位置 1 または位置 2 に到達する遷移はありません。最終的な答えは次のとおりです。`0`。 

のために`103`と`k = 2`、最初の位置では許可されます`1`そして`10`、どちらも素数ではありません。 位置 1 からの遷移は桁から始まります。`0`、ただし、複数桁の候補であるため、先行ゼロのチェックによってループがすぐに停止されます。 長さが 3 であるため、完全な数値は使用できません。`k = 2`。 結果的に答えは`0`。 

のために`23`と`k = 2`、最初の位置は両方を生成します`2`そして`23`。 最初の遷移は次のようになります。`dp[1] = 1`、一方、2番目は与えます`dp[2] = 1`。 1 位から、残りは`3`は素数なので、別の方法で到達します`dp[2]`。 最終状態には以下が含まれます`2`、2 つの有効な分解に正確に一致します。 

のために`1000000`と`k = 6`、最初の桁で始まるすべての候補は次のいずれかです。`1`、`10`、`100`、 等々。 のみ`1`有効な分解の接頭辞である可能性がありますが、素数ではありません。 DP がゼロ桁に達すると、そこから始まる有効な複数桁の部分はなくなり、ゼロ自体は素数ではありません。 最終位置に到達する遷移はないため、答えは次のようになります。`0`。
