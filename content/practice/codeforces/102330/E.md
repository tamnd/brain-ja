---
title: "CF 102330E - \u0413\u0435\u043e\u0440\u0433\u0438\u0439 \u0438 \u0432\u043e\u0435\u043d\u043a\u043e\u043c\u0430\u0442"
description: "Есть n человек и k последовательных печатей. Печати расположены в фиксированном порядке: сначала человек должен получить первую, затем вторую и так далее до k-й。 Обслуживания t[j] を参照してください。 Человек i приходит в момент a[i]."
date: "2026-08-13T04:02:40+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102330
codeforces_index: "E"
codeforces_contest_name: "\u0421\u0438\u0440\u0438\u0443\u0441.2019.\u041d\u043e\u044f\u0431\u0440\u044c.\u041e\u0447\u043d\u044b\u0439 \u043e\u0442\u0431\u043e\u0440"
rating: 0
weight: 102330
solve_time_s: 282
verified: true
draft: false
---

[CF 102330E - \u0413\u0435\u043e\u0440\u0433\u0438\u0439 \u0438 \u0432\u043e\u0435\u043d\u043a\u043e\u043c\u0430\u0442](https://codeforces.com/problemset/problem/102330/E)

 **評価:** -
 **タグ:** -
 **解決時間:** 4 分 42 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 Есть`n`Їеловек и`k`そうです。 Печати расположены в фиксированном порядке: сначала человек должен получить первую, затем вторую и так 今日は`k`-© いいえ`j`известна длительность обслуживания`t[j]`。 

Человек`i`簡単に説明します`a[i]`。 Сразу после прихода он попадает в очередь первой печати. Если несколько человек призли одновременно, их порядок определяется исходными номерами. После успего получения очередной печати человек мгновенно переходит в очередь следующей. Каждая очередь обслуживается в порядке поступления, а сотрудник начинает следующего человека сразу после освобождения。 

У каждого человека есть ограничение`b[i]`。 При`b[i] = 0`он может ждать неограниченно долго。 Иначе момент его ухода равен`a[i] + b[i]`。 Если человек не успел закончить нужную процедуру к этому моменту, он покидает военкомат. Если последняя печать завергается ровно в момент ухода, человек считается успевлим, как показывает первый そうです。 

Нужно для каждого человека вывести`completion - a[i]`、 если он получил все`k`いいですね。 Если он угел раньзе заверления процедуры, нужно вывести`-1`。 

Ключевая особенность ограничений состоит в условии`n * k <= 10^5`。 Это означает, что нам разреbolено обработать каждого человека на каждой печати в среднем константное число раз。 Полиномиальные резения вроде`O(n^2 k)`あなたの名前: при`n = 10^5`や`k = 1`они дают порядка`10^10`операций。 Нам нужна обработка порядка`n * k`、плюс сортировка людей перед первой очередью。 

В задаче есть несколько границ, на которых легко получить неправильный ответ. Первая возникает при точном совпадении завергения последней печати с дедлайном. ああ```
1 1
1
1 1
```ответ равен`1`、あーん`-1`。 Человек заканчивает последнюю печать в момент`2`、 ровно через`b = 1`必要に応じて、必要な情報を入力してください。 

Вторая граница возникает, если человек точно в момент дедлайна заканчивает не последнюю, а промежуточную そうですね。 Например、```
1 2
3 1
1 3
```Первая печать заканчивается в момент`4`、который совпадает с дедлайном человека。 Он получает эту печать, но процедура еще не закончена, поэтому сразу после этого покидает военкомат и あなたのことを考えてください。 Ответ равен`-1`。 

Третья ситуация связана с человеком, который уже ждал до дедлайна, пока сотрудник был занят другим человеком. Например、```
2 1
5
1 5
1 5
```Первый человек получает печать в момент`6`。 Второй все это время ждет и в момент`6`уже должен уйти. Его нельзя начинать обслуживать в тот же момент. Ответ равен`5 -1`。 

Наконец、`b[i] = 0`нельзя трактовать как дедлайн`a[i]`。 В этом случае человек вообще не ограничен временем ожидания. Например、```
1 1
7
10 0
```Ответ равен`7`。 

## アプローチ

 Самый прямой вариант симуляции можно построить буквально по происходящим событиям。 Для каждой печати нужно хранить очередь людей, добавлять в нее людей, которые призли, брать первого человека и выяснять, успеет ли он закончить обслуживание. Такой подход уже близок к правильному резению, но неудачная реализация может для каждого обслуживаемого Їеловека заново искать его позицию в очереди среди всех людей. Тогда для одной печати получится`O(n^2)`、今日は、`O(n^2 k)`。 В худвем случае при`n = 100000`や`k = 1`Ѝто около`10^10`проверок、что за секунду невозможно。 

FIFO を使用して、FIFO を使用して、必要な情報を確認してください。 Если мы обрабатываем отдельно, все ее входящие люди уже известны в порядке, в котором они Соявляются в этой очереди. ロシアのホテル`(a[i], i)`。 Мечати люди появляются именно в порядке завергения предыдущей печати, а этот порядок уже известен из назей симуляции.

 Значит、каждую печать можно обработать одним проходом. Мы поддерживаем текущий момент, когда сотрудник освобождается, и очередь уже призедлих людей. Если очередь пуста, время переносится к следующему приходу. Если человек находится в начале очереди, мы сравниваем время окончания его обслуживания с его дедлайном. Если он успевает закончить последнюю печать, он покидает военкомат с успеДным результатом. Если он не успевает, сотрудник остается занят до момента его ухода, потому что человек может уйти прямо во Сремя постановки печати. После этого очередь продолжает работу.

 После обработки печати мы передаем на следующую печать только тех людей, которые действительно смогли 確認してください。 Их время прихода в новую очередь равно времени заверления текущей печати.

 | アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース |`O(n^2 k)`|`O(n)`| 遅すぎる |
 | 最適 |`O(n log n + nk)`|`O(n)`| 承認済み |

 Здесь`nk <= 10^5`、поэтому линейная обработка всех пар человек-печать укладывается в ограничение. Сортировка нужна только один раз, чтобы построить первоначальную очередь.

 ## アルゴリズムのチュートリアル

 1. Сохраняем для каждого человека время прихода`a[i]`и абсолютный момент ухода`a[i] + b[i]`。 ああ`b[i] = 0`используем достаточно бользое значение вместо настоящего дедлайна. Начальная очередь первой печати сортируется по`(a[i], i)`、потому что именно такой порядок задается условием.
 2. Обрабатываем печати последовательно, от первой до`k`-© あなたの質問に答えてください。 ロシアは、それを実現します。 
3. ロシア語を学ぶ`cur`, момент, когда сотрудник становится свободен, указатель на еще не добавленных людей и очередь уже прибывзих ロシア。 Пока очередь пуста,`cur`переносится к времени следующего прихода. Затем в очередь добавляются все люди с временем прихода не бользе`cur`。 
4. Берем первого человека из очереди. Если`cur`уже не меньзе его дедлайна, человек должен был уйти раньзе или прямо сейчас, поэтому обслуживания не ね。 Следующий человек может быть обслужен в тот же`cur`、 если он подходит по сроку.
 5. Если человек еще находится в военкомате, вычисляем`finish = cur + t[j]`。 Если`finish`あなたのことを忘れないでください。 Если это последняя печать, сохраняем`finish`как окончательное время выхода.
 6. Если`finish`превосходит дедлайн, человек не исчезает из модели мгновенно в момент начала обслуживания. Он остается у сотрудника до своего дедлайна и затем уходит, поэтому`cur`нужно установить в дедлайн. Это влияет на всех следующих людей в очереди.
 7. Если промежуточная печать заканчивается ровно в дедлайн, человек успел получить эту печать, но ありがとうございます。 Он сразу уходит и не попадает в очередь следующей печати. Именно поэтому для промежуточных печатей продолжить можно только при`finish < deadline`。 ロシアでは、`finish <= deadline`。 
8. Все люди, успегдие текущую печать имеющие право продолжить процедуру, добавляются в список Следующей очереди с временем прихода`finish`。 Поскольку длительность каждой печати положительна, времена успеложий идут строго по возрастанию, 最も重要な問題は次のとおりです。 
9. После последней печати для каждого успезно заверловего человека вычисляем`finish - a[i]`。 Состальных оставляем`-1`。 

### なぜ効果があるのか

 Инвариант обработки одной печати заключается в том, что перед каждым выбором из очереди она содержит ровно тех людей, которые уже призли к текущему моменту и еще не были обработаны, причем в точном FIFO-порядке. Человек, стоящий впереди, обязан либо получить обслуживание, либо уйти, и никто позади него не может быть обслужен раньзе него。 Если он успевает, мы записываем реальное время заверДения. Если не успевает, сотрудник занят до его дедлайна, после чего человек уходит, и`cur`Солучает точное время, в которое сервер действительно становится доступен. Поэтому после обработки всей очереди получаются ровно те люди и те моменты времени, которые происходят в 最高です。 

После каждой печати список передаваемых людей содержит ровно тех людей, которые получили эту печать и не покинули военкомат из-за ограничения времени. Их время появления в следующей очереди равно времени фактического получения печати. Значит、инвариант переносится на следующую печать。 По индукции после`k`-© печати алгоритм точно знает всех людей, заверсивлих всю процедуру.

 ## Python ソリューション```python
import sys
input = sys.stdin.readline

INF = 10**30

def process_station(arrivals, duration, deadlines, last):
    """
    arrivals: list of (arrival_time, person_id), already sorted
    duration: time needed for this stamp
    deadlines: absolute departure times, INF means unlimited
    last: whether this is the final stamp

    Returns:
        list of (completion_time, person_id) for people who may
        continue to the next stamp, or for all successful people
        if this is the last stamp.
    """
    queue = []
    head = 0
    ptr = 0
    cur = 0

    result = []

    while ptr < len(arrivals) or head < len(queue):
        if head == len(queue):
            if ptr >= len(arrivals):
                break
            if cur < arrivals[ptr][0]:
                cur = arrivals[ptr][0]

        while ptr < len(arrivals) and arrivals[ptr][0] <= cur:
            queue.append(arrivals[ptr])
            ptr += 1

        person_arrival, person = queue[head]
        head += 1

        deadline = deadlines[person]

        if cur >= deadline:
            continue

        finish = cur + duration

        if finish > deadline:
            cur = deadline
            continue

        cur = finish

        if last:
            result.append((finish, person))
        else:
            if finish < deadline:
                result.append((finish, person))

    return result

def solve():
    n, k = map(int, input().split())
    t = list(map(int, input().split()))

    a = [0] * n
    deadlines = [INF] * n

    for i in range(n):
        ai, bi = map(int, input().split())
        a[i] = ai
        if bi != 0:
            deadlines[i] = ai + bi

    arrivals = [(a[i], i) for i in range(n)]
    arrivals.sort()

    for station in range(k):
        arrivals = process_station(
            arrivals,
            t[station],
            deadlines,
            station == k - 1
        )

        if not arrivals:
            break

    answer = [-1] * n

    if k == 0:
        return

    for finish, person in arrivals:
        answer[person] = finish - a[person]

    print(*answer)

if __name__ == "__main__":
    solve()
```В начале`deadlines[i]`あなたのことを思い出してください。 Это существенно упрощает проверки, потому что в любой точке симуляции достаточно сравнить`cur`とか`finish`с одним числом.

 Функция`process_station`必要に応じて、最新の情報を入手してください。`ptr`указывает на еще не добавленного человека, а`head`на первого необработанного человека в массиве очереди. Физически удалять элементы из начала списка не нужно, поэтому каждый человек добавляется один раз и извлекается один раз.

 Когда очередь пуста,`cur`переносится к следующему приходу. После этого добавляются все люди с`arrival <= cur`。 Такое условие особенно полезно, когда сотрудник освобождается в тот же момент, когда несколько людей уже находятся в военкомате。 

Проверка`cur >= deadline`означает、что человек уже должен уйти до начала обслуживания。 Если же`cur < deadline`、сотрудник начинает работать немедленно。 При`finish > deadline`человек не успевает закончить и занимает сотрудника только до момента ухода, поэтому`cur = deadline`。 Именно эта строка позволяет корректно обработать людей, которые бросают процедуру прямо во время постановки そうですね。 

Проверка`finish < deadline`для промежуточных печатей нужна из-за того, что после получения этой печати процедура еще не закончена. Если`finish == deadline`、человек получает текущую печать, но в тот же момент покидает военкомат и не попадает в следующую очередь。 ロシアは、жечати равенство разрезено, поскольку вся процедура уже завербена.

 В Python は、Python を使用して、簡単に実行できます。`a[i] + b[i]`и последовательные времена окончания не переполняются. Значение`10**30`намного бользе любого возможного времени в тесте и безопасно используется как обозначение отсутствия дедлайна.

 ## 実用的な例

 ### サンプル 1

 Вход:```
1 1
1
1 1
```Здесь есть одна печать、поэтому это сразу последняя стадия。 

| 到着 | 人 | 前に | 締め切り | 仕上げ | 結果 |
 | --- | --- | --- | --- | --- | --- |
 | 1 | 1 | 1 | 2 | 2 | 完了しました |

 Человек начинает обслуживание в момент`1`、 печать занимает одну единицу времени и заканчивается в`2`。 Его дедлайн также равен`2`、 а последняя печать разрезает заверление ровно в дедлайн. Время пребывания равно`2 - 1 = 1`。 

Ответ:```
1
```Этот пример проверяет главную границу`finish == deadline`そうです。 

### サンプル 2

 Вход:```
6 1
17
10 100
3 30
80 59
24 86
59 76
69 15
```Все люди проходят только одну、последнюю、печать。 Начальная очередь сортируется по времени прихода, поэтому порядок такой: человек 2, человек 1, человек 4, человек 5、человек 6、человек 3.

 | 人 | 到着 | 締め切り | 前に | 終了/出発 | 結果 |
 | --- | --- | --- | --- | --- | --- |
 | 2 | 3 | 33 | 3 | 20 | 成功 |
 | 1 | 10 | 110 | 20 | 37 | 成功 |
 | 4 | 24 | 110 | 37 | 54 | 成功 |
 | 5 | 59 | 135 | 59 | 76 | 成功 |
 | 6 | 69 | 84 | 76 | 84 | 葉 |
 | 3 | 80 | 139 | 84 | 101 | 成功 |

 для зестого человека печать длится до`93`、 но его дедлайн равен`84`。 Он поэтому занимает сотрудника с`76`до`84`、 после чего уходит。 Человек 3 уже стоял в очереди с момента`80`、 поэтому начинает ровно в`84`と言うか`101`。 

Итоговые времена пребывания равны`27, 17, 21, 30, 17, -1`、что показывает、почему недостаточно просто выкинуть человека、не успевающего закончить печать。 Его время до ухода продолжает занимать сотрудника и сдвигает всех следующих людей.

 ## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 |`O(n log n + nk)`| Первая очередь сортируется, затем каждый человек обрабатывается не более одного раза на каждой печати |
 | スペース |`O(n)`| Храним данные людей и текущую очередь, размер которой не превызает`n`|

 Условие`n * k <= 10^5`ограничивает суммарное число обработок человек-печать величиной`10^5`。 Сортировка занимает не более`O(n log n)`、 а все последующие проходы линейны. 256 МБを参照してください。 

## テストケース```python
import io
import sys

INF = 10**30

def solve_instance(inp: str) -> str:
    data = inp.strip().split()
    it = iter(data)

    n = int(next(it))
    k = int(next(it))

    t = [int(next(it)) for _ in range(k)]

    a = [0] * n
    deadlines = [INF] * n

    for i in range(n):
        ai = int(next(it))
        bi = int(next(it))
        a[i] = ai
        if bi != 0:
            deadlines[i] = ai + bi

    arrivals = [(a[i], i) for i in range(n)]
    arrivals.sort()

    for station in range(k):
        duration = t[station]
        last = station == k - 1

        queue = []
        head = 0
        ptr = 0
        cur = 0
        nxt = []

        while ptr < len(arrivals) or head < len(queue):
            if head == len(queue):
                if ptr >= len(arrivals):
                    break
                cur = max(cur, arrivals[ptr][0])

            while ptr < len(arrivals) and arrivals[ptr][0] <= cur:
                queue.append(arrivals[ptr])
                ptr += 1

            _, person = queue[head]
            head += 1

            deadline = deadlines[person]

            if cur >= deadline:
                continue

            finish = cur + duration

            if finish > deadline:
                cur = deadline
                continue

            cur = finish

            if last:
                nxt.append((finish, person))
            elif finish < deadline:
                nxt.append((finish, person))

        arrivals = nxt

        if not arrivals:
            break

    answer = [-1] * n

    for finish, person in arrivals:
        answer[person] = finish - a[person]

    return " ".join(map(str, answer))

# Provided sample 1
assert solve_instance("""\
1 1
1
1 1
""") == "1", "sample 1"

# Provided sample 2
assert solve_instance("""\
6 1
17
10 100
3 30
80 59
24 86
59 76
69 15
""") == "27 17 21 30 17 -1", "sample 2"

# Minimum size and unlimited waiting
assert solve_instance("""\
1 1
7
10 0
""") == "7", "minimum size and b=0"

# All equal arrival times, with one person missing the deadline
assert solve_instance("""\
2 1
5
1 5
1 5
""") == "5 -1", "equal arrivals and exact waiting boundary"

# Exact deadline on an intermediate stamp
assert solve_instance("""\
1 2
3 1
1 3
""") == "-1", "intermediate stamp ends exactly at deadline"

# All values equal, multiple stamps
assert solve_instance("""\
3 2
2 3
1 100
1 100
1 100
""") == "9 12 15", "equal arrivals and equal deadlines"

# Maximum n*k = 100000, n=100000, k=1.
# Every person can wait forever and all arrive together.
n = 100000
parts = [f"{n} 1", "1"]
parts.extend(["1 0"] * n)
max_input = "\n".join(parts) + "\n"

max_output = solve_instance(max_input).split()
assert len(max_output) == n
assert max_output[0] == "1"
assert max_output[-1] == str(n)
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`1 1 / 7 / 10 0`|`7`| 最小インスタンスと無制限の待機 |
 |`2 1 / 5 / 1 5 / 1 5`|`5 -1`| 平等に到着し、締め切りが前のサービス終了の瞬間にある人 |
 |`1 2 / 3 1 / 1 3`|`-1`| 中間スタンプが期限どおりに完了したことを広めてはなりません。 
|`3 2 / 2 3 / 1 100 / 1 100 / 1 100`|`9 12 15`| 均等な到着、繰り返しの FIFO 処理、および複数のスタンプ |
 |`100000 1 / 1 / 100000 identical arrivals`| 最初の答え`1`、最後の答え`100000`| 最大許容値`n * k`直線的なパフォーマンス |

 ## 特殊なケース

 ### 無制限の待機

 のために```
1 1
7
10 0
```期限は次のように表されます。`INF`。 その人はから始まります`10`、で終了します`17`、と受け入れられます。 アルゴリズムはこの人物に対して有限期限比較を決して適用しないため、結果は次のようになります。`7`。 

### 最後のスタンプは期限ぴったりに終了します

 のために```
1 1
1
1 1
```絶対的な期限は`2`。 計算された終了時間も`2`。 最後のスタンプですので、状態は`finish <= deadline`成功します、そして答えは`2 - 1 = 1`。 

### 中間スタンプは期限ぴったりに終了します

 のために```
1 2
3 1
1 3
```最初のスタンプは次から始まります`1`そして終了します`4`、その人の締め切りにぴったりです。 1つ目のスタンプ自体は完了しているのですが、手続きが終わっていないため次に進むことができません。 アルゴリズムは意図的に次のことを要求します`finish < deadline`次のキューに人を追加する前に、その人は最初のスタンプの後に消え、最後の答えが残ります。`-1`。 

### サービスを受けている間に人が立ち去る

 のために```
6 1
17
10 100
3 30
80 59
24 86
59 76
69 15
```人物 6 は次から始まります`76`、期限内に`84`。 必要なサービスは次の時点で終了します`93`のため、出発時にはサービスが中断されます。`84`。 アルゴリズムセット`cur = 84`それよりも`cur = 93`。 到着した人3`80`、から開始できます`84`そして終了します`101`。 まさにこれが人 3 が費やす理由です`21`オフィスでの時間単位の代わりに`30`。 

### 均等な到着

 のために```
2 1
5
1 5
1 5
```両方の人が同時に最初の列に入ります`1`、インデックスが小さいため、人 1 が優先されます。 人 1 は に終了します`6`、正確に締め切りに達し、これが最後のスタンプであるため成功します。 人 2 は次まで待っています`6`、それは締め切りでもあるため、彼らはサービスを受けずに去ります。 出力は`5 -1`。 

初期リストは次の基準でソートされるため、キュー表現ではこの順序が自動的に保持されます。`(arrival_time, person_id)`一方、後のすべてのキューは、前のスタンプから正常に完了した順序を継承します。
