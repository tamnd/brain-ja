---
title: "CF 102281A - \u041f\u0440\u043e\u0441\u0442\u0430\u044f \u0437\u0430\u0434\u0430\u0447\u0430"
description: "n 個のクッキーが 1 つ山になっています。 2 人のプレイヤーが交互にクッキーを削除し、プロフェッサー X が最初に動きます。 正当な措置により、p^k 個の Cookie が削除されます。ここで、p は素数、k は非負の整数です。 k = 0 が許可されているため、Cookie を 1 つだけ削除することは常に正当です。"
date: "2026-08-13T09:18:30+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102281
codeforces_index: "A"
codeforces_contest_name: "2011, IV \u0421\u0430\u043c\u0430\u0440\u0441\u043a\u0430\u044f \u043e\u0431\u043b\u0430\u0441\u0442\u043d\u0430\u044f \u043c\u0435\u0436\u0432\u0443\u0437\u043e\u0432\u0441\u043a\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u043f\u043e \u043f\u0440\u043e\u0433\u0440\u0430\u043c\u043c\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u044e"
rating: 0
weight: 102281
solve_time_s: 76
verified: true
draft: false
---

[CF 102281A - \u041f\u0440\u043e\u0441\u0442\u0430\u044f \u0437\u0430\u0434\u0430\u0447\u0430](https://codeforces.com/problemset/problem/102281/A)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 16 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 私たちには、`n`クッキー。 2 人のプレイヤーが交互にクッキーを削除し、プロフェッサー X が最初に動きます。 法的措置により削除されます`p^k`クッキー、ここで`p`プライムであり、`k`は負でない整数です。 以来`k = 0`許可されており、正確に削除します`1`Cookie は常に合法です。 最後のクッキーを削除したプレイヤーが勝ちです。 

課題は、最適なプレイでどの教授が勝つかを決定することです。 X が勝った場合、Y 教授を負けた状態にする正当な先手を 1 つ出力する必要もあります。 

入力には 1 つの整数が含まれています`n`、クッキーの初期数、`1 <= n <= 10^9`。 すべての位置にわたる動的プログラミング ソリューションでは、すでに最大 10 億の状態を処理する必要がありますが、これは利用可能な時間とメモリをはるかに超えています。 ゲーム自体の構造がはるかに単純であるため、最適化されたふるいや素数の生成でさえ、本当の問題には対処できません。 勝利戦略は次の方法で決定できます。`n mod 6`。 

不完全な手のリストに基づく実装が失敗する可能性がある小さなケースがいくつかあります。 のために`n = 1`、Xは削除できます`1 = 2^0`したがって、正しい出力は次のようになります。`X WINS`に続く`1`。 指数を正として扱う解決策は、X には動きがないと誤って主張することになります。 

のために`n = 4`, X を削除すると勝ちです。`4 = 2^2`、クッキーは残りません。 したがって、正しい出力は次のようになります。`X WINS`に続く`4`。 これにより、素数のみを生成し、より高い素数累乗を忘れる実装が捕捉されます。 

のために`n = 6`、正しい出力は次のとおりです。`Y WINS`。 魅力的な動き`2`葉`4`、 しかし`4`それ自体が次のプレイヤーの勝利です。 実際、あらゆる法的措置は、`6`非倍数を残す`6`、およびすべての非倍数`6`勝っている。 

のために`n = 12`、正しい出力は再び次のようになります`Y WINS`。 同じ議論が当てはまります。`12`で割り切れます`6`。 かどうかを確認するだけの不用意な実装`n`その結果生じる立場を分析するのではなく、それ自体が法的な措置であると、この訴訟を誤解する可能性があります。 

## アプローチ

 直接ゲーム ソルバーは、すべてのポジションを次から分類します。`0`を通して`n`。 位置`0`移動するプレイヤーが Cookie を持っていないため、負けています。 それぞれの正の位置について、その位置を超えないすべての素数を試します。 少なくとも 1 つの手が負けの局面に達した場合、その局面は勝ちとなり、すべての正当な手が勝ちの局面に達した場合は負けとなります。 

ゲームは有限であるため、この反復は正しいです。 一度移動すると、Cookie の数は厳密に減少するため、ポジションは小さい値から大きい値まで分類できます。 問題はその大きさです。 プライムムーブだけを考えても、`50,847,534`を超えない素数`10^9`。 単純な DP には最大 10 億の州があり、各州の多くの法的な動きをチェックすると、1.5 秒の制限をはるかに超える膨大な数の操作が発生します。 主権を追加しても状況は改善されません。 

ゲーム グラフが非循環であるため総当り攻撃は機能しますが、すべてのポジションが無関係であるとして扱われるため失敗します。 重要な観察は、合法的な動きが剰余剰余と完全に相互作用するということです。`6`。 

あらゆる法的措置は主要な権限です。 これらの数値はいずれも割り切れません`6`というのは、素数べき乗には素約数が 1 つしかないからです。 次に、次のように割り切れる位置を考えます。`6`。 合法的な手を差し引くと、次の数で割り切れない数値が得られます。`6`。 逆に、ゼロ以外のすべての剰余モジュロ`6`次の 5 つの小さな法的手段のいずれかによって直接削除できます。`1`は`p^0`、`2`そして`3`プライムです、`4 = 2^2`、 そして`5`プライムです。 

仮定する`n`残留物がある`1`モジュロ`6`。 削除中`1`の倍数に達する`6`。 残留物用`2`、 取り除く`2`; 残留物用`3`、 取り除く`3`; 残留物用`4`、 取り除く`4`; そして残留物については`5`、 取り除く`5`。 したがって、すべての非倍数は、`6`の倍数に移動します`6`。 

これにより、完全な特徴が得られます。 まさに次の正の倍数`6`ポジションを失っている。 の倍数から`6`、あらゆる法的措置は、非倍数の者に行われます。`6`、勝っています。 すべての非倍数から`6`、のいずれか`1, 2, 3, 4, 5`の倍数に達する`6`、現在のプレイヤーに勝利の手を与えます。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース DP | 少なくとも O(n * pi(n)) | O(n) | 遅すぎる |
 | 最適 | お(1) | お(1) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. Cookie の初期数を読み取ります。`n`。 その剰余モジュロのみ`6`負けているポジションはまさに の正の倍数であるため、重要です。`6`。 
2. コンピューティング`r = n % 6`。 もし`r == 0`、出力`Y WINS`。 There is no winning first move for X because every legal move has a nonzero residue modulo`6`、したがって、結果として得られる山は次で割り切れません。`6`。 
3. もし`r != 0`、出力`X WINS`そして選択してください`r`Xの最初の動きとして。 値`r`は常に合法です:`1`は任意の素数の 0 乗であり、`2`そして`3`素数です、`4`は`2^2`、 そして`5`プライムです。 
4.取り外し後`r`、残りの数は`n - r`で割り切れます。`6`。 結果的に Y 教授は負けた立場にある。 Y がどのような法的措置を講じても、結果として得られる数値は次の数で割り切れません。`6`, したがって、X は再び の倍数に戻ることができます。`6`。 この戦略を繰り返すと、最終的に X が取得する Cookie はゼロになります。 

### なぜ効果があるのか

 不変条件は、X が常にゲームを次の倍数に戻すことができるということです。`6`Yさんの引越し後。 の倍数`6`別の倍数に移動することはできません`6`, なぜなら、法的な動きは以下で割り切れないからです。`6`。 一方、すべての非倍数から、`6`, X はその剰余を剰余として取り除くことができます。`6`、そしてその残留物は常に合法的な動きの1つです`1, 2, 3, 4, 5`。 したがって、次の倍数`6`はまさに負けているポジションであり、アルゴリズムは勝者を特定し、勝ちの先手を提供します。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve():
    n = int(input())

    move = n % 6

    if move == 0:
        print("Y WINS")
    else:
        print("X WINS")
        print(move)

if __name__ == "__main__":
    solve()
```プログラムは唯一の入力値を読み取り、その剰余モジュロを計算します。`6`。 明示的に選択する唯一の手が関連する最小の 5 つの素数パワーであるため、素数を生成する必要はありません。 

余りが 0 の場合、位置が負けているため、最初の出力行のみが出力されます。 剰余がゼロ以外の場合、その剰余自体は正当な手であり、2 行目に出力されます。 

境界線`n = 1`自動的に動作します:`1 % 6 = 1`, したがって、X は 1 つの Cookie を削除します。 Python ではオーバーフローの懸念はなく、固定幅言語であっても関係するすべての値は最大でも`10^9`。 

操作の順序も概念的に重要です。 テストしなければなりません`n % 6`行動を選択する前に。 余りがゼロの場合は、次のような割り算が維持される手が存在しないことを意味します。`6`一方、ゼロ以外の剰余は、負けているクラスに到達するために必要な手を正確に与えます。 

## 実用的な例

 最初の公式サンプルには、`n = 10`。 

|`n`|`n % 6`| 決定 | 最初の動き |
 | --- | --- | --- | --- |
 | 10 | 4 | X が勝ち | 4 |
 | 6 | 0 | Y は負けています | |

 X を削除した後`4`、残りのクッキーは6個です。 番号`6`で割り切れます`6`, そのため、順番が回ってきたプレイヤーにとっては負けのポジションとなります。 これにより、必要な出力が得られます`X WINS`に続く`4`。 この例では、勝ち手が素数である必要がない理由も示しています。`4 = 2^2`法的な第一権力です。 

2 番目の公式サンプルには、`n = 12`。 

|`n`|`n % 6`| 決定 | 最初の動き |
 | --- | --- | --- | --- |
 | 12 | 0 | Y の勝ち | なし |

 12 個の Cookie はすでに次の倍数です`6`。 あらゆる法的措置により、割り切れない数が削除されます。`6`したがって、X が利用できるすべての手が Y に勝ちの位置を与えます。 したがって、出力は単純に次のようになります`Y WINS`。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | お(1) | 1 つのモジュロ演算と一定サイズの出力 |
 | スペース | お(1) | 少数の整変数のみが保存されます。 

入力は次のように大きくできます。`10^9`ただし、アルゴリズムは次の値まで反復することはありません。`n`そして主力のセットを構築することは決してありません。 実行時間とメモリ使用量は一定であるため、1.5 秒と 128 MB の制限内に快適に収まります。 

## テストケース```python
import sys
import io

def solve():
    n = int(input())
    move = n % 6

    if move == 0:
        print("Y WINS")
    else:
        print("X WINS")
        print(move)

def run(inp: str) -> str:
    global input

    old_stdin = sys.stdin
    old_input = input

    try:
        sys.stdin = io.StringIO(inp)
        input = sys.stdin.readline

        old_stdout = sys.stdout
        sys.stdout = io.StringIO()

        solve()
        result = sys.stdout.getvalue()

        sys.stdout = old_stdout
        return result
    finally:
        sys.stdin = old_stdin
        input = old_input

# Provided samples
assert run("10\n") == "X WINS\n4\n", "sample 1"
assert run("12\n") == "Y WINS\n", "sample 2"

# Minimum-size input
assert run("1\n") == "X WINS\n1\n", "minimum n"

# Small losing position
assert run("6\n") == "Y WINS\n", "first positive multiple of 6"

# Maximum-size input
assert run("1000000000\n") == "X WINS\n4\n", "maximum n"

# Boundary immediately before a multiple of 6
assert run("11\n") == "X WINS\n5\n", "residue 5"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`1`|`X WINS`/`1`| 最小入力とゼロ乗移動 |
 |`6`|`Y WINS`| 最初のプラスの負けポジション |
 |`1000000000`|`X WINS`/`4`| 最大入力と残留物`4`|
 |`11`|`X WINS`/`5`| の倍数の直前の境界`6`|

 ## 特殊なケース

 のために`n = 1`、アルゴリズムは次のように計算します。`1 % 6 = 1`。 移動を選択します`1`、これは合法です。`1 = 2^0`、残りの山はゼロになります。 出力は`X WINS`に続く`1`。 

のために`n = 4`、残りは`4`。 アルゴリズムが移動を選択します`4`、 そして`4`合法であるため、`4 = 2^2`。 すぐに山がゼロになるので、X が勝ちます。 このケースでは、移動セットに素数自体を超える素数累乗が含まれていることを検証します。 

のために`n = 6`、残りはゼロであるため、アルゴリズムは次のように報告します。`Y WINS`。 Xを削除した場合`1`、`2`、`3`、`4`、 または`5`、残りの山はそれぞれ`5`、`4`、`3`、`2`、 または`1`、すべての非倍数`6`。 これらの各ポジションにより、次のプレイヤーは対応する残留物を削除し、次のポジションの倍数に戻ることができます。`6`。 

のために`n = 12`、まったく同じ議論が当てはまります。 あらゆる法的措置には残留物が残る`1, 2, 3, 4, 5`モジュロ`6`, したがって、X は別の負けポジションに移動することはできません。 Y はその剰余を削除することで常に答えることができ、次のように割り算を復元できます。`6`。 

最大値については`n = 1,000,000,000`、残りは`4`したがって、プログラムは次のように出力します。`X WINS`そして`4`。 結果として得られるパイルには次のものが含まれます`999,999,996`Cookie、これは次のように割り切れます。`6`。 の大きさ`n`実行される操作の数には影響しません。
