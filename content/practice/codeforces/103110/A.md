---
title: "CF 103110A - 登山"
description: "$n = s + t$ とします。 チェイス シーケンス $C{st}$ は、すべての $(s,t)$ の組み合わせのグレイコード順序付けであり、連続する組み合わせは、バイナリ表現での $0$s の連続ブロックにわたる $1$ の単一単位の転送によって異なります。これは、同等にリストを更新することによって行われます…"
date: "2026-07-03T20:59:51+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103110
codeforces_index: "A"
codeforces_contest_name: "mBIT Standard Spring 2021"
rating: 0
weight: 103110
solve_time_s: 149
verified: false
draft: false
---

[CF 103110A - 登山](https://codeforces.com/problemset/problem/103110/A)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 29 秒
 **確認済み:** いいえ

 ## 解決策
 ## セットアップ

 しましょう$n = s + t$。 チェイスシーケンス$C_{st}$すべてのグレーコード順序付けです$(s,t)$- 連続する組み合わせが、単一ユニットの転送によって異なる組み合わせ。$1$連続したブロック全体にわたって$0$バイナリ表現での s、つまりリスト表現の更新によるもの$c_t \dots c_1$単一のエントリを変更することによって。 

影響を受ける座標の変化が以下の場合、トランジションは **完璧** と呼ばれます。$1$大きさが大きく、変化が**不完全**である場合$2$アルゴリズム L およびセクション 7.2.1.3 の前述の演習で使用される分類と同様に、大きさが異なります。 それぞれのステップ$C_{st}$1 つのコンポーネントだけを変更する$c_j$。 

タスクは、全サイクルのステップ数を決定することです。$C_{st}$それは不完全なものです。 

## 解決策

 を表します$(s,t)$-バイナリ文字列による組み合わせ$a_{n-1}\dots a_0$まさに$t$もの。 Chase のシーケンスでは、各トランジションは単一の動きをします。$1$パターンを交換することで隣接する位置間で$10 \leftrightarrow 01$。 で$c_t \dots c_1$これは、選択された 1 つのインデックスを移動することに相当します。$c_j$隣接する選択した要素の局所的な構成に応じて、正または負の量で変化します。 

移行はまさに完璧です$1$他から孤立している$1$これは、それが誘導された合成表現の最大ブロックの境界に位置し、その結果、動きが 1 つのユニット ギャップにのみ影響を与えるという意味です。 この場合、対応する更新により、単一のギャップ変数が次のように変更されます。$\pm 1$したがって変更されます$c_j$による$1$。 

不完全な遷移は、まさに移動時に発生します。$1$ローカルギャップ構造が 2 レベルの調整を強制する構成と相互作用します。$c$-コーディネート。 これは、ローカル バイナリ パターンに、バイナリ ストリング内の 1 ステップの転送によって 1 つの追加の選択位置にわたるキャリーが生成される構成が含まれているという意味で、移動要素が別の選択された要素に隣接している場合に正確に発生します。 で$c$-表現、これによりサイズのジャンプが発生します$2$ひとつのコーディネートで。 

したがって、不完全な遷移は、移動操作が 2 つの連続する境界で発生するバイナリ文字列に全単射的に対応します。$1$間の境界ではなく、構成の循環表現内にあります。$1$そして孤立した$0$-ブロック。 

それぞれの不完全な遷移は、次のデータを選択することによって一意に決定されます。 まずはインデックスを選択$j$の座標の$c_t \dots c_1$影響を受けるのは、$1 \le j \le t-1$最後の座標では 2 つのレベルにわたるキャリーが発生することはないためです。 次に、残りの構成を選択します$n-2$二重調整に含まれる 2 つのポジションを 1 つの有効なブロックに縮小した後のポジション。 この収縮により構造は次のように縮小されます。$(s,t-1)$-組み合わせオン$n-2$1 からのポジション$1$および 1 つの有効な隣接障害物が 1 つのユニットにマージされます。 

この対応関係は可逆的です。$(s,t-1)$-組み合わせオン$n-2$ポジションといずれかの選択$t-1$マージされた障害物が存在するギャップ、拡張により独自に再構築されます。$(s,t)$・その段階でのチェイス移動の組み合わせが不完全。 他の構成では、残りのすべての遷移が単一ギャップのシフトに対応するため、2 単位の変更は生成されません。 

の数$(s,t-1)$-組み合わせオン$n-2$要素は$\binom{n-2}{t-1}.$このような構成ごとに、マージされた障害物の位置を次の方法で選択できます。$t-1$の選択に対応する方法$t-1$の内部隣接$c$-シーケンスは不完全なジャンプに参加します。 したがって、不完全な遷移の総数は次のようになります。$(t-1)\binom{n-2}{t-1}.$アイデンティティの使用$\binom{n-2}{t-1} = \binom{s+t-2}{t-1}$そして対称性$n=s+t$、この式は同等の二項形式で書くこともできますが、これ以上単純化してもその組み合わせの意味が減じることはありません。 

したがって、チェイスのシーケンスのステップ数は$C_{st}$不完全な遷移を使用するものは、$\boxed{(t-1)\binom{n-2}{t-1}}.$## 検証

 それぞれの不完全な遷移には、必然的に 2 つの隣接するものの局所的な相互作用が含まれます。$1$s はバイナリ表現の巡回構造内にあります。そのような構成のみが、$c$-コーディネート。 

相互作用するペアを縮小すると、構成スペースが削減されます。$n$との位置$t$に$n-2$との位置$t-1$1 つ、正確に 1 つなので$1$は収縮したユニットに吸収され、相互作用部位から 1 つの位置が削除されます。 これにより、不完全な遷移と、$(s,t-1)$- 明確な内部隣接関係との組み合わせ。$t-1$構成ごとに。 

したがって、結果のカウントは次のようになります。$(t-1)\binom{n-2}{t-1}$不完全な各ステップは、その縮小されたイメージと影響を受ける隣接のインデックスによって一意に決定されるため、過大なカウントや過少カウントが発生しません。 

これで証明は完了です。 ∎

 ## 注意事項

 結果の構造は、不完全な遷移が「非局所的」キャリー伝播からのみ生じることを反映しています。$c$- 組み合わせの表現、およびそれらの列挙は、相互作用するペアの縮小後の区別された内部隣接関係を持つ構成をカウントすることに帰着します。
