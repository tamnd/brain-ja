---
title: "CF 103931H - 家宝の絵画"
description: "長さ $n$ のモールス符号語は、アルファベット ${cdot, -}$ 上のシーケンスであり、各ドットが重み $1$ に寄与し、各ダッシュが重み $2$ に寄与し、合計の重みは正確に $n$ になります。"
date: "2026-07-02T07:17:56+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103931
codeforces_index: "H"
codeforces_contest_name: "2022 Shanghai Collegiate Programming Contest"
rating: 0
weight: 103931
solve_time_s: 53
verified: false
draft: false
---

[CF 103931H - 家宝の絵画](https://codeforces.com/problemset/problem/103931/H)

 **評価:** -
 **タグ:** -
 **解決時間:** 53 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 ### パート (a)

 モールス信号の長さの単語$n$アルファベット上のシーケンスです${\cdot, -}$各ドットが重みを与える$1$各ダッシュは重みに寄与します$2$、総重量は正確に$n$。 これは次の合成に相当します。$n$部品に分割$1$そして$2$、ドットはエンコードします$1$そしてダッシュはエンコードします$2$。 

すべての頂点がそのような重みの単語であるグラフを定義します$n$。 ダッシュを 2 つの連続するドットに置き換えるか、2 つの連続するドットをダッシュ​​に置き換えることによって、一方の単語が他方の単語から取得できる場合、2 つの単語は隣接しています。 各移動は総重量を維持します。$2 \leftrightarrow 1+1$。 

タスクは、この頂点セット上でグレイ コードを構築することです。これは、連続する単語がそのようなローカル置換の 1 つだけ異なるハミルトニアン パスを意味します。 

シーケンスを再帰的に構築します。 させて$G(n)$重要なすべてのモールス語の順序を示します$n$。 

のために$n=0$空の単語が 1 つあります。 のために$n=1$あるだけです$\cdot$、 それで$G(1)=\cdot$。 

のために$n \ge 2$、すべての言葉に重みがあります$n$どちらかで終わる$\cdot$またはで$-$。 これにより、単語のセットが 2 つのクラスに分割されます。 

で終わる言葉$\cdot$重みのある言葉に対応する$n-1$最後のドットとで終わる単語を削除すると、$-$重みのある言葉に対応する$n-2$最後のダッシュを削除します。 

これは全単射を与えます$$\{\text{words of weight } n \text{ ending in } \cdot\} \leftrightarrow G(n-1),$$

$$\{\text{words of weight } n \text{ ending in } -\} \leftrightarrow G(n-2).$$再帰を定義します$$G(n) = \bigl(\cdot \, G(n-1)\bigr) \;\; \text{concatenated with} \;\; \bigl(- \, G(n-2)\bigr)^{R},$$どこ$(\cdot,G(n-1))$接頭辞を意味します$\cdot$のすべての単語に$G(n-1)$、 そして$( -,G(n-2))^R$接頭辞を意味します$-$のすべての単語に$G(n-2)$そしてリストの順序を逆にします。 

これにより、重みのあるすべての言葉が生成されます$n$すべての単語は、最後の記号に応じて 2 つのケースのいずれかに一意に分類されるためです。 

内部の連続トランジション$\cdot,G(n-1)$最後のドットを削除すると、両方の単語が隣接する要素に減らされるため、帰納法によって隣接性が維持されます。$G(n-1)$そしてドットを再アタッチすると、許可された移動構造が保持されます。 

内部でも同様です$-;G(n-2)^R$同じ帰納的議論により、$G(n-2)$逆の順序で。 

2 つのブロック間の接続を確認する作業が残っています。 最後の言葉は、$\cdot,G(n-1)$は$\cdot$～の最後の単語に接頭辞を付ける$G(n-1)$、構造に従って終了記号を交互に配置することによって再帰的に取得されます。 の最初の言葉$-;G(n-2)^R$は$-$～の最後の単語に接頭辞を付ける$G(n-2)$。 これら 2 つの単語は、最初の構造の単一のドットが 2 番目の構造のダッシュに置き換えられる終端領域で正確に異なります。これは、許可された変換に対応します。$\cdot\cdot \leftrightarrow -$境界位置に適用されます。 したがって、連結点では隣接関係が維持されます。 

これにより、長さのすべてのモールス語を生成するグレイ コードが確立されます。$n$。 

### パート (b)

 表示されている文字列は、$$q\ q\ q\ q\ q,$$は 15 個の点で構成される単語を表します。$q$はドットに対応し、ダッシュのグループ化は存在しません。 

パート (a) で構築されたグレーの順序付けでは、最初の単語はすべてドットの単語です。$\cdot^{15}$、次の変更は、2 つの連続するドットをダッシュ​​に置き換えることができる右端の位置に最初の許容される変換を適用することによって行われます。 

の右端のドットのペア$\cdot^{15}$位置にあります$14$そして$15$, したがって、次の単語は、これら 2 つのドットをダッシュ​​に置き換えることによって取得されます。 

したがって、後続語は、13 個のドットとそれに続く 1 つのダッシュで構成される単語になります。$$\underbrace{\cdot\cdots\cdots}_{13}\ -.$$### 注記

 構造はフィボナッチ立方体 偽装されたグレイコード: 重みのモールス語$n$ある長さのタイリングに対応します。$n$サイズのタイルごとにセグメント化する$1$そして$2$、再帰は最後のタイルに従って分割されます。 2 番目のブロックの反転は、結合時の単一ビット (ここでは単一ローカル置換) の変更を保証する標準メカニズムです。
