---
title: "CF 104248J - 誕生日"
description: "$k ge 2$ を偶数とし、$(kr+2)$-cube $G = Gk G{k-1} cdots G1 G0 G{-1}$ を考えます。ここで、$Gi$ は $i0$ および $G0 = G{-1} = P2$ の $r$-cube です。 頂点 $v$ は $$v = vk v{k-1} cdots v1 v0 v{-1},$$ と書かれます。ここで、$i0$ の場合は {0,1}^r$ の $vi が、{0,1}$ の $v0,v{-1} となります。"
date: "2026-07-01T22:11:48+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104248
codeforces_index: "J"
codeforces_contest_name: "Udmurt SU Contest 2010"
rating: 0
weight: 104248
solve_time_s: 130
verified: false
draft: false
---

[CF 104248J - 誕生日](https://codeforces.com/problemset/problem/104248/J)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 10 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 しましょう$k \ge 2$平等になって考慮してください$(kr+2)$-キューブ$G = G_k G_{k-1} \cdots G_1 G_0 G_{-1}$、 どこ$G_i$です$r$-キューブ用$i>0$そして$G_0 = G_{-1} = P_2$。 頂点$v$と書かれています$$v = v_k v_{k-1} \cdots v_1 v_0 v_{-1},$$どこ$v_i \in {0,1}^r$のために$i>0$そして$v_0,v_{-1} \in {0,1}$。 

のために$i>0$、 させて$s_i$のパリティになる$v_i$。 を定義します$k$-ビット署名$$\sigma(v) = s_k s_{k-1} \cdots s_2 (s_1 \oplus v_0).$$これを一般化すると、$k=4$最後の座標をねじる構造。$v_0$。 

のために$1 \le l \le k$、 させて$M_l(v)$をひっくり返す完璧なマッチングになります$l$- 番目のブロック$v_l$内部$G_l$他のすべてのブロックは固定されたままになります。 させて$M_0(v) = v \oplus 2$、2ビット座標を反転します$(v_0,v_{-1})$。 のように、$k=4$場合、これら$k+1$マッチングはペアごとに素であり、すべての入射エッジをカバーします。$G$。 

各頂点$v$出力エッジを 1 つだけ選択します$M_{l(v)}(v)$機能に応じて$l(v)$その署名によって決定されます。 

この構造は、固定ルックアップ テーブルを置き換えることによって拡張されます。$k=4$体系的に分割されたケース${0,1}^k$の中へ$k+1$によってインデックス付けされたクラス${0,1,\dots,k}$。 

グレーの順序を定義します$k$- バイナリ反映グレイコード経由のビット文字列$g(0), g(1), \dots, g(2^k-1)$(7.2.1.1-(5))より。 このサイクルを次のように分割します$k+1$連続したブロック$$B_0, B_1, \dots, B_k$$各ブロックには、グレー ウォークの最初の遷移が、次のブロック間の個別の座標方向に対応する文字列が含まれるようにします。${0,1,\dots,k}$。 パリティ条件の定義$\sigma(v)$隣接する頂点が確実に$G$反転するため、グレー サイクルで隣接するシグネチャが誘発されます。$v_l$パリティ ビットを 1 つだけ変更します$s_l$そして、上位のブロックはすべて変更されないままになります。 

定義する$l(v)$次のような一意のインデックスになります$\sigma(v) \in B_{l(v)}$。 

この定義により、各頂点は$v$出力エッジが 1 つだけあります$v \to M_{l(v)}(v)$, そのため、有向グラフは頂点が互いに独立したサイクルに分解されます。 

サイクル構造の正しさは、演習 45 で使用したものと同じ不変条件から導き出されます。任意のエッジに沿って$v \to M_l(v)(v)$、のみ$l$- 番目の座標ブロックが変更されるため、署名が$\sigma(v)$上のグレーグラフのシングルビット反転によって進化します。${0,1}^k$。 したがって、生成されたウォークに沿った署名シーケンスは、$k$-次元のグレーサイクル、すべてのステップは所定のパーティションブロック内に残ります$B_{l(v)}$。 

以来$k$偶数、グレー サイクルがオン$k$ビットは、1 の数のパリティと追加のツイストに関して 2 部構成です。$(s_1 \oplus v_0)$の間の一貫性を維持します。$G_1$方向と$M_0$トグル。 これにより、有向サイクルがブロック内の方向を反転できないことが保証されるため、すべての最大歩行は、次の場合とまったく同じようにサイクルに近づきます。$k=4$場合。 

グラウンド頂点は、その署名が次の累乗である頂点です。$2$つまり、アクティブな座標が 1 つだけあるグレイ コード頂点です。 兄弟地表頂点は、演習 45: 同一の頂点を持つものとまったく同じように定義されます。$v_k \cdots v_1$しかし違うのは$(v_0,v_{-1})$。 

同値関係を定義する$u \equiv v$もし$u$そして$v$同じ有向サイクル上にあるか、兄弟地表頂点であるか、またはそのような関係のチェーンによって接続されています。 演習 45 のパート (b) の構築はそのまま拡張されます。唯一必要なプロパティは、各グラウンド頂点が 2 つのグレイ コード間隔の境界にあり、マッチング$M_0$署名の一貫性を維持しながら 2 つの兄弟を接続します。 

各同値クラスは再び、有向サイクルの結合を誘導します。この結合では、兄弟地面頂点が強制的にマージされます。$M_0$演習 45 のパート (c) とまったく同じように、エッジはそのままにします。 

単一のハミルトニアン サイクルを取得するには、演習 45(d) と同じスプライシング引数を適用します。 各同値クラス サイクルには、そのクラス内のすべての頂点が含まれており、追加された頂点は$M_0$エッジは、これらのサイクルを接続して、単一の接続された 2 つの正規グラフを作成します。 選択したエッジの結合ではすべての頂点の次数が 2 であるため、接続性は、構造がすべてをカバーする単一サイクルであることを意味します。$2^{kr+2}$頂点。 

マッチングの選択$M_1,\dots,M_k$上のグレー サイクルのパーティションを選択することに対応します。$k$ビット入り$k+1$ラベル付き遷移クラス。 このような各ラベル付けは、パリティ制約と互換性のある座標方向の循環順序付けによって決定されます。 したがって、許容されるグローバルな再ラベル付けの数は、$k+1$グレー サイクルの隣接関係を考慮したシンボル。$k$そして注文されています$k!$周期対称まで。 

したがって、拡張機能は演習 45 のメカニズムを保存します。署名はグレー ウォークを誘発します。$k$ビット、マッチングにより座標反転が実現され、同値クラスのマージによりハミルトニアン サイクルが生成されます。 

これで、演習 45 の拡張が完了しました。$(kr+2)$-偶数の立方体$k$。 ∎
