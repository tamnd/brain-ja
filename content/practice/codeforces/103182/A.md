---
title: "CF 103182A - 企業の問題"
description: "練習問題 12 の正規基底 $(alpha1,ldots,alphat)$ は、このセクションでは、$n$ の中の $t$ の区別された位置の選択として、つまり $n=s+t$ の $(s,t)$ の組み合わせとして表され、$sum ai=t$ を満たすバイナリ文字列 $a{n-1}ldots a1a0$ によってエンコードされます。"
date: "2026-07-03T16:23:17+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103182
codeforces_index: "A"
codeforces_contest_name: "AGM 2021, Final Round, Day 2"
rating: 0
weight: 103182
solve_time_s: 132
verified: false
draft: false
---

[CF 103182A - 企業の問題](https://codeforces.com/problemset/problem/103182/A)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 12 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 正規の基礎$(\alpha_1,\ldots,\alpha_t)$演習 12 の内容は、このセクションでは次の選択肢として表されます。$t$の間で傑出した地位$n$、等価的に$(s,t)$-との組み合わせ$n=s+t$、バイナリ文字列によってエンコードされる$a_{n-1}\ldots a_1a_0$満足のいく$\sum a_i=t$。 ステップが 1 ビットだけ変更されるという条件は、連続する設定が 1 回のトグルによって異なることを意味します。$a_i \leftarrow 1-a_i$。 

したがって、タスクは、長さのすべてのバイナリ文字列を訪問するグレー パスを構築することです。$n$まさに$t$1 つは、連続する文字列が 1 つの座標だけで異なるという制限付きです。 

この構築では、(7.2.1.3) の組み合わせの合成形式を使用します。 それぞれ$(s,t)$- 組み合わせは構成に一意に対応します$s = q_t + \cdots + q_1 + q_0$と$q_j \ge 0$式 (11) により、バイナリ文字列は次の構造になります。$a_{n-1}\ldots a_0 = 0^{q_t}1\,0^{q_{t-1}}1\cdots 1\,0^{q_0}.$バイナリ文字列内の 1 ビットの変更は、合成ベクトルの 2 つの隣接するコンポーネント間で 1 単位を転送することに対応します。$(q_t,\ldots,q_0)$、または同等に、連続する 1 間のギャップを 1 つだけ変更します。 

グレーのパスは帰納法によって構築されます。$t$。 のために$t=0$構成は 1 つだけであるため、この主張は成り立ちます。 すべてに対してグレーのパスが存在すると仮定します。$(s',t')$と$s'+t'<n$。 固定用$n=s+t$、すべてをパーティション分割$(s,t)$- 1 の最初の位置に応じた組み合わせ。左端の 1 がその位置にある場合$k$、残りは$t-1$それらは$(s-k,t-1)$-長さの接尾辞の組み合わせ$n-k-1$。 これにより、固定された構成のセットが識別されます。$k$すべての翻訳コピーとして$(s-k,t-1)$-組み合わせ。 

それぞれの固定内で$k$、帰納法仮説は、サフィックス構成にグレイ パスを提供し、一度に 1 ビットずつ変更します。 ブロックからの移行$k$ブロックする$k+1$左端の 1 を 1 つ左にシフトし、ローカル パターンを置き換えることによって実現されます。$01$による$10$。 これはバイナリ文字列内の 1 ビットの変更です。 

グローバルなグレー パスを確保するために、ブロックの走査は交互方向に実行されます。$k$増加すると、サフィックス上の誘導グレー パスが前方にトラバースされます。 いつ$k$減少すると、逆に横断されます。 このリフレクションにより、ブロックのエンドポイントが$k$ブロックの先頭とは異なります$k+1$左端の 1 をシフトするのとまったく同じ単一ビットの移動です。 

したがって、すべてのステップで、誘導グレイ パスによってサフィックスが変更されるか、単一の隣接位置にわたって先頭の 1 が移動されます。どちらも正確に 1 ビット変更されます。 毎$(s,t)$- 各ブロックは 1 回だけ使い尽くされ、すべての合成は左端の 1 の位置によってブロックに一意に割り当てられるため、組み合わせは 1 回だけ表示されます。 

これで証明は完了です。 ∎
