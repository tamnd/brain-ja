---
title: "CF 102697153 - パリティ チェッカー"
description: "正確に 9 つの 2 進数を受け取ります。 最初の桁はパリティ ビットで、残りの 8 桁は送信バイトを構成します。"
date: "2026-08-16T16:29:15+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102697
codeforces_index: "153"
codeforces_contest_name: "CodeRams Practice Problem Archive"
rating: 0
weight: 102697
solve_time_s: 401
verified: true
draft: false
---

[CF 102697153 - パリティ チェッカー](https://codeforces.com/problemset/problem/102697/153)

 **評価:** -
 **タグ:** -
 **解決時間:** 6 分 41 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 正確に 9 つの 2 進数を受け取ります。 最初の桁はパリティ ビットで、残りの 8 桁は送信バイトを構成します。 この問題で使用されるパリティ規則は少し特殊です。パリティ ビットは次のとおりです。`1`バイトに偶数が含まれる場合`1`ビット、そして`0`バイトに奇数が含まれる場合`1`ビット。 受信した 9 ビット シーケンスがその規則に従っているかどうかを判断する必要があります。 

ルールを表現する最も簡単な方法は、9 ビットすべてを一緒に調べることです。 バイトに偶数が含まれている場合、`1`つまり、最初のビットは次のようにする必要があります`1`, したがって、の合計数は`1`9 ビット シーケンスの s は奇数です。 バイトに奇数が含まれている場合、`1`つまり、最初のビットは次のようにする必要があります`0`、合計は再び奇数になります。 したがって、有効な送信には常に奇数の`1`9 つのポジションすべてに s が含まれています。 

入力サイズは固定されているため、意味のある大きなサイズはありません。`n`パフォーマンスの問題。 最大 9 文字を検査しますが、これは入力に関係なく一定の作業です。 1 秒の時間制限と 256 MB のメモリ制限は、このような小さな入力に必要な量よりもはるかに寛大です。 たとえ少量の指数列挙を伴うアプローチでも不必要ですが、単一パスですぐに十分です。 

8 つのデータ ビットだけをやみくもにチェックすると、間違いが生じる可能性があるケースがいくつかあります。 例えば、`100000000`ゼロがある`1`バイト内の s は偶数であるため、パリティ ビットは次のようになります。`1`。 正しい出力は次のとおりです`NO ERROR`。 シーケンス全体に偶数の要素が含まれることを期待する不注意な実装`1`s はそれを誤って拒否してしまいます。 

逆の状況は、`000000001`。 バイトには 1 つが含まれます`1`これは奇数であるため、パリティ ビットは次のようになります。`0`。 正しい出力は次のとおりです`NO ERROR`。 バイト自体が偶数パリティであるかどうかだけをチェックすると、最初のビットが奇数パリティの反対の値をエンコードするはずであるという事実を見逃してしまいます。 

オールゼロのシーケンス`000000000`も便利です。 そのバイトにはゼロが含まれています`1`s であるため、必要なパリティ ビットは次のようになります。`1`、 ない`0`。 正しい出力は次のとおりです`TRANSMISSION ERROR`。 これにより、すべてゼロのメッセージが常に有効であると誤って想定する実装が捕捉されます。 

## アプローチ

 直接的なアプローチは、次の数を数えることです。`1`■ 8 ビット メッセージ内のカウントが偶数か奇数かを判断し、必要なパリティ ビットを最初の入力文字と比較します。 これは正しいです。パリティ定義は正確にこれら 8 つのデータ ビットに対する条件であるからです。 最悪の場合は 8 つのデータ位置すべてを検査するため、8 ビット検査と 1 回の比較が実行されます。 入力には常に 9 文字が含まれるため、これは定数時間であり、十分に高速です。 

さらに有益な観察は、実際にはメッセージからパリティ ビットを分離する必要がないということです。 バイトに次の内容が含まれているとします。`k`もの。 もし`k`が偶数である場合、最初のビットは次でなければなりません`1`、与える`k + 1`、合計 1 の奇数。 もし`k`は奇数です。最初のビットは次でなければなりません`0`、出発`k`、これも奇妙です。 したがって、有効なシーケンスは 1 つの単純な条件によって特徴付けられます。つまり、その 9 ビットには奇数の`1`s.

 これにより、問題は数を数えるということになります。`1`文字列全体の s を調べて、そのカウントのパリティをチェックします。 ブルート フォース バージョンは、データ ビットが 8 ビットしかないため機能しますが、入力の約 2 つの別々の部分を推論します。 パリティ観察はその区別を取り除き、検証を単一の条件に変えます。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース | O(9) = O(1) | お(1) | 承認済み |
 | 最適 | O(9) = O(1) | お(1) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. 9 ビット文字列を読み取ります。 入力にはシーケンス内にスペースが含まれていないため、1 つの文字列として直接読み取ることができます。 
2. 完全な 9 ビット文字列内で以下に等しい文字が何個あるかを数えます。`'1'`。 パリティ ルールはすべて 1 の合計数で表現できるため、9 つの位置をすべて数えます。 
3. カウントが奇数かどうかを確認します。 パリティ ビットの定義から導出されるように、有効な送信には 1 の合計数が奇数でなければなりません。 
4. 印刷`NO ERROR`カウントが奇数のとき。 それ以外の場合は、印刷します`TRANSMISSION ERROR`。 

なぜ機能するのか: の数を考えてみましょう。`1`8 ビット メッセージ内の s`k`。 いつ`k`が偶数である場合、有効であるためにはパリティ ビットが次である必要があります。`1`、したがって、完全なシーケンスには以下が含まれます`k + 1`それは奇妙です。 いつ`k`は奇数です。有効にはパリティ ビットが次である必要があります。`0`、したがって、完全なシーケンスには以下が含まれます`k`これも奇妙です。 したがって、すべての有効なシーケンスには奇数の 1 が含まれます。 逆に、完全なシーケンスに奇数の 1 が含まれる場合、偶数のデータ カウントにより最初のビットは強制的に次のようになります。`1`、一方、奇数のデータ数では強制的に次のようになります。`0`、必要なパリティ ビットと正確に一致します。 条件は必要かつ十分です。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve(s):
    ones = s.count('1')

    if ones % 2 == 1:
        return "NO ERROR"
    return "TRANSMISSION ERROR"

def main():
    s = input().strip()
    print(solve(s))

if __name__ == "__main__":
    main()
```の`solve`関数は 9 ビット シーケンスを受け取り、`count('1')`1の合計数を取得します。 この問題は数値ではなく個々の 2 進数に関係しているため、シーケンスを整数に変換する必要はありません。 

モジュロ演算では、合計カウントが奇数かどうかをチェックします。 出力文字列は、スペースや大文字の使用も含めて正確に再現されます。 

インデックス操作がないため、誤ってパリティ ビットをバイトの一部として扱ったり、最終データ ビットを省略したりすることによるオフバイワンのリスクがありません。 Python の整数にもオーバーフローの心配はありませんが、ここでの最大数はわずか 9 です。 

への呼び出し`strip()`標準入力によって生成された改行を削除します。 入力には 9 桁の 2 進数が含まれているため、シーケンスから意味のある文字は削除されません。 

## 実用的な例

 ### サンプル 1

 提供されたサンプルは、`100110101`。 

| ステップ | シーケンス | の数`1`s | パリティ |
 | --- | --- | --- | --- |
 | 1 |`100110101`| 5 | 奇数 |
 | 2 |`100110101`| 5 | 有効 |
 | 3 |`100110101`| 5 |`NO ERROR`|

 完全なシーケンスには 5 つあります。 5 は奇数であるため、パリティ条件が満たされ、送信は有効になります。 

### 例 2

 検討してください`100000000`。 

| ステップ | シーケンス | の数`1`s | パリティ |
 | --- | --- | --- | --- |
 | 1 |`100000000`| 1 | 奇数 |
 | 2 |`100000000`| 1 | 有効 |
 | 3 |`100000000`| 1 |`NO ERROR`|

 8 ビットのメッセージは次のとおりです。`00000000`、これには 1 が含まれていないため、偶数パリティが得られます。 先頭のパリティビットは正しくあります`1`。 したがって、完全なシーケンスには 1 つの要素が含まれます。`1`、これは奇妙です。 

### 例 3

 検討してください`000000000`。 

| ステップ | シーケンス | の数`1`s | パリティ |
 | --- | --- | --- | --- |
 | 1 |`000000000`| 0 | 偶数 |
 | 2 |`000000000`| 0 | 無効 |
 | 3 |`000000000`| 0 |`TRANSMISSION ERROR`|

 メッセージには 1 が含まれていないため、パリティは偶数であり、先頭ビットは`1`。 先頭ビットが 0 の場合、送信は無効になります。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | お(1) | ちょうど 9 文字が検査されます。 |
 | スペース | お(1) | 入力文字列と単一の整数カウントのみが保存されます。 |

 入力サイズは 9 ビットに固定されているため、ソリューションは一定量の作業のみを実行します。 1 秒の制限時間内に収まり、256 MB の制限に比べてメモリ使用量は無視できます。 

## テストケース```python
# helper: run solution on input string, return output string
import sys
import io

def solve(s):
    ones = s.count('1')

    if ones % 2 == 1:
        return "NO ERROR"
    return "TRANSMISSION ERROR"

def run(inp: str) -> str:
    old_stdin = sys.stdin
    sys.stdin = io.StringIO(inp)
    try:
        s = sys.stdin.readline().strip()
        return solve(s) + "\n"
    finally:
        sys.stdin = old_stdin

# provided sample
assert run("100110101\n") == "NO ERROR\n", "sample 1"

# all zeros: even data parity requires a leading 1
assert run("000000000\n") == "TRANSMISSION ERROR\n", "all zeros"

# all ones: eight data ones are even, so the leading 1 is correct
assert run("111111111\n") == "NO ERROR\n", "all ones"

# one data bit set: odd data parity requires a leading 0
assert run("000000001\n") == "NO ERROR\n", "single data one"

# correct data parity but wrong parity bit
assert run("100000001\n") == "TRANSMISSION ERROR\n", "wrong leading parity bit"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`000000000`|`TRANSMISSION ERROR`| すべてゼロの境界の場合と必要な先頭パリティ ビット |
 |`111111111`|`NO ERROR`| すべて等しい値と 1 の最大数 |
 |`000000001`|`NO ERROR`| 最小の非ゼロバイトを持つ奇数データパリティ |
 |`100000001`|`TRANSMISSION ERROR`| 不正な先頭パリティ ビットを正しく検出する |

 ## 特殊なケース

 のために`100000000`、アルゴリズムは正確に 1 をカウントします`1`。 カウントが奇数なので印刷します`NO ERROR`。 基礎となるバイトには 1 が 0 個あり、カウントは偶数で、先頭に`1`まさに必要なパリティ ビットです。 

のために`000000001`、アルゴリズムは再び 1 をカウントします`1`、それで印刷されます`NO ERROR`。 ここのバイトには 1 つあります`1`、これは奇数であり、先頭のパリティ ビットは正しいです`0`。 これは、パリティ ビットがバイトのパリティと単純に等しいと期待できない理由を示しています。 

のために`000000000`、アルゴリズムは 0 の 1 を数えます。 ゼロは偶数なので印刷されます`TRANSMISSION ERROR`。 バイトには偶数パリティがありますが、最初のビットは`0`いつであるべきか`1`。 

のために`111111111`、アルゴリズムは 1 を 9 つ数えます。 9 は奇数なので印刷します`NO ERROR`。 8 ビットのメッセージには 8 つの 1 が含まれており、これは偶数であるため、先頭が 1 になります。`1`正しい。 これは、最終データ位置がカウントに含まれていることを確認するのにも役立ちます。
