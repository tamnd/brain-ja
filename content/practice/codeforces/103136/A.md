---
title: "CF 103136A - \u0411\u0443\u0444\u0435\u0442"
description: "文字列 $alpha$ が $t$ ゼロと $s$ 記号を正確に含む ${+, -, 0}$ の記号で構成されているとします。ここで、ゼロ以外の各記号は $+$ または $-$ のいずれかです。 R ブロックは $-^k+$, $k ge 0$ という形式の部分文字列で、直前に $0$ があり、その後には $-$ がありません。"
date: "2026-07-03T20:03:14+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103136
codeforces_index: "A"
codeforces_contest_name: "Student programming contest in Penza"
rating: 0
weight: 103136
solve_time_s: 161
verified: false
draft: false
---

[CF 103136A - \u0411\u0443\u0444\u0435\u0442](https://codeforces.com/problemset/problem/103136/A)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 41 秒
 **確認済み:** いいえ

 ## 解決策
 ## セットアップ

 文字列を入れてみましょう$\alpha$からのシンボルで構成されます${+, -, 0}$まさに$t$ゼロと$s$符号。ゼロ以外の各記号は次のいずれかです。$+$または$-$。 

**R ブロック** は次の形式の部分文字列です。$-^k+$、$k \ge 0$、その直前に$0$そしてその後には続きません$-$。 

**L ブロック** は次の形式の部分文字列です。$+-^k$、$k \ge 0$、その直後に続きます$0$。 

少なくとも 1 つのブロックが存在する場合、文字列には次のように定義されたサクセサーがあります。 右端のブロックが R ブロックの場合、右端にある次のブロックを置き換えます。$0-^k+1$による$-+^k0$。 それ以外の場合、右端のブロックは L ブロックであり、右端のブロックは$+-^k0$に置き換えられます$0+^{k+1}$。 この置換後、変更されたブロックの右側にある最初の符号を厳密に無効にします。 

目標は、この後続ルールをすべての文字列のセットで分析することです。$s$標識と$t$ゼロを使​​用して、結果として得られる有向グラフの構造的特性を証明し、そのような文字列すべてをカバーする単一のチェーンを形成することを示します。 

## 解決策

 後続の操作では、シンボルのマルチセットが保存されます。 各置換では 1 つだけ保持されます$0$そして$s$兆候、以来$0-^k+$パターンはに変換されます$-+^k0$、同じ数のゼロと符号が含まれており、同様に$+-^k0$になる$0+^{k+1}$一方、後続の否定はカウントを変更せずに符号のみを変更します。 したがって、状態空間は操作下では閉じられます。 

有向グラフを定義する$G$その頂点はすべて次の文字列です$t$ゼロと$s$標識であり、そのエッジが後継マップに対応します。 各頂点は最大でも出次数を持ちます$1$建設によって。 

R ブロックまたは L ブロックが発生しない場合、文字列にはブロックが存在しません。 R ブロックが存在しないということは、$0-^k+$必要な境界条件が存在するため、$0$続いて$-$の後には続けられません$+$。 L ブロックが存在しないということは、$+$続いて$-$の後には続けられません$0$。 これらの制約により、すべてが強制されます。$0$は、2 つの均一な符号ブロックを分離する単一の連続したブロックとして表示されます。$+$そして$-$によって区切られる$0$のいずれかを作成します$+-^k0$または$0-^k+$パターン。 したがって、すべてのブロックフリー文字列は次の形式になります。$$+^a 0^t -^b \quad \text{or} \quad -^a 0^t +^b,$$と$a+b=s$。 

逆に、どちらの形式の文字列にも部分文字列は含まれません。$0-^k+$または$+-^k0$、すべてから$0$は連続しており、両側の記号はすべて同一です。 これは、後続のない文字列を正確に特徴付けており、(a) の部分を証明しています。 

パート (b) では、有向サイクルが存在すると仮定します。 すべての頂点には最大でも出次数があるため、$1$、どのサイクルも、すべての頂点が次数を持つ単純な有向サイクルになります。$1$サイクル内で。 ポテンシャル関数を定義する$\Phi(\alpha)$の右端のブロックの位置として$\alpha$、左から右に測定します。 後続操作では常に右端のブロックが選択され、それが変更されます。その後、変換された領域によって選択された右端のブロックが削除され、その右側に新しいブロックを作成できないため、否定ステップによって新しく作成された適格な構造が左に厳密にシフトされます。 したがって$\Phi$後続ステップごとに厳密に減少します。 有限集合に沿った厳密な減少では、循環は除外されます。なぜなら、循環は暗黙的に意味するからです。$\Phi$正の長さのトラバース後に初期値に戻ります。 したがって、有向サイクルは存在しません。 

パート (c) については、次のようにします。$\alpha \mapsto \beta$後継ステップとなる。 ルールの定義はローカル パターンにのみ依存し、変更されたブロックの最初の符号の右側に適用される最終的な修正否定を除いて、グローバル否定の下では不変です。 グローバル否定を適用する$\beta$ごとに反転します$+$そして$-$ゼロの構造を維持しながら、R ブロックと L ブロックの役割を交換します。 の一番右のブロック$\alpha$否定の下で、の左端の該当するブロックに対応します。$-\alpha$、同じローカル変換が逆の順序で適用されます。 後続ルールの追加の否定ステップにより、グローバル符号反転の不一致が正確にキャンセルされ、ルールが次のように適用されます。$-\beta$回復する$-\alpha$。 これにより、$\alpha \mapsto \beta$暗示する$-\beta \mapsto -\alpha$したがって、各頂点には最大でも 1 つの先行要素があります。 

パート (d) については、次のように仮定します。$\alpha_0 \mapsto \alpha_1 \mapsto \cdots \mapsto \alpha_k$と$k>0$。 最初の変換は、次の右端のブロックを変更します。$\alpha_0$そして、必須の否定を通じて、その右側のシンボルの構成を厳密に変更します。 この操作により、少なくとも 1 つのオブジェクトの相対的な配置が変更されます。$0$そのブロックによって決定される標識領域間の境界に関して。 後続のすべてのステップは、以前に変更された位置の厳密に左側にあるブロックに対して動作し続けるため、後のステップでは元の相対順序が復元されません。$0$はそれらの位置にあります。 したがって、位置の複数セットは、$0$が入っています$\alpha_0$そして$\alpha_k$最初の動きですでに少なくとも 1 つが変更されているため、異なります。$0$-境界関係とそれを逆転させるステップはありません。 したがって、$\alpha_0$そして$\alpha_k$全部持ってない$0$は同じ位置にあります。 

パート (e) では、状態空間は有限であり、すべての頂点は最大でも出次数を持ちます。$1$したがって、有向グラフは、後続のない頂点で終わる互いに素な有向パスに分解されます。 部分 (b) ではサイクルが除外されるため、すべての最大有向パスは終了します。 パート (c) は最大でも次数を意味します$1$したがって、2 つの異なるパスが結合することはありません。 したがって、各頂点は正確に 1 つの最大チェーン上にあります。 

各チェーンはブロックのない文字列で始まり、ブロックのない文字列で終わります。 パート (a) の特徴付けは正確に次のことを示します。$2^{s}$サイン割り当て時間の選択$\binom{s+t}{t}$各チェーンは、その 2 つのエンドポイント間のすべての構成を反復することなくカバーする必要があります。 すべての頂点は 1 つのチェーンにのみ属し、他のチェーンと交差するチェーンはないため、チェーンのコレクションは文字列のセット全体を分割します。 後続ルールは各非終端頂点に対して全単射ステップを実行するため、各チェーンはそのコンポーネント内のすべての文字列を 1 回だけ走査します。 

これにより、次のすべての文字列が$s$標識と$t$ゼロは形式の 1 つのチェーンにのみ属します$$\alpha_0 \mapsto \alpha_1 \mapsto \cdots \mapsto \alpha_{\binom{s+t}{t}-1},$$そのコンポーネント内のすべての構成をカバーし、後続の構成が完全な状態空間にわたる互いに素なハミルトニアン連鎖への分解をもたらすという証明を完了します。 ∎

 ## 検証

 後続ルールでは、置換ごとに 1 つのゼロと 1 つのゼロを含む固定長パターンが書き換えられるため、シンボル数が保持されます。$k+1$1 つのゼロで別のパターンに署名し、$k+1$追加の否定はシンボル タイプにのみ影響し、多重度には影響しません。 

ブロックフリーの特性評価は徹底的です。$0$交互に$+$そして$-$必然的にどちらかの移行を引き起こします$+-^k0$または$0-^k+$、一方、主張されている正規形は構造上両方を回避しています。 

非循環性は、右端のアクティブなブロック位置によって与えられる厳密に減少する構造インデックスの存在から生じますが、後続変換では増加することはありません。 

大域的否定の下での対称性は、否定が交換されるため保持されます。$+$そして$-$2 つのブロック タイプの役割が逆転するまでパターン定義を均一に保持しますが、ルールはこれらの役割を交換した後に同じ構造変更を適用します。 

チェーンの素性は、サイクルの欠如と組み合わされた最大 1 の次数から生じます。これは、パスごとに 1 回正確にすべての頂点をカバーする有向パスのフォレストを意味します。 

## 注意事項

 この構造は、Chase の組み合わせ用のグレー コード メカニズムを 2 色に拡張したもので、ゼロは区切り文字として機能し、$+$そして$-$シンボルは、グローバルな潜在的な順序を維持するローカルな書き換えを通じて伝播します。 この構造は、層状グラフ上のハミルトニアン パスとして解釈できます。その層は固定ゼロ位置に対応し、その内部状態は符号付きの組み合わせの改良です。
