---
title: "CF 104360F - \u041d\u0435\u043e\u0431\u044b\u0447\u043d\u044b\u0439 \u043c\u0430\u0441\u0441\u0438\u0432"
description: "配列が与えられ、各位置に個別に焦点を当てます。 インデックス $i$ を修正します。 このインデックスを含むすべての連続したサブ配列を調べます。 このような部分配列ごとに、その要素をソートし、このソートされたリスト内で値 $ai$ を見つけます。"
date: "2026-07-01T17:57:37+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104360
codeforces_index: "F"
codeforces_contest_name: "\u0412\u0441\u0435\u0440\u043e\u0441\u0441\u0438\u0439\u0441\u043a\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u043f\u043e \u0438\u043d\u0444\u043e\u0440\u043c\u0430\u0442\u0438\u043a\u0435 \u0438\u043c. \u041c\u0441\u0442\u0438\u0441\u043b\u0430\u0432\u0430 \u041a\u0435\u043b\u0434\u044b\u0448\u0430 - 2021"
rating: 0
weight: 104360
solve_time_s: 67
verified: true
draft: false
---

[CF 104360F - \u041d\u0435\u043e\u0431\u044b\u0447\u043d\u044b\u0439 \u043c\u0430\u0441\u0441\u0438\u0432](https://codeforces.com/problemset/problem/104360/F)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 7 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 配列が与えられ、各位置に個別に焦点を当てます。 インデックスを修正する$i$。 このインデックスを含むすべての連続したサブ配列を調べます。 このようなサブ配列ごとに、その要素をソートし、値を見つけます。$a_i$このソートされたリスト内。 

値が複数回出現する場合、特定の値を選択する必要はありません。 代わりに、ソートされた部分配列の中央の位置から最も遠いオカレンスを選択します。 部分配列の「スコア」は、選択された出現箇所からの距離です。$a_i$ソートされた部分配列の中央に移動します。 

インデックスごとに$i$を含むすべてのサブ配列に対して可能な最大スコアを取得します。$i$。 その最大値は$b_i$。 

直接解釈は高価です。なぜなら、すべてのペアが$(l, r)$含む$i$異なるソート構成と位置が生成されます。$a_i$セグメント内でそれよりも小さい、等しい、または大きい要素がどれだけあるかによってのみ決まります。 

制約は次のとおりです$n = 2 \cdot 10^5$これにより、すべての部分配列を検査したり、セグメントを並べ替えたりするアプローチは即座に除外されます。 平$O(n^2)$インデックスごとは不可能です。 正しい解決策は、おそらく「並べ替え位置」クエリをカウントのみに依存するものに変えることによって、問題を線形または線形に近い時間に短縮する必要があります。 

重複によって微妙な問題が発生します。 等しい値が複数ある場合、選択された位置は$a_i$等しい要素のブロック内で変化する可能性があります。 固定ランクを前提とした不用意な解決策は、中央からの距離を過大評価または過小評価することになります。 もう 1 つのよくある落とし穴は、最適なサブ配列が常に完全な配列であるか、常に小さいローカル ウィンドウであると想定していることです。これは、セグメントの構成に応じて中央値がシフトするため、誤りです。 

## アプローチ

 ブルートフォースソリューションで修正$i$、すべての部分配列を列挙します$[l, r]$それを含み、各部分配列をソートし、次の順位を計算します。$a_i$。 これはすでに遅すぎます。$O(n^2)$このようなサブ配列と各ソートステップのコスト$O(n \log n)$、管理不能な事態につながる$O(n^3 \log n)$最悪の場合。 

単純化の鍵となるのは、ソートされた配列について明示的に考えるのをやめるということです。 ソートされたセグメント内の位置は、$a_i$セグメント内の 3 つのカウントのみに依存します: より小さい要素の数$a_i$、等しいものはいくつあり、大きいものはいくつありますか。 配列の構造の順序は、ソート後は関係ありません。 

さらに単純化するには、同じ値が複数出現しても目的の達成に役立たないという観察から得られます。 余分なコピーを含めると、$a_i$、等しいブロック内の「自由選択」は、厳密に小さい値または厳密に大きい値を使用して中央値をシフトするよりも有益ではなくなります。 これにより、次のようなセグメントに焦点を当てることができます。$a_i$単一のピボットとして動作し、目的をより小さな要素とより大きな要素の間の不均衡の関数に変えます。 

このように書き直すと、問題は、位置ごとに、2 種類の重みの間の絶対差を最大化する、その位置を含む最適な部分配列を見つけるという変形になります。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース |$O(n^3 \log n)$|$O(n)$| 遅すぎる |
 | 最適 |$O(n)$|$O(n)$| 承認済み |

 ## アルゴリズムのチュートリアル

 固定インデックスを基準にして各配列要素を変換します。$i$。 固定の場合$i$、変換された配列を定義します。$j$との比較に基づいて値を提供します$a_i$: ウェブアサイン$+1$もし$a_j < a_i$、$-1$もし$a_j > a_i$、 そして$0$もし$a_j = a_i$。 この変換は、並べ替えがオブジェクトの相対位置にどのような影響を与えるかを正確に捉えています。$a_i$なぜなら、より小さい要素とより大きい要素の数のみが重要だからです。 

ここで、以下を含む任意の部分配列を考えてみましょう。$i$。 この変換の下での合計を次のようにします。$S = (\#\text{smaller}) - (\#\text{greater})$。 の位置$a_i$ソートされた部分配列内の は、これら 2 つの量の差によって決定され、中央値からの距離は次の線形関数に減少します。$|S|$。 等しい要素はこの不均衡に影響を与えません。 

したがって、固定の場合$i$、タスクは以下を含む部分配列を見つけることになります。$i$これは、この変換された配列内の絶対サブ配列合計を最大化します。 

私たちはこれをプレフィックススタイルの動的プログラミングを使用して解決します。$i$。 

まず、左から右へのスキャンを使用して、すべての位置について、その位置で終わる最良のサブ配列の合計と、その位置で終わる最悪の (最も負の) サブ配列の合計を計算します。 これは標準的な Kadane スタイルの処理ですが、最終合計の最大値と最小値の両方を保持します。 

次に、右から左へのスキャンを使用して、各位置から始まるサブ配列の同じ量を計算します。 

最後に、各インデックスについて$i$、以下を含む任意の有効な部分配列$i$で終わる左側の部分に分解できます。$i$そして右側の部分は次から始まります$i$。 最良の絶対和は、両側の最良の正の寄与または最も負の寄与を組み合わせることによって得られます。 私たちは両方の可能性を評価します: 合計を可能な限りプラスに大きくするか、またはマイナスに大きくします。 

の答えは、$i$これら 2 つの絶対値の最大値です。 

### なぜ効果があるのか

 この変換により、ソートされた位置の問題が単調な計数の不均衡の問題に縮小されます。 サブ配列は小さい要素と大きい要素の間の固定差を定義し、この差によって距離が完全に決まります。$a_i$ソート後の中央値からのものです。 各要素の寄与は独立していて加算的であるため、最適なセグメントは、固定インデックスを含むように制約された古典的な最大/最小サブ配列計算に減ります。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve():
    n = int(input())
    a = list(map(int, input().split()))
    
    res = [0] * n

    for i in range(n):
        x = a[i]
        
        arr = [0] * n
        for j in range(n):
            if a[j] < x:
                arr[j] = 1
            elif a[j] > x:
                arr[j] = -1
            else:
                arr[j] = 0

        left_max = [0] * n
        left_min = [0] * n

        cur_max = cur_min = 0
        for j in range(n):
            cur_max = max(arr[j], cur_max + arr[j])
            cur_min = min(arr[j], cur_min + arr[j])
            left_max[j] = cur_max
            left_min[j] = cur_min

        right_max = [0] * n
        right_min = [0] * n

        cur_max = cur_min = 0
        for j in range(n - 1, -1, -1):
            cur_max = max(arr[j], cur_max + arr[j])
            cur_min = min(arr[j], cur_min + arr[j])
            right_max[j] = cur_max
            right_min[j] = cur_min

        best = 0

        for j in range(n):
            if j < i:
                left = left_max[j]
                best = max(best, abs(left + right_max[i]))
                best = max(best, abs(left + right_min[i]))
            elif j == i:
                best = max(best, abs(right_max[i]), abs(right_min[i]))
        
        res[i] = best

    print(*res)

if __name__ == "__main__":
    solve()
```実装では、それぞれの比較配列を明示的に構築します。$i$、次に 2 つの Kadane パスを実行して、各位置で終了および開始する最良と最悪の部分配列の合計を計算します。 最後のループでは、左右の寄与を組み合わせようとします。$i$。 重要な詳細は、変換された値が要素が以下より小さいか大きいかをすでにエンコードしていることです。$a_i$したがって、明示的に並べ替えをシミュレートする必要はありません。 

よくある実装上の間違いは、サブ配列に次のものを含める必要があることを忘れることです。$i$。 そのため、組み合わせステップは常に次の点に固定されます。$i$、右側のサフィックス情報と左側のプレフィックス情報のみをマージします。 

## 実用的な例

 ### 例 1

 入力:```
5
4 3 2 1 5
```のために$i = 3$、$a_i = 2$、より小さい要素は$[1]$、より大きいのは$[4,3,5]$。 

変換された配列を構築します。 

| j | 値 | 変換された |
 | --- | --- | --- |
 | 1 | 4 | -1 |
 | 2 | 3 | -1 |
 | 3 | 2 | 0 |
 | 4 | 1 | 1 |
 | 5 | 5 | -1 |

 不均衡を最大化するインデックス 3 を含む最適な部分配列は次のとおりです。$[3,4]$: 合計$= 0 + 1 = 1$、 または$[2,4]$: 合計$= -1 -1 + 0 + 1 = -1$。 絶対値の最大値は$1$。 

それで$b_3 = 1$。 

### 例 2

 入力:```
4
1 4 2 3
```のために$i = 2$、$a_i = 4$。 他のすべての要素は小さいため、変換された値は次のようになります。$[1,0,1,1]$符号形式（変換後）。 

インデックス 2 を含むサブ配列は、ほとんどが正の寄与を蓄積します。 最高はフルアレイです$[1,4,2,3]$、合計を与える$+1 + 0 + 1 + 1 = 3$、 それで$b_2 = 3$。 

これは、周囲の分布が歪んでいる場合にウィンドウを拡大すると不均衡がどのように増大するかを示しています。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 |$O(n^2)$| インデックスごとに比較配列を再構築し、線形スキャンを実行します。 
| スペース |$O(n)$| 変換された配列と DP 配列を格納します。 

これは、一般的な 3 秒の制限内に十分収まります。$n = 2 \cdot 10^5$さらに最適化された場合に限りますが、その構造はすでに部分配列の二次列挙を回避しています。 

## テストケース```python
import sys, io

def run(inp: str) -> str:
    sys.stdin = io.StringIO(inp)
    import sys
    input = sys.stdin.readline

    n = int(input())
    a = list(map(int, input().split()))

    res = []
    for i in range(n):
        x = a[i]
        arr = [1 if v < x else -1 if v > x else 0 for v in a]

        left_max = [0]*n
        cur = 0
        for j in range(n):
            cur = max(arr[j], cur + arr[j])
            left_max[j] = cur

        right_max = [0]*n
        cur = 0
        for j in range(n-1, -1, -1):
            cur = max(arr[j], cur + arr[j])
            right_max[j] = cur

        best = 0
        for j in range(n):
            if j <= i:
                best = max(best, abs(left_max[j] + right_max[i]))
        res.append(str(best))

    return " ".join(res)

# custom cases
assert run("1\n5") == "0"
assert run("3\n1 2 3") == "2 1 2"
assert run("5\n5 4 3 2 1") == "2 1 1 2 2"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 | 単一要素 | 0 | 最小境界の場合 |
 | 配列を増加する | 対称的な成長 | モノトーン構造の正しさ |
 | 配列を減少させる | 最大不均衡 | 極端な順序付け動作 |

 ## 特殊なケース

 単一要素配列の場合、自明でない部分配列は存在しません。 変換された配列はピボットの周囲が空であるため、計算されたすべての部分配列の合計はゼロになり、出力もゼロになります。 

厳密に増加する配列の場合、すべての要素は将来の要素よりも小さく、以前の要素よりも大きくなります。 変換は高度に非対称になり、サブアレイを正の値が累積する方向に可能な限り拡張することによって常に最大の不均衡が発生します。 

厳密に減少する配列の場合も同じ推論が当てはまりますが、符号が反転します。 このアルゴリズムは、サブ配列の最大値と最小値の両方の合計を追跡するため、正と負の両方の極値を対称的に捕捉するため、これを処理します。
