---
title: "CF 103037H - シンフォニー"
description: "正規基底 $(alpha1,ldots,alphat)$ を、${1,ldots,n}$ の個別の要素の順序付き $t$ タプルとして表すとします。 これは、$n$ から選択された $t$ の個別のシンボルを、順序が保存された状態で並べ替えることに相当します。"
date: "2026-07-04T01:54:55+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103037
codeforces_index: "H"
codeforces_contest_name: "UTPC Contest 04-02-21 Div. 1 (Advanced)"
rating: 0
weight: 103037
solve_time_s: 146
verified: false
draft: false
---

[CF 103037H - シンフォニー](https://codeforces.com/problemset/problem/103037/H)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 26 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 正規ベースにしましょう$(\alpha_1,\ldots,\alpha_t)$順序付けられたものとして表現される$t$- の異なる要素のタプル${1,\ldots,n}$。 これは次の順列と等価です$t$～から選択された個別のシンボル$n$、順序は保たれます。 2 つの塩基がちょうど 1 つの座標で異なる場合、目的のグレー パス内で隣接しています。つまり、インデックスが 1 つだけ存在します。$j$そのような$\alpha_j \neq \alpha'_j$、 その間$\alpha_i = \alpha'_i$みんなのために$i \neq j$。 

この隣接関係はジョンソン グラフを定義します$J(n,t)$注文済み$t$- 反復のないタプル。エッジは区別性を維持しながら 1 つの座標を変更することに対応します。 

タスクは、このグラフ内にハミルトニアン パスを構築することです。 

帰納法によってパスを構築します$n$、正規ベースの拡張の制御されたトラバースを使用して、$J(n-1,t)$に$J(n,t)$。 

まず、ハミルトニアン経路が存在すると仮定します。$J(n-1,t)$すべての正規ベースをリストする${1,\ldots,n-1}$。 このパスを次のように表します$$(\alpha_1^{(1)},\ldots,\alpha_t^{(1)}), (\alpha_1^{(2)},\ldots,\alpha_t^{(2)}), \ldots$$ここで、連続するタプルは 1 つの座標だけで異なります。 

これを次のように拡張します$J(n,t)$記号を挿入することで$n$隣接性を維持した可逆的な順序で、各座標位置に順番に配置されます。 

固定位置の場合$j$を置き換える操作を定義します。$\alpha_j$による$n$。 以来$n$どのタプルにも現れません${1,\ldots,n-1}$、この操作では区別性が維持され、1 つの座標だけが変更されます。 したがって、各塩基は$J(n-1,t)$生成する$t$の新しい拠点$J(n,t)$、各座標位置に 1 つずつ。 

これらのブロックを次の構造で連結します。 のために$j = 1$に$t$、次のハミルトニアン パスをたどります。$J(n-1,t)$、各タプルを置き換えて変更します。$j$- 番目の座標$n$、結果のブロックをパリティに応じて順または逆の順序で走査します。$j$。 この交互の方向により、最後のタプルが$j$- 番目のブロックとその最初のタプル$(j+1)$-st ブロックは 1 つの座標だけで異なります。$n$位置から移動される$j$位置を決める$j+1$。 

ブロック間の隣接性を検証するには、ブロックの最後の要素を考慮します。$j$。 それは持っています$n$所定の位置にある$j$そして何らかの価値$x \in {1,\ldots,n-1}$所定の位置にある$j+1$。 ブロックの最初の要素$j+1$もっている$n$所定の位置にある$j+1$そして同じ値$x$所定の位置にある$j$。 他のすべての座標は同一であるため、正確に 1 つの座標が変更されます: 位置$j$または$j+1$方向によっては。 これにより、有効なグレーの動きが得られます。 

各ブロック内では、帰納法仮説によって隣接関係が成立します。$J(n-1,t)$なぜなら、各ステップで変化する座標は 1 つだけだからです。 

したがって、すべて$t\binom{n}{t}$正規のベースは 1 回だけ訪問され、連続するベースは 1 つの座標だけで異なります。 この構造により、ハミルトニアン パスが生成されます。$J(n,t)$。 

最後に、最初と最後のブロックのエンドポイントは、循環再配置によってのみ異なるため、$n$すべての位置を通じて、同じ交互方向の引数は、最初の単一座標移動までの最後の要素を識別し、必要に応じてパスをサイクルに閉じます。 演習のステートメントにはパスのみが必要なので、終了条件は課されません。 

これで証明は完了です。 ∎
