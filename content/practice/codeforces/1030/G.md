---
title: "CF 1030G - 線形合同ジェネレーター"
description: "$n$ 次元のベクトルを離散ステップにわたって進化させるシステムが与えられています。 各座標は、同じ種類のルール、つまり素数を法とする線形変換を使用して独立して進化します。"
date: "2026-06-16T21:04:51+07:00"
tags: ["codeforces", "competitive-programming", "number-theory"]
categories: ["algorithms"]
codeforces_contest: 1030
codeforces_index: "G"
codeforces_contest_name: "Technocup 2019 - Elimination Round 1"
rating: 2900
weight: 1030
solve_time_s: 151
verified: true
draft: false
---

[CF 1030G - 線形合同ジェネレーター](https://codeforces.com/problemset/problem/1030/G)

 **評価:** 2900
 **タグ:** 数論
 **解決時間:** 2 分 31 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 私たちには、進化するシステムが与えられています。$n$離散ステップ上の - 次元ベクトル。 各座標は、同じ種類のルール、つまり素数を法とする線形変換を使用して独立して進化します。 

インデックスごとに$i$、値$f_i^{(k)}$以前の値のみに依存します$f_i^{(k-1)}$アフィン関数モジュロを介して$p_i$。 タプル全体のシーケンスを作成することを目的として、これらのアフィン関数の初期ベクトルとすべての係数を自由に選択できます。$(f_1^{(k)}, \dots, f_n^{(k)})$最終的に繰り返される前に、できるだけ多くの異なる状態を生成します。 

タスクは、そのようなシーケンスに出現できる個別のタプルの最大可能数を決定することです。 

重要な制約は、$n$まで可能です$2 \cdot 10^5$したがって、どのような解も基本的に線形である必要があります。$n$。 それぞれ$p_i$までの素数です$2 \cdot 10^6$これは、値を法的に反復処理することは決してないことを強く示唆しています。$p_i$ですが、代わりに更新ルールの代数構造に依存します。 

単純なシミュレーションでは、選択したパラメータの各座標のサイクルについて推論しようとします。 各座標には次の値があるため、これはすぐに失敗します。$p_i$状態とすべての積$p_i$天文学的に大きいです。 

任意の線形反復が複雑なサイクル構造を生成し、個別に分析する必要があると仮定すると、より微妙な失敗例が現れます。 たとえば、各アフィン関数のサイクル長を計算してそれらを結合しようとしますが、最大サイクル長は両方の関数に依存すると誤って仮定する可能性があります。$a_i$そして$b_i$複雑な方法で。 実際には、素体上の構造は、最大サイクルを強制できるほど十分に剛直です。 

最も難しいのは、固定発電機を分析しているわけではないことを認識することです。 軌道サイズを最大化するために発電機を選択しています。 

## アプローチ

 各座標はマップの下で独立して進化します$$x \mapsto a_i x + b_i \pmod{p_i}$$素数サイズの有限体上で。 したがって、各座標は一連のサイズの関数です$p_i$、そして完全なシステムは独立した動的システムの産物です。 

ブルートフォースメンタルモデルは、パラメータを選択し、誘発されるサイクル長をシミュレートすることです。 座標ごとに、次のすべての選択肢を列挙できます。$a_i, b_i, x_i$、繰り返しまでシミュレーションし、軌道サイズを計算してから、それらを組み合わせてみます。 1 つの座標でもすでに$O(p_i)$状態、パラメータの選択肢の数は$O(p_i^2)$, したがって、総作業量はすぐに爆発します。 

重要な観察は、素体上のアフィン変換の可能な最大軌道サイズは複雑ではないということです。 マップは順列であるかどうかのいずれかであり、順列である場合、そのサイクル分解は単一サイクルのサイズにすることができます。$p_i$。 

素数フィールド上で、形式の任意の関数$x \mapsto x + c$と$c \neq 0$はすべての要素の循環シフトです。 繰り返してすべてを調べます$p_i$繰り返す前の値。 一連のサイズのシステムがないため、これにより、単一の座標に対して可能な最大期間がすでに得られます。$p_i$より長いサイクルを持つことができます$p_i$。 

座標は独立して進化するため、個別のタプルの総数は個々のサイクル長の積に等しくなります。 各座標を独立して最大化すると、積が最大化されます。 

したがって、最適な戦略は、すべての座標に正確に周期を持たせることです。$p_i$、答えはすべての素数の積になります。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | パラメータごとのブルート フォース サイクル推論 | 指数関数 | お(1) | 遅すぎる |
 | 素数観測の最適積 | O(n) | お(1) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. 各座標が素数を法とする関数の下で独立して進化することを観察します。$p_i$。 これは、完全なタプル進化が独立した 1 次元動的システムのデカルト積であることを意味します。 
2. 固定座標の場合$i$、状態空間が正確に持っていることを認識します。$p_i$要素。 決定論的関数の繰り返し適用によって生成されるシーケンスは、最終的には繰り返される必要があるため、軌道の長さは最大で$p_i$。 これにより、座標ごとの絶対的な上限が得られます。 
3. この上限が達成可能であることを示します。 選択する$a_i = 1$そして$b_i = 1$変革を起こす$x \mapsto x + 1 \pmod{p_i}$。 各アプリケーションは、すべての残基を訪問するまで新しい値に移動し、その後最初に戻ります。 これにより長さのサイクルが生成されます$p_i$。 
4. 座標の最大可能軌道長は次のとおりであると結論付ける$i$まさに$p_i$なぜなら、それは状態の数を超えることができず、この構造はそれを達成するからです。 
5. 座標は独立しているため、タプルはすべての座標が同時に繰り返される場合にのみ繰り返されます。 したがって、全周期は独立したサイクルの最も一般的ではない同期であり、ここでは正確にそれらの長さの積です。 
6. すべてを乗算します$p_i$モジュロで一緒に$10^9+7$。 

### なぜ効果があるのか

 完全な状態空間の進化は、座標ごとに 1 つの独立した有限サイクルの結果です。 各座標サイクルは、長さに合わせて個別に最大化できます。$p_i$、そしてどの座標もその状態空間のサイズを超えることはできません。 独立性により、すべての座標が同時に開始位置に戻るときにタプルの繰り返しが正確に発生することが保証されるため、個別のタプルの総数はサイクル長の積に等しくなります。 これにより、上限とその達成可能性の両方が確立されます。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

MOD = 10**9 + 7

def main():
    n = int(input())
    p = list(map(int, input().split()))
    
    ans = 1
    for x in p:
        ans = (ans * x) % MOD
    
    print(ans)

if __name__ == "__main__":
    main()
```実装では、派生した結果が直接適用されます。 維持される唯一の状態は、すべての素数の剰余の実行積です。$10^9+7$。 座標間には隠れた依存関係がないため、それ以上の処理は必要ありません。 

ここでよくある落とし穴は、再発を複雑にしすぎて分析しようとすることです。$a_i, b_i$組み合わせ。 最適な構築では、完全なサイクルを保証するパラメーターを選択することで、これらすべてを回避します。 

## 実用的な例

 ### サンプル 1

 入力:```
4
2 3 5 7
```積を段階的に計算します。 

| ステップ | プライム | ランニング製品 |
 | --- | --- | --- |
 | 1 | 2 | 2 |
 | 2 | 3 | 6 |
 | 3 | 5 | 30 |
 | 4 | 7 | 210 |

 結果は 210 で、これは長さ 2、3、5、および 7 の完全なサイクルを独立して達成することに対応します。 

### サンプル 2

 次のことを考慮してください。```
3
3 3 5
```| ステップ | プライム | ランニング製品 |
 | --- | --- | --- |
 | 1 | 3 | 3 |
 | 2 | 3 | 9 |
 | 3 | 5 | 45 |

 答えは 45 です。各座標はその素数を法とするすべての残基を独立して循環し、タプルは完全なデカルト積を循環します。 

これにより、座標間の乗法独立性がグローバル サイクルを正しくモデル化していることが確認されます。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 |$O(n)$| 素数ごとに 1 つの乗算 |
 | スペース |$O(1)$| 稼働中の製品のみが保管されます |

 制約により、最大で$2 \cdot 10^5$素数なので、単一の線形パスで十分です。 の値$p_i$Python でオーバーフローを心配することなく、モジュロ演算で安全に乗算できるほど十分に小さいです。 

## テストケース```python
import sys, io

MOD = 10**9 + 7

def solve():
    input = sys.stdin.readline
    n = int(input())
    p = list(map(int, input().split()))
    ans = 1
    for x in p:
        ans = (ans * x) % MOD
    print(ans)

def run(inp: str) -> str:
    old_stdin = sys.stdin
    sys.stdin = io.StringIO(inp)
    old_stdout = sys.stdout
    sys.stdout = io.StringIO()
    solve()
    out = sys.stdout.getvalue()
    sys.stdin = old_stdin
    sys.stdout = old_stdout
    return out.strip()

# provided sample
assert run("4\n2 3 5 7\n") == "210"

# minimum size
assert run("1\n2\n") == "2"

# repeated primes
assert run("3\n3 3 3\n") == str((3*3*3) % MOD)

# mixed primes
assert run("2\n2 7\n") == str((2*7) % MOD)

# larger case
assert run("5\n2 3 5 7 11\n") == str((2*3*5*7*11) % MOD)
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 | 単一素数 | 2 | 最小境界 |
 | 繰り返される素数 | 27 | 同一のモジュール間での独立性 |
 | 混合素数 | 14 | 基本的な掛け算の正確さ |
 | 5 つの素数 | 2310 | より大きな製品の動作 |

 ## 特殊なケース

 微妙なエッジケースは、すべてが$p_i$この場合、各座標には 2 つの状態しかなく、アフィン選択間の相互作用によってサイクルが短縮されるように見えるかもしれません。 ただし、使用すると$x \mapsto x + 1 \bmod 2$座標ごとに 2 サイクルが保証されるため、完全なタプル空間にはまだサイズがあります$2^n$、アルゴリズムは積を正しく返します。$2^n \bmod (10^9+7)$。 

もう一つのケースは次のような場合です。$n = 1$。 システムは、素体上の単一のアフィン マップに縮退します。 最大軌道はまさに$p_1$、アルゴリズムは正しく返します$p_1$これは、完全な巡回シフトが素数サイズの有限体上に存在するという事実と一致します。
