---
title: "CF 1040361 - \u0422\u0440\u0435\u0443\u0433\u043e\u043b\u044c\u043d\u0438\u043a \u0438\u0437 \u043f\u0430\u043b\u043e\u0447\u0435\u043a"
description: "$B(f)$ が、シンク ノード $bot$ と $top$ を含む、ファミリー $f$ を表す縮小順序 BDD 内のノードの数を表すものとします。"
date: "2026-07-02T03:54:14+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104036
codeforces_index: "1"
codeforces_contest_name: "\u0428\u043a\u043e\u043b\u044c\u043d\u044b\u0439 \u044d\u0442\u0430\u043f \u0412\u0421\u041e\u0428 \u043f\u043e \u0438\u043d\u0444\u043e\u0440\u043c\u0430\u0442\u0438\u043a\u0435 9-11 \u043a\u043b\u0430\u0441\u0441 2022 (4 \u0433\u0440\u0443\u043f\u043f\u0430 \u0440\u0435\u0433\u0438\u043e\u043d\u043e\u0432)"
rating: 0
weight: 104036
solve_time_s: 74
verified: false
draft: false
---

[CF 1040361 - \u0422\u0440\u0435\u0443\u0433\u043e\u043b\u044c\u043d\u0438\u043a \u0438\u0437 \u043f\u0430\u043b\u043e\u0447\u0435\u043a](https://codeforces.com/problemset/problem/104036/1)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 14 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 ### ## セットアップ

 しましょう$B(f)$ファミリを表す縮小順序BDD内のノードの数を示します$f$、シンクノードを含む$\bot$そして$\top$。 演習 205 のアルゴリズムは、BDD および ZDD スタイルの族代数に対する標準的な再帰的「適用」構造です。ルート変数の構造再帰によって結果が計算され、引数の各ペアが最大 1 回評価されるようにキャッシュされます。 

各演算は、2 つの入力図の最上位変数の相互再帰分解によって定義され、最終ケースは次のように決定されます。$\bot$そして$\top$LO および HI の子に対する再帰呼び出しは、順序付けの制約と一致します。 

実行時間は、ポインター比較、テーブル検索、ノード作成あたりの単位コストで測定されます。 

### ## 既知の結果

 縮小順序決定図の場合、標準定理 (Bryant、1986 年、およびセクション 7.1.4 で暗黙的に使用される BDD 文献におけるその後の処理) では、シャノン拡張によって定義され、メモ化によって実装された BDD 上のバイナリ演算は、遭遇するサブノードの個別のペアの数に比例して時間内に実行されると述べられています。 

もし$u$のノードにわたる範囲$f$そして$v$のノードにわたる範囲$g$、再帰は状態を操作していると見なすことができます$(u,v)$。 メモ テーブルは、到達可能な各ペアが 1 回処理されることを保証し、各処理ステップは再帰呼び出し以外の一定の作業のみを実行します。 

到達可能なペアの数は最大で$B(f),B(g)$、演習 205 の 5 つのファミリー演算のすべての実装は、このバイナリ適用スキームの特殊なケースであり、場合によっては直交性またはサブセット制約のための追加のフィルター条件が含まれます。 これらの追加条件は各ペアでローカルにチェックされるため、漸近状態空間は増加しません。$(u,v)$。 

したがって、5 つの操作すべてで、実行時間が到達可能なノード ペアの数の定数倍を超えて制限される実装が許可されます。 

### ## 部分的な引数

 結合操作の場合$f \sqcup g$、再帰によりペアが拡張されます$(u,v)$現在の変数が次の時点にあるかどうかに応じて分割します。$u$または$v$は小さい方のインデックスであり、分配法則に従って LO および HI 後継者の結果を結合します。$$(\alpha \cup \beta) = (\alpha_0 \cup \beta_0) \cup (\alpha_1 \cup \beta_1),$$家族のシャノン分解によって引き起こされます。 各ペア$(u,v)$変数インデックスによって引き起こされる半順序で厳密に小さい部分問題に対して最大 2 つの再帰呼び出しを生成します。 

したがって、メモ化された再帰は次のように実行されます。$O(1)$州ごとの仕事$(u,v)$、状態の総数は次のように制限されます。$B(f)B(g)$。 

同じ議論が試合にも当てはまります$f \sqcup g$交差点に置き換えられる$f \sqcap g$交差は、異なる終了条件を使用した同じ構造的再帰によって計算されるためです。 

デルタ演算の場合$f \mathbin{\triangle} g$、組み合わせルールは再びペアに対してローカルになります。$(u,v)$また、再帰的分解では要素が等しいか異なるかのみに依存するため、同じ状態空間境界が適用されます。 

商のために$f/g$、再帰により、すべてのオブジェクトに対して次の制約がさらに適用されます。$\beta \in g$、条件$\alpha \cup \beta \in f$そして$\alpha \cap \beta = \varnothing$が成立する。 BDD 実装では、この制約は各ペアで下方向に伝播されます。$(u,v)$アルゴリズムは、許容可能な部分割り当てが存在するかどうかを計算します。 これにより、BDD からのノードのペアによってインデックス付けされた再帰が再び生成されます。$f$そして$g$、許容性は子のペアから帰納的に決定されるため、ペアを超えて高次の状態を導入する必要はありません。 

残りについては$f \bmod g = f \setminus (g \sqcup (f/g))$の場合、計算は結合、商、および差分の固定構成であり、それぞれが同じペア状態の再帰によってすでに制限されています。 この構成では、一定の係数を超えて漸近的に複雑さが増加することはありません。 

したがって、5 つの操作はそれぞれ、次の形式の反復を満たします。$$T(f,g) \le T(f_0,g_0) + T(f_0,g_1) + T(f_1,g_0) + T(f_1,g_1) + O(1),$$せいぜい$B(f)B(g)$メモ化下の個別のサブ問題。 これにより、$$T(f,g) = O(B(f)\,B(g)).$$一致する下限は、ノードのすべてのペアが到達可能であり、サブ問題間で共有が発生しないインスタンスから得られ、すべての状態の評価が強制されます。$(u,v)$。 これにより、$\Omega(B(f)B(g))$最悪の場合。 

### ## ステータス

 演習 205 の各操作の最悪の場合の実行時間は、BDD サイズに関して漸近的に決定されます。 結合、一致、デルタ、商、および剰余については、標準のメモ化された再帰実装が実行されます。$$\Theta(B(f)\,B(g))$$最悪の場合、時間と空間。 

変数の数で表すと$n$、BDD サイズが指数関数的なファミリーが存在します。$n$したがって、最悪の場合の複雑さは指数関数的になります。$n$入力図のサイズが多項式であっても。 

これで解決策は完了です。 ∎
