---
title: "CF 103584B - ホワイトグースフット"
description: "$a{n-1}dots a1a0$ を $sum{j=0}^{n-1} aj=t$ のバイナリ文字列とし、$1le jle n-1$ に対して $bj=ajoplus a{j-1}$ を定義します。 エネルギーは $r=sum{j=1}^{n-1} bj.$ です。$ajne a{j-1}$ のとき、それぞれの $bj=1$ は正確なので、$r$ はシーケンス $a0,a1,dots,a{n-1}$ 内の遷移の数に等しくなります。"
date: "2026-07-03T03:06:33+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103584
codeforces_index: "B"
codeforces_contest_name: "UTPC Contest 02-25-22 Div. 2 (Beginner)"
rating: 0
weight: 103584
solve_time_s: 66
verified: false
draft: false
---

[CF 103584B - ホワイトグースフット](https://codeforces.com/problemset/problem/103584/B)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 6 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 しましょう$a_{n-1}\dots a_1a_0$バイナリ文字列であること$\sum_{j=0}^{n-1} a_j=t$そして定義します$b_j=a_j\oplus a_{j-1}$のために$1\le j\le n-1$。 エネルギーは$r=\sum_{j=1}^{n-1} b_j.$それぞれ$b_j=1$まさにいつ$a_j\ne a_{j-1}$、 それで$r$シーケンス内の遷移の数に等しい$a_0,a_1,\dots,a_{n-1}$。 したがって、文字列は次のように一意に分解されます。$r+1$最大の定数実行。 

これらの実行を左から右に次のように書きます。$u_1,u_2,\dots,u_{r+1},$どこ$u_i\ge 1$そして$u_1+u_2+\cdots+u_{r+1}=n.$最初のビットを次のようにします$\varepsilon\in{0,1}$。 ランは交互に行われるので、ランします$i$ビットで構成されています$\varepsilon \oplus (i-1)\bmod 2.$重みの制約$\sum_{j=0}^{n-1} a_j=t$はランレングスに関して線形条件になります。 インデックスセットを定義する$I_1(\varepsilon)=\{\,i\mid 1\le i\le r+1,\ \varepsilon\oplus(i-1)\equiv 1 \pmod 2\,\}.$それから$\sum_{i\in I_1(\varepsilon)} u_i=t.$したがって、すべての構成は初期ビットの選択によって一意に決定されます。$\varepsilon$そして作曲$(u_1,\dots,u_{r+1})$の$n$上記の線形制約を満たす正の部分に変換します。 

辞書編集的な生成の場合、構成を標準の差分表現で置き換えると便利です。 定義する$p_0=u_1,\quad p_i=u_{i+1}-u_i\ \ (1\le i\le r),$となることによって$u_k=p_0+p_1+\cdots+p_{k-1}\quad (1\le k\le r+1).$条件$u_i\ge 1$に翻訳する$p_0\ge 1,\qquad p_0+p_1+\cdots+p_k\ge k+1\ \ (0\le k\le r).$総和条件は、$n=\sum_{k=0}^{r} (r+1-k)p_k + \sum_{k=1}^{r} k p_k,$これは、合成と非負の増分シーケンスの間の標準的な全単射に単純化されます。 特に、それぞれの許容される$(u_1,\dots,u_{r+1})$の構成によって一意に表現されます。$n$の中へ$r+1$ポジティブな部分。 

したがって、生成は、次のすべての構成を生成することになります。$n$の中へ$r+1$ポジティブパーツと重量制約のテスト。 

アルゴリズム的に、$(u_1,\dots,u_{r+1})$標準的な構成生成と同様に、辞書編集順に生成されます (同等に、の位置を反復することによって)。$r$間の区切り文字$n-1$ギャップ）。 生成された各構成について、実行のパリティによる重み寄与を計算します。 

直接的な辞書編集は次のように行われます。 維持する$u_1,\dots,u_{r+1}$と$u_i\ge 1$そして$\sum u_i=n$。 各ステップで、右端のインデックスを増やします$j$そのような$u_j$プラスの部分で完成させることを可能にしながら、増やすことができます。$u_j < n - \sum_{i<j} u_i - (r+1-j).$増やした後$u_j$、 セット$u_{j+1}=\cdots=u_{r+1}=1.$これにより、すべての構成が辞書順に 1 回だけ生成されます。 

生成された各組成について、計算します。$t'=\sum_{i\in I_1(\varepsilon)} u_i.$構成は次の場合に正確に受け入れられます。$t'=t$。 

正しさを正当化するために、エネルギーを持つすべてのバイナリ文字列$r$独自の実行分解を生成します$(u_1,\dots,u_{r+1})$および固有の初期ビット$\varepsilon$したがって、関連する組成が生成され、重み方程式を満たす場合にのみ、この手順によって生成されます。 逆に、生成されたすべてのペア$(\varepsilon,(u_1,\dots,u_{r+1}))$実行構造が正確に一致する一意のバイナリ文字列を定義します。$r$トランジションと体重$t$交互代入と合計制約の構築によって。 

これにより、有効な構成と受け入れられた生成されたペアの間の全単射が確立されるため、アルゴリズムは必要な文字列のみをすべて生成します。 

これで証明は完了です。 ∎
