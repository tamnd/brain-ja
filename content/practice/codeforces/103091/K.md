---
title: "CF 103091K - ビー玉"
description: "セクション 7.2.1.3 の関数 $tau(x)$ は、$0 le x le 1$ に対して $$tau(x) = sum{k=1}^{infty} int{0}^{x} rk(t),dt, qquad rk(t) = (-1)^{lfloor 2^k t rfloor}.$$ によって $0 le x le 1$ 上で定義された高木関数です。 $$L(r) = {x in [0,1] : tau(x) = r} を設定します。"
date: "2026-07-03T23:15:22+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103091
codeforces_index: "K"
codeforces_contest_name: "Stanford ProCo 2021"
rating: 0
weight: 103091
solve_time_s: 152
verified: false
draft: false
---

[CF 103091K - ビー玉](https://codeforces.com/problemset/problem/103091/K)

 **評価:** -
 **タグ:** -
 **解決時間:** 2 分 32 秒
 **確認済み:** いいえ

 ## 解決策
 ## セットアップ

 機能$\tau(x)$セクション 7.2.1.3 は、上で定義されたTakagi 関数です。$0 \le x \le 1$による$$\tau(x) = \sum_{k=1}^{\infty} \int_{0}^{x} r_k(t)\,dt,
\qquad r_k(t) = (-1)^{\lfloor 2^k t \rfloor}.$$実数ごとに$r$、レベルセットを定義します$$L(r) = \{x \in [0,1] : \tau(x) = r\}.$$問題は集合を求めます$R$すべての有理数の$r$そのような$L(r)$は数えられない、そして$\tau(x) \in R$いつでも$x$不合理であり、$\tau(x)$合理的です。 

機能は継続的です$[0,1]$、どこにも微分可能ではなく、二項区間で区分的に線形であり、バイナリ スケーリングによって自己相似性が引き起こされます。 の構造$L(r)$は、この自己相似性の下でのバイナリ展開の分岐によって支配されます。 

## 既知の結果

 高木関数は、同等の形式で広範囲に研究されています。$$\tau(x) = \sum_{k \ge 1} 2^{-k} \, \mathrm{dist}(2^k x, \mathbb{Z}),$$これは、セクション 7.2.1.3 の積分表現から、Rademacher 関数の初等積分によって取得されます。 

知られているのは、$\tau$は連続的であり、微分可能ではありません$[0,1]$であり、二値分解によって暗示される関数方程式を満たします。$$\tau(x) = \frac{1}{2}\tau(2x) + x \quad (0 \le x \le \tfrac12),$$そして対称関係$[\tfrac12,1]$から得た$\tau(x)=\tau(1-x)$。 

の範囲$\tau$はコンパクトな間隔であり、古典的には次のように知られています。$[0,\tfrac23]$。 Every level set$L(r)$は閉じており、その基数は次のように大きく変化する可能性があります。$r$。 

後の著者による結果 (TAOCP では利用できないが、高木関数の最新の解析では標準) では、レベル セットが有限、可算無限、および不可算の 3 つの領域を示し、大規模で構造的に豊富な集合に対して不可算レベル セットが発生することが示されています。$r$。 特に、Takagi 関数のレベルセットに関する Allaart と Kawasaki の研究は、無数のレベルセットが、$\tau$。 

ただし、パラメータセットの完全な特徴付けは、$$R = \{r \in \mathbb{Q} : |L(r)| \text{ is uncountable}\}$$閉じた算術形式または組み合わせ形式は不明です。 この分類は、バイナリ展開の微妙な組み合わせ論に依存しており、Rademacher 構造を貫いているため、そのようなすべての有理値の単純な算術記述は利用できません。 

## 部分的な引数

 の自己類似性$\tau$あらゆるものに対して$x \in [0,1]$、値$\tau(x)$の最初の 2 進数に従って分解できます。$x$。 書き込み$x = \tfrac{b_1}{2} + \tfrac{y}{2}$と$b_1 \in {0,1}$、関数方程式は分割されます$\tau(x)$のアフィン変換に$\tau(y)$線形補正を加えます。 この構築を繰り返すと、任意の値のプリイメージのバイナリ ツリーが生成されます。$r$。 

ポイント$x$数え切れないほどの繊維がある$L(\tau(x))$この二分木が有限または可算多くの枝に折りたたまれなかった場合に限ります。 これには、両方の二者択一 ($b_k=0$そして$b_k=1$) 制約との互換性を維持する$\tau(x)=r$。 その場合、プリイメージには、レベルの無限のサブシーケンスに沿った独立した選択によって生成されたカントール タイプのサブセットが含まれます。 

このメカニズムは、なぜ不可算性が二項再帰定義における永続的な縮退と結びついているのかを説明します。$\tau$。 また、許容点の 2 進拡張が無限に多くの独立した「中立」位置を許容する場合には、両方の拡張が同じ累積された高木和を保持する場合には常に、数えられないレベルのセットが発生することも示しています。 

有理値の場合$r$、許容可能なバイナリ展開の再帰は、自明ではない方法で最終的な周期性制約と相互作用します。 ある程度合理的$r$最終的には無限の分岐を許容し、数え切れないほどの結果を生成する周期的な記号制約に対応します。$L(r)$。 その他の合理的$r$再帰ツリーの最終的な安定化を強制し、多くても数えられるファイバーを生成します。 この区別は、次の二項表現におけるキャリー伝播の微細な構造に依存します。$r$高木の関数方程式の下で。 

同様の妨害が 2 番目の質問にも当てはまります。 もし$x$不合理であり、$\tau(x)$は有理数であり、 のバイナリ展開$x$は非周期的ですが、対応するレベルが設定されているかどうかは決まりません。$L(\tau(x))$カントールのサブセットを含める必要があります。 の合理性$\tau(x)$Rademacher の合計にグローバルな線形制約を課しますが、これらの制約はすべてのプリイメージの分岐構造を直接制御しません。 一般的な意味の強制はありません$\tau(x) \in R$知られています。 

## ステータス

 完全な説明$R$は知られていない。 既存の結果は、広範なクラスの特徴を示しています$r$有限、可算、または不可算のレベル セットを使用しますが、不可算条件を有理数上の単純な算術条件に還元しません。$r$。 どの有理値が不可算レベルセットを生成するかを正確に特定するという問題は、バイナリ展開とキャリーの TAOCP レベルの組み合わせ解析に関連する意味で未解決のままです。 

意味合い$$x \notin \mathbb{Q}, \ \tau(x) \in \mathbb{Q} \quad \Longrightarrow \quad \tau(x) \in R$$は一般に確立されておらず、二値展開に関する追加の仮説がなければ普遍的に成り立つか失敗するかは知られていません。$x$あるいはその高木軌道の象徴的な力学。 

これで解決策は完了です。 ∎
