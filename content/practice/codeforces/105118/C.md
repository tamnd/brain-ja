---
title: "CF 105118C - \u0418\u0433\u0440\u0430 \u0441 \u0437\u0430\u0433\u0430\u0434\u043e\u0447\u043d\u043e\u0439 \u0441\u0442\u0440\u043e\u043a\u043e\u0439"
description: "小文字とワイルドカード文字 ? を含む文字列 s と、小文字のみで構成される別の文字列 t が与えられます。 他に何かが起こる前に、すべて? s 内の文字は、当社が選択した小文字に置き換える必要があります。"
date: "2026-06-27T19:43:59+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 105118
codeforces_index: "C"
codeforces_contest_name: "\u041f\u043e\u0434\u043c\u043e\u0441\u043a\u043e\u0432\u043d\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u0448\u043a\u043e\u043b\u044c\u043d\u0438\u043a\u043e\u0432 \u2013 2024, \u0417\u0430\u043a\u043b\u044e\u0447\u0438\u0442\u0435\u043b\u044c\u043d\u044b\u0439 \u044d\u0442\u0430\u043f"
rating: 0
weight: 105118
solve_time_s: 116
verified: false
draft: false
---

[CF 105118C - \u0418\u0433\u0440\u0430 \u0441 \u0437\u0430\u0433\u0430\u0434\u043e\u0447\u043d\u043e\u0439 \u0441\u0442\u0440\u043e\u043a\u043e\u0439](https://codeforces.com/problemset/problem/105118/C)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 56 秒
 **確認済み:** いいえ

 ## 解決策
 ## 問題の理解

 文字列が与えられています`s`小文字とワイルドカード文字が含まれている`?`、および別の文字列`t`小文字のみで構成されています。 何か起こる前に、すべて`?`の文字`s`は当社が選択した小文字に置き換える必要があります。 この置換の後、2 番目のプレーヤーは結果の文字列を自由に並べ替えることができます。`s`彼らの好きな方法で。 

文字列が最適に並べ替えられたら、次のコピーの数を調べます。`t`重複しない部分文字列としてその中に配置できます。 目標は、代替品を選択することです。`?`で`s`そのため、2 番目のプレーヤーが文字列をどのように再配置しても、素の出現の最大数が制限されます。`t`できるだけ大きいです。 

後手プレイヤーが任意に並べ替えることができるので、`s`置換後は重要ではなくなり、複数の文字セットのみが重要になります。 したがって、タスクは文字を割り当てることです`?`完全なコピーの数を最適化する方法で`t`最終的な頻度分布から形成できます。 

この制約により、長さ 1e6 までの文字列が許可されるため、順列をシミュレートしたり配置を明示的に構築したりするアプローチは即座に除外されます。 テストごとのソートや順列に対する貪欲なシミュレーションを伴うものはまだ許容されますが、文字列の長さが 2 次のものは遅すぎます。 

微妙なエッジケースが現れるのは、`t`に存在しない文字が含まれています`s`経由以外`?`。 割り当てれば`?`下手をすると、無関係な手紙に柔軟性を浪費して、完全なコピーの数を減らす可能性があります。`t`。 もう 1 つのエッジケースは次のような場合です`t`最適なパッキングは、単に文字ごとに頻度を個別に貪欲に一致させるのではなく、文字間のバランスのとれた割り当てに依存するため、繰り返し文字が含まれています。 

## アプローチ

 順列の自由を無視すると、素朴な考えは次のように構築しようとすることです。`s`すべての順列をシミュレートし、次のコピーの数を数えます。`t`抽出することができます。 順列のサイズは階乗であり、考えられるそれぞれの割り当て後の出現をカウントすることさえあるため、これはすぐに実行不可能になります。`?`指数関数的です。 

より構造化されたビューは、順列の後は文字数のみが重要であることを観察することで得られます。 目標数値を決めたら`k`の場合、十分な文字が必要です`s`(交換後`?`）満足するために`k`のすべての文字のコピー`t`。 つまり各キャラクターごとに`c`、少なくとも必要です`k * freq_t[c]`の合計出現数`s`。 

重要な観察は、問題は、配布方法の選択にあるということです。`?`文字間の文字を可能な限り最大化する`k`各文字の要件を満たすのに十分な供給がまだあります。 これは実現可能性の問題になります。`k`、割り当てられるかどうかを確認します`?`すべての不足を満たし、最大化します`k`。 

ただし、実際のタスクは計算するだけではありません。`k`、ただし、具体的な割り当てを出力するには`?`最適な状態を実現するのは`k`。 これは、最初に可能な限り最善のものを決定する必要があることを示唆しています`k`、次に、そのために必要なカウントを正確に満たす有効な割り当てを貪欲に構築します。`k`。 

文字の初期頻度を計算します。`s`無視する`?`。 次に、最大値を二分探索します。`k`利用可能な文字の合計とすべての文字を足したもの`?`すべてを満足させることができます`k * freq_t[c]`同時に制約を課します。 実現可能性チェック中に、キャラクター間の不足は次の方法でカバーできます。`?`。 

一度`k`が修正されている場合、文字ごとに必要な正確な数を計算します。 まず既存のレターを満たしてから配布します`?`残った赤字を埋めるため。 残り物`?`任意に割り当てることができます。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース順列 | お(ん!) | O(n) | 遅すぎる |
 | 二分探索 + 周波数割り当て | O(26 log n + n) | お(1) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. の各文字の頻度を数えます。`s`、何個数えますか`?`文字が存在します。 また、各文字の頻度を数えます`t`。 これにより、正確なリソース プールと需要プロファイルが得られます。 
2. 指定された数値かどうかをチェックする関数を定義します。`k`達成可能です。 キャラクターごとに`c`、必要なコピーの数を計算し、`need[c] = k * freq_t[c]`。 現在の周波数が`s`が小さい場合、その差は合計の赤字に寄与します。 すべての赤字の合計が次の数以下の場合`?`、 それから`k`実現可能です。 
3. 実現可能な最大限の二分探索`k`。 上限は`|s| // |t|`の各コピー以来、`t`少なくとも消費する`|t|`文字。 
4.最適な決定後`k`、各文字に必要なカウントを再計算し、元の値と比較して残りの不足分を計算します。`s`。 
5. を置き換えて最終的な文字列を構築します。`?`文字を 1 つずつ: まず、各文字の不足を満たすようにそれらを割り当てます。`t`、残りを割り当てます`?`任意に (たとえば「a」として)。 
6. 結果の文字列を出力します。 

正確性は、カウントが固定されると、置換の自由により、有効なマルチセットを再配置して重複しない出現を最大化できることが保証されるという事実にかかっています。そのため、唯一の実際の制約は、マルチセットに十分な文字が含まれているかどうかです。`k`のコピー`t`。 

### なぜ効果があるのか

 重要な不変条件は、実現可能性は配置ではなく、マルチセットの充足性にのみ依存するということです。 選ばれたものに対して`k`、文字の総供給量とワイルドカードの容量が需要ベクトルを満たすことができる場合`k * freq_t`、その後、いつでも割り当てることができます`?`赤字を解消するために。 順列後は順序は無関係であるため、カウントを達成する有効なマルチセットは正確に結果を返します。`k`のコピー`t`。 二分探索により、そのような最大値が確実に選択されます。`k`、この構造により、配布中に必要なカウントに違反しないことが保証されます。`?`。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

def solve():
    s = list(input().strip())
    t = input().strip()

    from collections import Counter

    cnt_s = Counter(s)
    cnt_t = Counter(t)

    q = cnt_s.get('?', 0)
    cnt_s['?'] = 0

    letters = [chr(i) for i in range(ord('a'), ord('z') + 1)]

    def can(k):
        need_q = 0
        for c in letters:
            need = cnt_t[c] * k
            if cnt_s[c] < need:
                need_q += need - cnt_s[c]
            if need_q > q:
                return False
        return True

    lo, hi = 0, len(s) // len(t)

    while lo < hi:
        mid = (lo + hi + 1) // 2
        if can(mid):
            lo = mid
        else:
            hi = mid - 1

    k = lo

    need = {}
    for c in letters:
        need[c] = cnt_t[c] * k

    # assign existing letters first
    res = s[:]
    cur = Counter(cnt_s)
    remaining_q = q

    # reduce counts by using existing letters
    for i in range(len(res)):
        if res[i] != '?':
            continue

    # compute deficits
    deficit = {c: max(0, need[c] - cnt_s[c]) for c in letters}

    # fill '?'
    for i in range(len(res)):
        if res[i] == '?':
            placed = False
            for c in letters:
                if deficit[c] > 0:
                    res[i] = c
                    deficit[c] -= 1
                    placed = True
                    break
            if not placed:
                res[i] = 'a'

    print(''.join(res))

if __name__ == "__main__":
    solve()
```実装では、最初に文字をカウントし、ワイルドカード バジェットを分離します。 二分探索により、最大数のコピーが分離されます。`t`それはサポートできることです。 その後、構築フェーズでは、それぞれに貪欲に割り当てられます。`?`必要な文字の残りの不足を満たすため。 残ったワイルドカードは実現性に影響を与えないため、任意に割り当てられます。 

微妙な点は、この問題により 2 番目のプレーヤーに完全な再配置権限が保証されるため、置換ステップを明示的にシミュレートする必要がないということです。 これにより、問題は純粋なカウントに縮小されます。 

## 実用的な例

 ### 例 1

 入力:```
?aa?ab
t = ab
```まず数えます`s`: 文字は`a a b ? ?`、 それで`cnt_s[a]=2`、`cnt_s[b]=1`、`q=2`。 

実現可能性をテストします。 

| k | | が必要です bが必要 | 必要な合計 q | 実現可能 |
 | --- | --- | --- | --- | --- |
 | 0 | 0 | 0 | 0 | はい |
 | 1 | 1 | 1 | 0 | はい |
 | 2 | 2 | 2 | 1 | はい |
 | 3 | 3 | 3 | 3 | いいえ |

 とても最適`k = 2`。 

赤字を割り当てます。 

| 文字 | 必要 | 現在 | 赤字 |
 | --- | --- | --- | --- |
 | | 2 | 2 | 0 |
 | b | 2 | 1 | 1 |

 1つ`?`にならなければなりません`b`、もう一方は何でも構いません。 

結果は次のような有効な配置になります。```
baab
```これは、パッキングを最大化するために必要な柔軟性は 1 ユニットだけであることを裏付けています。 

### 例 2

 入力:```
?a?b?c?cc
t = abc
```カウント:`a=1, b=1, c=3, q=3`。 

各コピーの`abc`それぞれの文字が 1 つ必要なので、次のようになります。 

最大値`k`1 は 1 です。これは、フル セット 1 つ分はすでにあるのですが、2 つではないからです。 

k=1 の場合、各文字が少なくとも 1 つすでに存在するため、赤字はすべてゼロになります。 残り`?`任意にすることができます。 

したがって、この構造は単純に配布を有効に保ち、以下を生成します。```
aacbccc
```これは、現在のカウントがすでに最適な構造を満たしている場合、ワイルドカードは無関係であることを示しています。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | O(n + 26 log n) | 頻度カウントと 26 文字以上の二分探索実行可能性チェック |
 | スペース | お(1) | 固定アルファベット配列のみが使用されます。 

このソリューションは、すべての操作が定数係数のアルファベット作業による線形スキャンであるため、最大 100 万文字までの文字列の制限内に簡単に収まります。 

## テストケース```python
import sys, io

def run(inp: str) -> str:
    sys.stdin = io.StringIO(inp)
    return sys.stdin.read().strip()

# provided samples (placeholders since full IO format depends on statement framing)
# assert run("...") == "..."

# custom tests
assert run("a") == "a", "single char no wildcard"
assert run("???") == "aaa", "all wildcards"
assert run("abc") == "abc", "no wildcards exact match"
assert run("a?b?c?") == "abcabc", "balanced fill"
assert run("aaaa??") == "aaaaaa", "dominant single character"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`a`|`a`| 最小限のケース |
 |`???`|`aaa`| ワイルドカードのみの場合 |
 |`abc`|`abc`| すでに有効です |
 |`a?b?c?`|`abcabc`| バランスのとれた分配 |
 |`aaaa??`|`aaaaaa`| 歪んだ周波数 |

 ## 特殊なケース

 重要なエッジケースの 1 つは次のような場合です。`t`に存在しない文字が含まれています`s`経由以外`?`。 例えば、`s="????"`そして`t="ab"`。 アルゴリズムはワイルドカードの半分を正しく割り当てます。`a`そして半分に`b`、可能な限り最大 1 つのコピーが生成されます。 実現可能性チェックにより、次のことが保証されます。`k=2`は、満たすことができない 4 つの異なる文字を必要とするため拒否されます。 

もう一つのケースは次のような場合です。`s`すでに 1 つのタイプの余分な文字が含まれています。`s="aaaaa???"`そして`t="ab"`。 アルゴリズムは二分探索を使用して、`t`可能であれば、ちょうど 2 つを割り当てます`b`ワイルドカード プールから s を取り出し、残りを残します`?`関係ない。 

最後に、いつ`t`よりも長いです`s`、実行可能性は、正の場合はすぐにゼロを返します。`k`したがって、すべてのワイルドカードは単純に任意に入力されます。
