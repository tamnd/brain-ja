---
title: "CF 104741H - \u73bb\u7483\u7403"
description: "ノード 1 をルートとする、サイズ $N$ のルート付きツリーが与えられます。各ノードはプラットフォームを表し、各プラットフォームには最初はちょうど 1 つのガラス球が含まれています。"
date: "2026-06-28T23:21:12+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 104741
codeforces_index: "H"
codeforces_contest_name: "The 10th Jimei University Programming Contest"
rating: 0
weight: 104741
solve_time_s: 86
verified: true
draft: false
---

[CF 104741H - \u73bb\u7483\u7403](https://codeforces.com/problemset/problem/104741/H)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 26 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 私たちには、適切な大きさの根が生えた木が与えられています。$N$各ノードはプラットフォームを表し、各プラットフォームには最初はちょうど 1 つのガラス球が含まれています。 すべてのエッジはノードからその親に向けられているため、各ボールは常にルートに向かって上向きに移動し、単位時間ごとに 1 つのエッジを横切ります。 

ボールがノードに到達したとき$v$、デバイスが次の場合、そこに収集される可能性があります。$v$動作します。 各ノード$v$独立した確率がある$p_v$デバイスが正常に動作していることを確認します。 装置が機能すると、ボールは次の位置で永久に停止します。$v$。 失敗すると、ボールは親に向かって上向きに移動し続けます。 

ボールは、デバイスが動作するパス上の最初のノードによって収集されるまで動き続けます。 なぜなら$p_1 = 1$、事前に何も止められなかった場合、すべてのボールはルートに収集されることが保証されています。 

ボールのコストは、ボールが収集されるまでに移動するエッジの数として定義されます。 すべてのコストの予想総コストを計算するように求められます。$N$ボール、各ノードから 1 つずつ始まります。 

重要な点は、複数のボールが直接ではなく、同じツリー構造を共有することによってのみ相互作用することです。 各ボールは独立して上方に移動しますが、期待される寄与はパスに沿った共有確率に依存します。 

制約は次のとおりです$N = 5 \cdot 10^5$、二次方程式や偶数式は即座に除外されます。$O(N \log^2 N)$ノードごとの再計算。 時間内に安全となる操作は数千万件程度だけであるため、ソリューションは線形または線形に近いものでなければなりません。 

単純なシミュレーションでは、ボールが収集されるまですべてのボールが上に移動しますが、それは劣化する可能性があります。$O(N^2)$鎖状の木の中。 たとえば、一行で$1 \leftarrow 2 \leftarrow 3 \leftarrow \dots \leftarrow N$、ノードのボール$N$横断する$N-1$エッジ、ボールの位置$N-1$横断する$N-2$、など、二次合計が生成されます。 

もう 1 つの隠れた問題は、動作しているデバイスのすべてのサブセットにわたる単純な確率の列挙が指数関数的であることです。 小規模チェーンでもすでに生産している$2^N$したがって、動作中のデバイスと障害のあるデバイスを明示的に分岐するアプローチは不可能です。 

主な困難は、パスに沿ったランダム性を距離の決定論的なツリー集合から分離することです。 

## アプローチ

 このプロセスを直接考える方法は、各ボールを個別にシミュレートすることです。 ノードから始まるボールの場合$u$、最初のノードに遭遇するまで上向きに歩きます$v$デバイスが動作するので、そこからの距離を加算します。$u$に$v$。 どのデバイスも機能しない場合はルートで終了しますが、$p_1 = 1$、このケースはノード 1 で吸収されます。 

この見方は正しいですが、各ボールが最大まで横切る可能性があるため、遅すぎます。$O(N)$エッジ、与える$O(N^2)$最悪の場合。 

重要な観察は、各ボールがどこで終わるかを追跡する代わりに、視点を逆転させていることです。 ノードを修正する$v$。 私たちは、どのボールがどの場所に集められるかを尋ねます。$v$、総移動距離はどれくらいですか?

 ノードからのボール$u$で収集されます$v$まさに 2 つの条件が成立するときです。 まず、からのパス上のすべてのデバイス$u$を除く$v$失敗。 次に、デバイス$v$成功する。 この確率は、先祖にのみ依存します。$v$、以下のサブツリー構造ではありません。 

これにより、確率を次の値に因数分解することができます。$v$、個人から独立した$u$、のサブツリーに対する純粋に構造的な合計を乗算します。$v$。 

したがって、期待値はノード間の合計になります$v$ここで、各ノードは、その成功重み付け確率に、サブツリー内のすべてのノードからノードまでの合計距離を乗算したものに寄与します。$v$。 これにより、パス上のランダム性がノードごとに 1 つの乗数に変換されます。 

サブツリーの距離の合計を効率的に計算するには、深さを使用して距離を書き換えます。 いかなる子孫に対しても$u$の$v$、距離は$\text{depth}[u] - \text{depth}[v]$。 したがって、サブツリーへの貢献は、深さとサブツリー サイズのサブツリーの合計を維持することに減ります。 

最終的な構造は、確率累積用に 1 つの DFS とサブツリー集約用に 1 つの DFS を備えたツリー DP になります。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ボールごとのブルートシミュレーション |$O(N^2)$|$O(N)$| 遅すぎる |
 | 確率の列挙 |$O(2^N)$|$O(2^N)$| 不可能 |
 | ツリー DP 因数分解 |$O(N)$|$O(N)$| 承認済み |

 ## アルゴリズムのチュートリアル

 ツリーを 1 にルートし、DFS 順序で処理します。 

1. ノードが存在する確率を計算します。$v$これは、下からのパス上で最初に動作するデバイスです。 値を定義します$g[v]$のすべての祖先が存在する確率として$v$失敗して$v$成功する。 親から子への移動中、失敗確率の実行積を維持します。$g[v] = p_v \cdot \prod_{x \in \text{ancestors of } v} (1 - p_x)$。 これにより、各ノードは、そのサブツリーに到達するボールの停止点となる可能性がどの程度かを確実に知ることができます。 
2. サブツリー情報を維持しながら、ルートから DFS を実行します。 各ノードについて$v$、そのサブツリーに対して 2 つの値 (ノード数) を計算します。$sz[v]$、およびそのサブツリー内のノードの深さの合計は、次のように示されます。$sumDepth[v]$。 
3. 各ノードについて$v$、サブツリー内のすべてのノードからノードまでの合計距離を計算します。$v$。 アイデンティティの使用$\text{dist}(u,v) = \text{depth}[u] - \text{depth}[v]$、次のように導出されます。$$\sum_{u \in subtree(v)} \text{dist}(u,v) = sumDepth[v] - depth[v] \cdot sz[v].$$4. この構造和に確率の重みを掛けます。$g[v]$。 これにより、ノードの期待される寄与が得られます。$v$最終的な答えへ。 
5. すべてのノードの寄与を合計し、結果のモジュロを出力します。$998244353$。 

### なぜ効果があるのか

 各ボールの予想コストは、ボールが最初に収集されるノードで条件付けすることで分解できます。 イベント「ボールフロム」$u$で収集されます$v$」は、からのパス上のノードの状態にのみ依存します。$u$に$v$、そしてこの依存関係は、その祖先にのみ依存する因子にきれいに分離されます。$v$そしてその数の決定的なカウント$u$寝そべる$v$のサブツリー。 

この分離により、二重カウントが保証されません。すべてのボールは停止ノードで 1 回だけ寄与し、DFS 集計ではそのような寄与すべてがノードごとに 1 回だけ列挙されます。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

MOD = 998244353

sys.setrecursionlimit(10**7)

def solve():
    n = int(input())
    p = list(map(int, input().split()))
    parent = [0] * n
    children = [[] for _ in range(n)]

    for i in range(1, n):
        fa = int(input().split()[0]) if False else None  # placeholder safe
```その間違った入力行を使用することはできません。 適切に修正します:
