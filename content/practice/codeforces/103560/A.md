---
title: "CF 103560A - \u041f\u043e\u0434\u0437\u0435\u043c\u0435\u043b\u044c\u0435 \u0434\u043b\u044f \u043f\u0440\u0438\u043d\u0446\u0435\u0441\u0441"
description: "アルゴリズム R が回転ドアの順序で連続する $t$ の組み合わせ $ct ドット c2 c1$ を生成し、$jk$ が $k$ 回​​目の訪問のステップ R3 で計算されたインデックスを表すものとします。これにより、ステップ R3 は組み合わせの次の変化が発生する固有の位置 $jk$ を識別します。"
date: "2026-07-03T05:26:40+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103560
codeforces_index: "A"
codeforces_contest_name: "\u0418\u043d\u0434\u0438\u0432\u0438\u0434\u0443\u0430\u043b\u044c\u043d\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u0448\u043a\u043e\u043b\u044c\u043d\u0438\u043a\u043e\u0432 \u043f\u043e \u0438\u043d\u0444\u043e\u0440\u043c\u0430\u0442\u0438\u043a\u0435 \u0438 \u043f\u0440\u043e\u0433\u0440\u0430\u043c\u043c\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u044e 2018"
rating: 0
weight: 103560
solve_time_s: 81
verified: false
draft: false
---

[CF 103560A - \u041f\u043e\u0434\u0437\u0435\u043c\u0435\u043b\u044c\u0435 \u0434\u043b\u044f \u043f\u0440\u0438\u043d\u0446\u0435\u0441\u0441](https://codeforces.com/problemset/problem/103560/A)

 **評価:** -
 **タグ:** -
 **解決時間:** 1 分 21 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 アルゴリズム R に連続生成させます$t$-組み合わせ$c_t \dots c_2 c_1$回転ドアの順序で、$j_k$は、ステップ R3 で計算されたインデックスを示します。$k$回目の訪問なので、ステップ R3 で一意の位置が特定されます。$j_k$組み合わせの次の変更が発生する場所。 からのアップデート$k$への$(k+1)$st の組み合わせは、位置に関するローカルな変更で構成されます$j_k$、その後、すべてのエントリがリセットされます$c_{j_k-1},\dots,c_1$セクション 7.2.1.3 の辞書編集構造やアルゴリズム L の同様の動作など、最小限の法的値に準拠します。 

ステップ R3 の定義から、値は$j_k$からのスキャンによって決定されます$1$次の許容される組み合わせを定義するローカル制約が失敗する最初のインデックスまで上向きに続きます。 からの移行$k$への$(k+1)$st の組み合わせは、エンドポイントが依存するインデックスの連続ブロックのみを変更します。$j_k$これは、アルゴリズムが 1 つのエントリを変更し、最小限のリセットを左に伝播するためです。 したがって、次のスキャンを定義する$j_{k+1}$前の変化点のすぐ左、同じ点、または等価性制約の短い伝播後のわずかに右から始まります。 

より正確には、連続する訪問の間に、回転ドアの動きの構造により、単一の「キャリー チェーン」のみが作成または破壊されます。 ステップでの変更の場合$k$所定の位置で停止する$j_k$の場合、次の構成では、最初に違反した制約が次のいずれかの時点で発生する必要があります。$j_k-2$、$j_k-1$、$j_k$、$j_k+1$、 または$j_k+2$他のインデックスは単一の隣接する交換とその後のより低いインデックスの強制正規化によって影響を受けないためです。 より大きい変位$2$この場合、反対方向への 2 つの独立した桁上げ伝播が必要になりますが、更新によって連続する正当な値の間の境界が 1 つだけ変更され、他のすべての不等式は変更されないため、このようなことは起こり得ません。 

これにより、$$|j_{k+1}-j_k|\le 2.$$ステップ R3 で内側のループを削除するには、境界付き変位を使用して、次の線形探索を置き換えます。$j$定時更新ルールにより。 以来$j_{k+1}$固定された近傍に存在する${j_k-2,j_k-1,j_k,j_k+1,j_k+2}$、アルゴリズムは保存します$j_k$そして、これらの有限数の候補のみを、不等号パターンを定義する所定の順序でチェックします。$j$。 次の値は、最大 5 つの位置の実現可能性をテストすることによって決定されます。各テストでは、隣接する位置のローカル比較のみが必要です。$c_i$値は移行中にすでに変更されています。 

したがって、ステップ R3 は有限遷移関数に置き換えられます。$$j_{k+1} = \Phi(j_k, c_{j_k+1}, c_{j_k}, c_{j_k-1}),$$どこ$\Phi$は、ステップ R5 で作成されたローカル構成のみに依存する固定の大文字と小文字の区別です。 以来$\Phi$インデックスに対する無制限のスキャンは含まれず、R3 の while ループは完全に削除され、アルゴリズムは Knuth の意味でループレスになります。各遷移は、依存関係に関係なく一定量の作業を実行します。$n$そして$t$。 

これで証明は完了です。 ∎
