---
title: "CF 102443H - プラネット ナイン"
description: "レジスタは a で始まり、b で終わる必要があります。 イベントは2種類のみ。 加算ではレジスタが 9 の正の倍数に増加しますが、削除では先頭の 10 進数の一部が削除され、削除されたすべての桁は 1 でなければなりません。"
date: "2026-08-09T18:09:37+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102443
codeforces_index: "H"
codeforces_contest_name: "2019-2020 Russia Team Open, High School Programming Contest (VKOSHP 19)"
rating: 0
weight: 102443
solve_time_s: 479
verified: true
draft: false
---

[CF 102443H - プラネット ナイン](https://codeforces.com/problemset/problem/102443/H)

 **評価:** -
 **タグ:** -
 **解決時間:** 7 分 59 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 レジスターは次から始まります`a`そして次で終了する必要があります`b`。 イベントは2種類のみ。 加算ではレジスタが 9 の正の倍数に増加しますが、削除では先頭の 10 進数の一部が削除され、削除されたすべての桁は次のようになります。`1`。 出力は、最大 1000 個のそのようなイベントの有効なシーケンスです。 すべての中間レジスタ値は最大 10 18 でなければなりません。 

有用な量は 9 を法とした余りです。9x を加算しても変化することはありません。 削除したら`y`10 進数の先頭の場合、削除された接頭辞は以下と一致します。`y`モジュロ 9 であり、10 のすべてのべき乗も 1 モジュロ 9 と合同です。`y`桁は残りを正確に減算します`y`モジュロ 9。これは、構築の背後にある中心的な算術プロパティです。 

の境界`a`そして`b`十分に小さいため、10 進数表現は最大 10 桁になります。 中間値の制限は 10 18 とはるかに大きいため、かなり多くの桁の数値を一時的に作成する余地があります。 操作の制限は 1000 で、長い操作シーケンスによる検索は除外されますが、以下の構築に必要な操作はわずか数十回です。 

一見合理的に見える構築が失敗する可能性のあるいくつかの特殊なケースがあります。 のために`0 0`、正解は単純に`Stable`に続く`0`、イベントが必要ないためです。 常に正の加算を出力するプログラムでは、レジスタが不必要に変更されてしまいます。 

のために`1 9`、2 つの値は 9 を法とした剰余が異なります。単に 9 を加算するだけでは変更できません。`1`の中へ`9`。 有効な構造の 1 つはサンプルのものです。`+ 2`、変化します`1`に`19`、 に続く`- 1`、先頭を削除します`1`そして葉を残します`9`。 かどうかだけをチェックするプログラム`a <= b`そして追加します`(b-a)/9`このケースは誤って拒否されてしまいます。 

ケース`0 1`も有用な境界です。 1 は 9 の倍数ではないため、直接の加算はありません。先頭を繰り返し作成できます。`1`それを削除し、残りを 1 段階ずつ変更し、最終的に 1 に到達します。`0 -> 18 -> 8 -> 17 -> 7 -> ... -> 11 -> 1`。 一時的な値は最終値よりも大きくなっていますが、それでも 10 18 をはるかに下回っています。 

最後に、次のような値`1000000000`10桁あるので注意が必要です。 先頭に 1 が多すぎる数値を作成する構造は、10 18 の制限を超える可能性があります。 最適な構築では、最大 18 桁の値が 1.12⋅10 17 未満のレピュユニットを意図的に使用します。 

## アプローチ

 ブルート フォース アプローチでは、考えられるすべてのレジスタ値を状態としてモデル化し、すべての状態から両方の操作タイプを試行できます。 加算には可能な引数の範囲が非常に広いため、これはすぐに失敗します。 ゼロからでも、

 ⌊ 9 10 18 ⌋=111111111111111111

 中間値の境界を尊重する可能な正の加算。 操作シーケンスの検索はさらに悪いことに、各ステップで 2 つの固定選択肢のみに人為的に制限した場合、深さ 1000 の検索にはすでに 2 1000 のシーケンスが含まれていることになります。 実際の操作セットははるかに大きいため、ブルート フォースは意味のあるオプションではありません。 

ブルート フォースは、有効なシーケンスが存在する場合には最終的にそれを発見するため、概念的に機能します。 有益な観察は、追加を検索する必要がまったくないということです。 代数的にそれらを選択できます。 

現在の値が`v`、そしてそれを持たせてください`d`10 進数。 定義する

 t=(v−1)mod9。 

ここで考えてみましょう

 T=10d+t。 

番号`T`数字で始まります`1`、そして

 T≡1+t≡v(mod9)。 

その結果`T-v`は 9 の正の倍数です。`T`1 回加算して最初の桁を削除し、正確に取得します。`t`。 したがって、2 つの操作であらゆるものを変換します。`v`9 を法とした剰余が古い剰余より 1 小さい数に変換されます。 

これにより、モジュロ 9 の剰余が一致するまで調整する決定論的な方法が得られます。`b`。 余りが一致し、現在値を超えなくなったら`b`、1 回の加算で到達する`b`。 

複雑な問題が 1 つあります。 もし`a`少なくとも次と同じ大きさです`b`、一時的な剰余調整プロセスでは、より大きな値が通過する可能性があります。`b`、最終的な加算値がマイナスになる可能性があります。 最初に送信することでそれを完全に回避します`a`ゼロに。 これを行うには、10 進数のレピュ単位を選択します

 R L =11…1

 誰の長さ`L`に一致します`a`モジュロ 9 であり、その値が少なくとも`a`。 そんな`L`L≤18 の場合は常に存在します。 R L ≡L(mod9) なので、その差は`R_L-a`は 9 で割り切れます。その差を加算してから、すべてを削除します`L`先頭のもの、ゼロに達するもの。 これはまさに公式社説で述べられている二段階構造である。 

ゼロから、剰余調整プロセスを安全に使用して、次の剰余に達することができます。`b`、残りの 9 の倍数を加算します。イベントの総数は小さいままです。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルートフォース | 演算制限では指数関数的であり、最初の加算は 10 17 を超える可能性があります。 指数 | 遅すぎる |
 | 最適 | O(log 10 最大(a,b)) | 出力の O(log 10 max(a,b)) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. もし`a == b`、出力`Stable`そしてゼロオペレーション。 工事は必要ありません。 
2. もし`a < b`、現在の値を次の値に保ちます。`a`現在の値については、9 を法とした余りを繰り返し調整します。`v`、その桁数を計算します`d`、次に設定します`t = (v - 1) % 9`そして`T = 10^d + t`。 
3.追加`(T-v)/9`ナイン。 結果はまさに`T`、 そして`T`から始まる`1`。 商がプラスになるのは、`T > v`、積分である一方で、`T`そして`v`9を法とした同じ剰余になります。 
4. 先頭の 1 桁を削除します。 その数字は`1`、操作は合法であり、レジスターは`t`。 その剰余は、前の剰余モジュロ 9 より 1 小さくなります。 
5. 現在の値の剰余が 9 と同じになるまで、前の 2 つの手順を繰り返します。`b`。 すべての変換で剰余が 9 を法として 1 つ減少するため、このような変換は最大 9 回必要になります。 
6.追加`(b-v)/9`。 余りが一致するので、商は整数になります。 なぜなら`a < b`、剰余調整フェーズ後の現在の値は最大 8 であるため、以下の値になります。`b`。 したがって、値がすでに等しくない限り、最終的な加算は正になります。 
7. もし`a > b`、最初に repunit を構築します`R`次のような最大 18 桁の数字`R >= a`そして`R % 9 == a % 9`。 このような長さは、1 から 18 までの長さを確認することでわかります。 
8.追加`(R-a)/9`9 以外の場合`R == a`この場合、追加は必要ありません。 レジスターは正確に`R`。 
9. すべて削除`len(R)`先頭の数字。 すべての数字は`1`, したがって、削除は有効であり、レジスタがゼロに変更されます。 
10. ゼロから始めて、同じ剰余調整手順を次のレベルに適用します。`b`、次に一致する剰余からへの最後の加算を実行します。`b`。 

不変条件は単純です。 構築されたすべての加算はレジスタを 9 の倍数で変更するため、現在の剰余が保存されます。 削除するたびに、先頭の 1 つだけが削除されます`1`私たちの構築では、剰余が 9 を法として 1 つ減ります。各削除の前に構築される数値は常に次のようになります。`10^d+t`、最初の桁が次であることが保証されていることを意味します。`1`そして削除は合法です。 余りが等しくなったら`b % 9`、現在値との差`b`は 9 の倍数なので、最終的な足し算は次のようになります。`b`その通り。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

LIMIT = 10**18

def add_to_repunit(a, ops):
    """Transform a to zero using one repunit."""
    if a == 0:
        return 0

    # Find a repunit R >= a with R % 9 == a % 9.
    rep = 0
    chosen = None

    for length in range(1, 19):
        rep = rep * 10 + 1
        if rep >= a and rep % 9 == a % 9:
            chosen = rep
            chosen_len = length
            break

    # Such a repunit always exists for a <= 1e9.
    if chosen is None:
        raise RuntimeError("No suitable repunit")

    if chosen > a:
        x = (chosen - a) // 9
        ops.append(("+", x))

    ops.append(("-", chosen_len))
    return 0

def adjust_residue(cur, target_residue, ops):
    """
    Repeatedly decrease cur's residue modulo 9 until it equals
    target_residue. Each iteration uses +x, -1.
    """
    while cur % 9 != target_residue:
        d = len(str(cur))
        t = (cur - 1) % 9
        target = 10**d + t

        x = (target - cur) // 9
        if x <= 0:
            raise RuntimeError("Invalid positive addition")

        ops.append(("+", x))
        ops.append(("-", 1))

        cur = t

    return cur

def solve():
    a, b = map(int, input().split())

    if a == b:
        print("Stable")
        print(0)
        return

    ops = []

    if a >= b:
        # First reach zero.
        cur = add_to_repunit(a, ops)

        # Then construct b from zero.
        cur = adjust_residue(cur, b % 9, ops)

        if cur < b:
            x = (b - cur) // 9
            ops.append(("+", x))
            cur = b
    else:
        # a < b, so we can directly adjust the residue and then add.
        cur = adjust_residue(a, b % 9, ops)

        if cur < b:
            x = (b - cur) // 9
            ops.append(("+", x))
            cur = b

    assert cur == b
    assert len(ops) <= 1000

    print("Stable")
    print(len(ops))
    for typ, x in ops:
        print(typ, x)

if __name__ == "__main__":
    solve()
```の`add_to_repunit`関数は構造の前半を実装します。`a >= b`。 検索できるリピュニット長は 18 個だけです。 各候補について、9 で割り切れる値は次のようになります。`rep % 9 == a % 9`, したがって、必要な加算数は整数になります。 

の`adjust_residue`関数は、キーの 2 つの操作による遷移を実装します。 のために`cur`と`d`数字、`10**d + t`まさに`d+1`で始まる数字`1`。 加算カウントは演算を実行する前に計算されるため、10 進桁上げ動作への依存が回避されます。 

後`- 1`、新しい値は正確に`t`、から最初の桁を削除するため、`10^d+t`の 10 進数表現を残します`t`という可能性も含めて、`t`はゼロです。 

18 桁の再現単位は 10 18 未満で安全であり、最大 10 桁の入力から生成される他のすべての一時値は最大 10 10 +8 です。 Python の整数には任意の精度もあるため、構築中に算術オーバーフローが発生することはありません。 

操作順序は重要です。 削除は先頭の数字が次の場合にのみ有効であるため、追加は削除の前に行う必要があります。`1`。 The final addition is performed only after the modulo 9 residues agree, guaranteeing that its argument is an integer.

 ## 実用的な例

 ### サンプル 1

 入力用`0 0`、値はすでに一致しています。 

| 現在 | ターゲット | アクション | 新しい流れ |
 | --- | --- | --- | --- |
 | 0 | 0 | なし | 0 |

 アルゴリズムはゼロ操作を即座に出力します。 これは可能な限り最小の構成であり、不必要な正の演算の生成を回避します。 

### サンプル 2

 入力用`1 9`、 我々は持っています`1 < 9`、目標の残りは`0`。 

| 現在 |`current % 9`|`t = (current-1) % 9`| 構築された値 | アクション | 新しい流れ |
 | --- | --- | --- | --- | --- | --- |
 | 1 | 1 | 0 | 10 |`+ 1`、`- 1`| 0 |
 | 0 | 0 | 0 | 0 |`+ 1`| 9 |

 最初のペアは剰余を 1 から 0 に変更します。最後の加算は 0 から 9 に変更します。サンプルではより短い構造が使用されています。`+ 2`、`- 1`ただし、この問題では任意の有効なシーケンスが受け入れられます。 

トレースは、操作数を最小限に抑える必要がない理由を示しています。 構築では追加のイベントがいくつか使用される可能性がありますが、制限の 1000 をはるかに下回ります。 

### 境界の例

 検討してください`0 1`。 

| 現在 |`current % 9`|`t`| 構築された値 | 新しい流れ |
 | --- | --- | --- | --- | --- |
 | 0 | 0 | 8 | 18 | 8 |
 | 8 | 8 | 7 | 17 | 7 |
 | 7 | 7 | 6 | 16 | 6 |
 | 6 | 6 | 5 | 15 | 5 |
 | 5 | 5 | 4 | 14 | 4 |
 | 4 | 4 | 3 | 13 | 3 |
 | 3 | 3 | 2 | 12 | 2 |
 | 2 | 2 | 1 | 11 | 1 |

 必要な剰余は 1 であるため、プロセスは 1 で停止します。最終的な加算は必要ありません。 

この例では、一時的に 2 桁の数値が作成されるものの、ターゲットが非常に小さい場合を演習します。 一時的な最大値は 18 のみです。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | O(logmax(a,b)) | 最大 18 回の反復チェックと最大 9 回の剰余調整ラウンド。それぞれ定数サイズの整数を使用します。 
| スペース | O(logmax(a,b)) | 演算リストには、10 進数の桁数に応じた一定数の演算のみが含まれます。 

この実装では、実際の操作数は最大 21 です。 最初のレピュニット変換では最大 2 つのイベントが使用され、残差調整では最大 18 のイベントが使用され、その後に 1 つの最終加算が続きます。 したがって、1000 回の操作という必要な制限は非常に緩やかです。 すべての一時値は 10 18 未満であるため、この構造はレジスタ境界も満たします。 

## テストケース

 出力は一意ではないため、テストでは、1 つの固定出力と比較するのではなく、生成された操作シーケンスを検証する必要があります。```python
import sys
import io

def run(inp: str) -> str:
    old_stdin = sys.stdin
    old_stdout = sys.stdout

    sys.stdin = io.StringIO(inp)
    sys.stdout = io.StringIO()

    solve()

    out = sys.stdout.getvalue()

    sys.stdin = old_stdin
    sys.stdout = old_stdout
    return out

def verify(inp: str, out: str) -> bool:
    a, b = map(int, inp.split())
    lines = out.strip().splitlines()

    assert lines[0] == "Stable"
    n = int(lines[1])
    assert 0 <= n <= 1000
    assert len(lines) == n + 2

    cur = a

    for line in lines[2:]:
        parts = line.split()
        assert len(parts) == 2

        typ, x = parts
        x = int(x)
        assert x > 0

        if typ == "+":
            cur += 9 * x
            assert cur <= 10**18

        elif typ == "-":
            s = str(cur)
            y = x

            assert 1 <= y <= len(s)
            assert all(ch == "1" for ch in s[:y])

            s = s[y:]
            cur = int(s) if s else 0
            assert cur <= 10**18

        else:
            assert False, "unknown operation"

    assert cur == b
    return True

# Provided sample 1 has an exact canonical output.
assert run("0 0") == "Stable\n0\n", "sample 1"

# Provided sample 2 has many valid outputs, so verify its semantics.
assert verify("1 9", run("1 9")), "sample 2"

# Minimum-size input.
assert verify("0 0", run("0 0")), "minimum values"

# All-equal nonzero values.
assert verify("123456789 123456789", run("123456789 123456789")), "equal values"

# Maximum input values.
assert verify("1000000000 1000000000", run("1000000000 1000000000")), "maximum equal values"

# Large value going down to zero, exercising the repunit construction.
assert verify("1000000000 0", run("1000000000 0")), "repunit boundary"

# Different residues with a very small target.
assert verify("0 1", run("0 1")), "small target"

# Adjacent boundary near 1e9.
assert verify("999999999 1000000000", run("999999999 1000000000")), "large adjacent values"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 |`0 0`|`Stable`、ゼロ操作 | 即時等価性と最小値 |
 |`1 9`| 任意の有効な`Stable`建設 | 提供されたサンプルと残留物の変更 |
 |`123456789 123456789`|`Stable`、ゼロ操作 | ゼロ以外の値が等しい |
 |`1000000000 1000000000`|`Stable`、ゼロ操作 | 最大入力境界 |
 |`1000000000 0`| 任意の有効な`Stable`建設 | Repunit の変換と多くの主要なものの削除 |
 |`0 1`| 任意の有効な`Stable`建設 | 異なるモジュロ 9 残基を持つ小さなターゲット |
 |`999999999 1000000000`| 任意の有効な`Stable`建設 | 大きな隣接値と繰り返しの剰余調整 |

 ## 特殊なケース

 のために`0 0`、等価性チェックは算術構築の前に終了します。 レジスタはゼロのままであるため、正確な出力は次のようになります。`Stable`に続く`0`。 

のために`1 9`差が 9 の倍数ではなく 8 であるため、直接加算は不可能です。アルゴリズムは、まず 10 を構築し、その先頭を削除することにより、剰余を 1 から 0 に変更します。`1`、ゼロを取得します。 次に、9 の倍数を 1 つ加算して 9 に達します。一時的な値 10 が有効です。 

のために`0 1`、アルゴリズムは剰余 0 から始まり、ターゲットには剰余 1 があります。`+x, -1`ペアは、剰余を 9 を法として 1 つ減らします。削除後の値のシーケンスは次のとおりです。`8, 7, 6, 5, 4, 3, 2, 1`, したがって、8ラウンド後に目標残量に到達します。 すべての加算が構成されます`18`、`17`、...、`11`、それぞれ、先頭の部分を削除します。`1`。 

のために`1000000000 0`、アルゴリズムは 10 桁の再現単位を選択します。`1111111111`。 それは次と同じ余りになります`1000000000`、その方が大きいため、その差は 9 で割り切れます。その差を加算すると repunit に達し、10 個すべてが削除されます。`1`数字はゼロを生成します。 一時的な最大値は`1111111111`。 

のために`999999999 1000000000`、初期値の剰余は 0 ですが、ターゲットの剰余は 1 です。剰余調整手順では、最初に 8、次に 7 が生成され、1 まで続きます。そこから、`1000000000`は 9 で割り切れるので、最後の加算が正確にターゲットに達します。 これは、中間レジスタ値が最終ターゲットを下回る必要はなく、必要な 10 18 境界を下回るだけであることを示しています。
