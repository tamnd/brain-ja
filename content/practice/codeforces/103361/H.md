---
title: "CF 103361H - \u041d\u043e\u0436\u043d\u0438\u0446\u044b"
description: "サイズ $m 倍 n$ の四角形が与えられており、固定サイズ $1 倍 k$ のより小さな四角形を使用してそれを完全にタイル化したいと考えています。"
date: "2026-07-03T13:07:14+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103361
codeforces_index: "H"
codeforces_contest_name: "\u041e\u0442\u043a\u0440\u044b\u0442\u0430\u044f \u041a\u043e\u043c\u0430\u043d\u0434\u043d\u0430\u044f \u041e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u042e\u041c\u0428 \u043f\u043e \u043f\u0440\u043e\u0433\u0440\u0430\u043c\u043c\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u044e"
rating: 0
weight: 103361
solve_time_s: 48
verified: true
draft: false
---

[CF 103361H - \u041d\u043e\u0436\u043d\u0438\u0446\u044b](https://codeforces.com/problemset/problem/103361/H)

 **評価:** -
 **タグ:** -
 **解決時間:** 48 秒
 **確認済み:** はい

 ## 解決策
 ## 問題の理解

 サイズの長方形が与えられます$m \times n$そして、固定サイズの小さな長方形を使用してそれを完全にタイル表示したいと考えています。$1 \times k$。 各タイルはどちらの方向にも配置できるため、次のように使用できます。$1 \times k$または$k \times 1$, そして、大きな長方形を重なりや隙間なくこのような部分にカットすることができます。 課題は、そのような完璧なタイリングが存在するかどうかを判断することです。 

入力は 3 つの整数で構成されます$m$、$n$、 そして$k$、それぞれ最大$10^9$。 テスト ケースは 1 つだけであり、すべての操作は定数時間の算術チェックであるため、どのソリューションも定数時間で実行する必要があります。 

主要な構造上の制約は、すべてのタイルが常に正確にカバーすることです。$k$単位正方形。 それはすぐに、その地域が$m \cdot n$で割り切れる必要があります$k$。 ただし、細長い長方形をグリッドに当てはめる幾何学形状により、行と列に沿った配置に関して追加の制約が課されるため、割り算だけでは十分ではありません。 

微妙なエッジケースが現れるのは、$k = 1$。 この場合、すべてのセルはすでに有効なタイルであるため、任意の四角形を簡単にタイル化できます。 もう 1 つのエッジ ケースは、1 つの次元がすでに 1 である場合に発生します。その場合、事実上 1D セグメントを操作していることになるからです。 

単純な間違いは、エリアの分割可能性のみをチェックすることです。 例えば、$m = 2, n = 3, k = 4$面積 6 が得られますが、これは 4 で割り切れないため、正確には不可能です。 しかし、考えてみましょう$m = 2, n = 3, k = 2$: 面積は 6 で、2 で割り切れます。実際に機能します。 本当の課題は、分割可能であるが配置が不可能なケースを区別することです。$m = 6, n = 6, k = 4$ここで、面積は 36 で 4 で割り切れますが、達成可能なストリップ構成が一致しないため、タイリングは不可能です。 

## アプローチ

 ブルートフォースアプローチでは、配置をシミュレートしようとします。$1 \times k$または$k \times 1$グリッド内のタイルを配置し、すべての配置をバックトラックします。 配置ごとに利用可能なスペースが減少するため、残りの領域を再帰的に埋めようとします。 各ステップで多くの可能な配置があり、控えめなグリッドであっても、これはすぐに指数関数的に増加するため、状態の数は非常に速く増加します。$m \cdot n$。 これは正確性の参照としてのみ役立ちます。 

重要な点は、長方形が均一であり、タイルも均一であるため、有効なタイルはローカル検索構造ではなくグローバル算術制約を尊重する必要があるということです。 各タイルは正確に貢献します$k$セルなので、総面積は次のように割り切れる必要があります。$k$。 それ以上に、唯一の構造上の制限は、長さのストリップを整列できるかどうかによって決まります。$k$いずれかの次元に沿って。 

水平を使用してタイル化しようとすると$1 \times k$ストリップ、必要です$n$で割り切れる$k$。 同様に、垂直方向を使用してタイル化すると、$k \times 1$ストリップ、必要です$m$で割り切れる$k$。 ただし、方向を混合することが許可されているため、単一のグローバル方向に制限されるわけではありません。 残りの自明ではないケースは、どちらの次元も単独では次の要素で割り切れない場合です。$k$。 その場合、整数グリッドの境界を尊重する方法で両方の方向を組み合わせる必要があります。これは、両方の方向が一致する場合にのみ可能であることが判明します。$m$そして$n$～と互換性のあるものの倍数でもある$k$、そして唯一の障害が生じるのは次の場合です。$k$両方の次元にわたってきれいに分解することはできません。 

構造をより正確に見る方法は、長方形を一定のサイズのブロックに分割するという観点から考えることです。$k$。 そのようなブロックはすべて、次の倍数で整列された行または列の内側に完全に存在する必要があります。$k$両方向にまたがる場合を除いて、一貫性条件が強制されます。$m \bmod k$そして$n \bmod k$。 最後の単純化は、少なくとも 1 つの次元を長さのセグメントに完全に分割できる場合にのみ、タイリングが可能であるということです。$k$、つまり、$m \bmod k = 0$または$n \bmod k = 0$。 どちらの次元も割り切れない場合$k$、方向を混合しようとすると、完了できない不可避の残りのフラグメントが作成されます。 

したがって、決定は 2 つの単純な条件をチェックすることに帰着します。それは、領域の分割可能性と、少なくとも 1 つの次元で分割できることです。$k$。 1 つの次元が分割可能になると、実際には面積条件が暗示されるため、最終チェックが簡単になります。 

| アプローチ | 時間計算量 | 空間の複雑さ | 評決 |
 | --- | --- | --- | --- |
 | ブルート フォース タイリング検索 | 指数関数 | O(mn) | 遅すぎる |
 | 算術 + 割り算推論 | お(1) | お(1) | 承認済み |

 ## アルゴリズムのチュートリアル

 1. 値を読み取る$m$、$n$、 そして$k$。 これらはグリッドとタイルの形状を定義します。 
2. もし$k = 1$、すぐに「YES」を出力します。 すべてのセルはすでに有効です$1 \times 1$タイルなので、長方形はいつでもタイル表示可能です。 
3. かどうかを確認します。$m \cdot n$で割り切れます$k$。 そうでない場合は「NO」を出力します。 各タイルが正確にカバーするため、これは必要です。$k$セルなので、合計面積はタイル面積の整数倍でなければなりません。 
4. 次の少なくとも 1 つが存在するかどうかを確認します。$m$または$n$で割り切れます$k$。 「はい」の場合は「YES」を出力します。 この場合、長方形をその寸法に揃えられたストリップに分割し、各ストリップは回転されたタイルまたは回転されていないタイルを使用して有効なタイルを形成できます。 
5. どちらの次元も割り切れない場合$k$、「NO」を出力します。 この状況では、$k$-length の長方形は、最終的には完了できない残りのストリップを残します。 

### なぜ効果があるのか

 すべてのタイルは常に正確に消費します$k$したがって、セルの総数は次の数で割り切れる必要があります。$k$。 それは全体的な必要条件を与えます。 2 番目の制約は位置合わせから生じます。つまり、少なくとも 1 つの次元が割り切れないことです。$k$、長さへの分解 -$k$セグメントは、互換性のない残留ストリップを残す方法で境界を越える必要があります。 1 つの次元が次の数で割り切れる場合$k$、最初にその次元に沿って分割し、問題を独立したものに減らすことができます。$k$-by-1 または 1-by-$k$ストリップ、それぞれは簡単にタイル化可能です。 これら 2 つの条件を組み合わせることで、完璧なタイリングが存在することを完全に特徴付けることができます。 

## Python ソリューション```python
import sys
input = sys.stdin.readline

m, n, k = map(int, input().split())

if k == 1:
    print("YES")
elif (m * n) % k != 0:
    print("NO")
elif m % k == 0 or n % k == 0:
    print("YES")
else:
    print("NO")
```コードは決定プロセスに直接続きます。 最初の条件は、タイルが単一セルである縮退ケースを分離します。 2 つ目は、グローバル エリア制約を適用します。 最終チェックでは、きれいなストリップ分解が少なくとも 1 つの方向に存在するかどうかをキャプチャします。これは、フラグメントが残らないようにするための構造要件です。 

微妙な点は、次の順序です。$k = 1$まず、Python が大きな整数を安全に処理する場合でも、整数サイズが固定された言語での不必要な乗算と潜在的なオーバーフローの問題を回避します。 

## 実用的な例

 ### 例 1:$2, 3, 2$条件を段階的に評価します。 

| ステップ | メートル | n | k | m・n % k | m%k | n%k | 決定 |
 | --- | --- | --- | --- | --- | --- | --- | --- |
 | 開始 | 2 | 3 | 2 | - | - | - | 計算する |
 | エリアチェック | 2 | 3 | 2 | 6 % 2 = 0 | - | - | パス |
 | 割り切れる次元 | 2 | 3 | 2 | - | 0 | 1 | はい |

 以来$m$で割り切れます$k$、グリッドをサイズの垂直ストリップに分割できます$2 \times 1$、それぞれのタイルを回転させたサイズのタイルで埋めることができます。$2 \times 1$。 これにより実現可能性が確認されます。 

### 例 2:$6, 6, 4$| ステップ | メートル | n | k | m・n % k | m%k | n%k | 決定 |
 | --- | --- | --- | --- | --- | --- | --- | --- |
 | 開始 | 6 | 6 | 4 | - | - | - | 計算する |
 | エリアチェック | 6 | 6 | 4 | 36 % 4 = 0 | - | - | パス |
 | 割り切れる次元 | 6 | 6 | 4 | - | 2 | 2 | いいえ |

 総面積が 4 で割り切れる場合でも、どちらの寸法も 4 の倍数に一致しないため、どのような配置でも$1 \times 4$または$4 \times 1$タイルは最終的に、タイル化できない幅または高さ 2 の残りの領域を作成します。 

## 複雑さの分析

 | 測定 | 複雑さ | 説明 |
 | --- | --- | --- |
 | 時間 | お(1) | 一定数の算術演算とモジュロ チェックのみ |
 | スペース | お(1) | 追加のデータ構造は使用されません。 

この解は、入力の大きさに関係なく定数時間の演算のみを実行するため、制約内に簡単に適合します。 

## テストケース```python
import sys, io

def run(inp: str) -> str:
    sys.stdin = io.StringIO(inp)
    import sys
    input = sys.stdin.readline

    m, n, k = map(int, input().split())

    if k == 1:
        return "YES"
    elif (m * n) % k != 0:
        return "NO"
    elif m % k == 0 or n % k == 0:
        return "YES"
    else:
        return "NO"

# provided samples
assert run("2 3 2") == "YES"
assert run("2 3 3") == "YES"

# custom cases
assert run("1 1 2") == "NO"
assert run("4 4 2") == "YES"
assert run("6 6 4") == "NO"
assert run("5 5 1") == "YES"
```| テスト入力 | 期待される出力 | 検証内容 |
 | --- | --- | --- |
 | 1 1 2 | いいえ | エリアの不一致のため不可能 |
 | 4 4 2 | はい | 両方の寸法が均等なきれいなタイリング |
 | 6 6 4 | いいえ | 割り切れるのが十分ではありません |
 | 5 5 1 | はい | 自明な k = 1 の場合 |

 ## 特殊なケース

 ケース$k = 1$幾何学的制約が完全に削除されるため、他のすべての入力とは異なる動作をします。 入力用$1\ 1\ 1$の場合、アルゴリズムはすぐに YES を返し、長方形がすでにタイル化されているという事実と一致します。 

いつ$m \cdot n$で割り切れない$k$、 のような$3\ 3\ 2$、アルゴリズムは構造的推論の前に正しく NO を返します。 エリアチェックで実行が停止するため、誤受付を防止します。 

どちらの次元も割り切れない場合$k$、 のような$6\ 6\ 4$、アルゴリズムは最終条件に到達し、正しく NO を返します。 36 は 4 で割り切れますが、きれいなストリップ分解が存在しないため、カバーできない領域が残ります。 

1 つの次元が次と完全に等しい場合$k$、 のような$4\ 7\ 4$の場合、アルゴリズムは YES を返します。$m \bmod k = 0$、グリッドは一定のサイズの垂直ストリップに分割できます。$4 \times 1$、それぞれ独立してタイル表示可能。
