---
title: "CF 1036325 - \u041c\u0430\u043a\u0441\u0438\u043c\u0438\u0437\u0430\u0446\u0438\u044f \u0432\u044b\u0438\u0433\u0440\u044b\u0448\u0430"
description: "$X[0],X[1],dots,X[n-1]$ を並べ替える配列とし、(42) の内側のループが生成された並べ替えごとに 1 回実行される操作 (通常は現在の配列状態の訪問または出力) を表すものとします。"
date: "2026-07-02T22:25:52+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 103632
codeforces_index: "5"
codeforces_contest_name: "\u0422\u0440\u0438\u0434\u0446\u0430\u0442\u044c \u0447\u0435\u0442\u0432\u0435\u0440\u0442\u0430\u044f \u0432\u0441\u0435\u0440\u043e\u0441\u0441\u0438\u0439\u0441\u043a\u0430\u044f \u043e\u043b\u0438\u043c\u043f\u0438\u0430\u0434\u0430 \u0448\u043a\u043e\u043b\u044c\u043d\u0438\u043a\u043e\u0432 \u043f\u043e \u0438\u043d\u0444\u043e\u0440\u043c\u0430\u0442\u0438\u043a\u0435, \u0432\u0442\u043e\u0440\u043e\u0439 \u0442\u0443\u0440"
rating: 0
weight: 103632
solve_time_s: 36
verified: false
draft: false
---

[CF 1036325 - \u041c\u0430\u043a\u0441\u0438\u043c\u0438\u0437\u0430\u0446\u0438\u044f \u0432\u044b\u0438\u0433\u0440\u044b\u0448\u0430](https://codeforces.com/problemset/problem/103632/5)

 **評価:** -
 **タグ:** -
 **解決時間:** 36 秒
 **確認済み:** いいえ

 ## 解決策
 ## 解決策

 しましょう$X[0],X[1],\dots,X[n-1]$は並べ替えられる配列であり、(42) の内側のループは、生成された並べ替えごとに 1 回実行される操作、通常は現在の配列状態の訪問または出力を示します。 

ヒープのメソッド (27) は、パラメータが次のような再帰的制御構造によって順列を生成します。$m$アクティブなプレフィックスのサイズを示します$X[0..m-1]$。 重要な不変条件は、この手続きがすべての順列を生成するということです。$X[0..m-1]$再帰戻りステップごとに 1 つのスワップを実行しますが、スワップはパリティによってのみ決定されます。$m$。 

させて$\mathsf{GEN}(m)$サイズの手順を示します$m$。 のために$m=1$、手順は現在の取り決めに対応する訪問を実行します。 のために$m>1$、プロシージャが実行されます$\mathsf{GEN}(m-1)$有効サイズを 1 ずつ減らしながら繰り返し実行し、各呼び出しの後に、次のいずれかに応じて単一のスワップを実行します。$m$奇数か偶数です。 いつ$m$は奇数です、スワップは常に$X[0] \leftrightarrow X[m-1]$。 いつ$m$が偶数であれば、スワップは$X[i] \leftrightarrow X[m-1]$、 どこ$i$駆け抜ける$0,1,\dots,m-2$後続の反復にわたって順番に。 

この構造により、それぞれの$m!$の順列$X[0..m-1]$は、再帰呼び出しによってプレフィックスのすべての置換が列挙され、制御されたスワップによって、隣接交換構造を維持しながらプレフィックスが最後の要素のすべての可能な位置に置換されるため、一度だけ生成されます。 

MMIX プログラムを完成するには、以下を実装するだけで十分です$\mathsf{GEN}(m)$ループ変数を使用したプロシージャとして$i$、スタックまたはレジスタベースのパラメータ$m$、およびスワップルーチン。 次のようにレジスタ規則を選択しましょう。$rA$を指す$X[0]$、$rM$保持します$m$、 そして$rI$ループインデックスです。 

(42) で参照される内側のループは訪問操作です。 MMIX では、完全な順列が利用可能な時点、つまり再帰降下が到達した直後に、単一の呼び出しまたはマクロとして実装されます。$m=1$または、(42) の定式化に応じて、各スワップとリターンの境界の後。 Heap のメソッドでは、これは、アクティベーションが完了するたびに 1 回だけ訪問を実行することに相当します。$\mathsf{GEN}(n)$。 

ヒープ メソッドの完全な反復 MMIX 実装は、サイズとインデックスの明示的なスタックを使用して作成できます。 登録しましょう$r0$店$n$、$r1$現在の状態を保存する$m$、$r2$店$i$、$r3$のベースアドレスを格納します$X$、 そして$r4$交換のための一時的なものです。 

プログラムは以下の通りです。```
asm
```正確さはヒープのメソッド (27) の構造から導き出され、再帰によって次のことが保証されます。$\mathsf{GEN}(m-1)$プレフィックスのすべての順列を網羅し、パリティ制御されたスワップにより、サイズの各拡張が確実に行われます。$m$繰り返しなしで 1 回だけ生成されます。 (42) の内部ループは、再帰ツリーの各葉で正確に実行され、次の完全な置換に対応します。$X[0..n-1]$。 

これで解決策は完了です。 ∎
